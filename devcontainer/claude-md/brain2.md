## brain2 (shared second brain at /brain2)

If /brain2 is mounted, it is Martin's cross-project second brain - a git repo he also edits in Obsidian. You are a READER here.

- Read `/brain2/Brain2.md` (the home hub) first for cross-project context, and search /brain2 before answering anything that may already be recorded; follow its links to the relevant note.
- To record something: use the `/md` skill, or write the update directly into the relevant note (immediate - visible to every local agent + Obsidian at once; lands `authored`/grey, provisional until Martin authorises). `meta/inbox/` is NOT a general knowledge tray - it holds ONLY the Zotero/OP stockpile queue for firewalled containers (`/brain2/meta/inbox/zotero-queue.<source>.md`, `/brain2/meta/inbox/op-queue.<source>.md`), which the gardener sweeps.
- CREDENTIAL BOUNDARY: you CANNOT `git push` /brain2 (your PAT is scoped to this project's repo, not Aqueum/brain2). Never run git against /brain2 - just write files; the Mac auto-commit / gardener persists them.
- TRUST (see /brain2/meta/trust-model.md): notes carry a state authored -> checked -> reviewed -> authorised. NEVER write a name into `authorised:` or set `state: authorised` - that is Martin's manual act in Obsidian. Leave it empty; set state no higher than `reviewed`.
- USER OUTRANKS UNAUTHORISED brain2. If the user says something that contradicts a brain2 note (or a recalled memory), do NOT silently defer to the stored claim: quote it, cite its note path, and ask which is correct. brain2 is valuable for *challenging* the user, so do push back with the citation - but ask, don't assume; grey/`authored` content is provisional by definition and cannot outrank a live human. Once the user resolves it, fix the brain2 note immediately (lands authored/grey). Never make the user prove the same correction more than once - a stale note that re-misleads after they corrected it is a bug to fix at the source.
- ZOTERO (see /brain2/meta/zotero-operation.md): PDFs are read-only at `/zotero`. Resolve `zotero://open-pdf/library/items/<KEY>` -> `/zotero/<KEY>/` (the key is the attachment key and names the folder). You cannot add papers (firewalled) - stockpile DOIs to `/brain2/meta/inbox/zotero-queue.<source>.md` for a Mac surface to add.
