Verdict: pass. `smoke/checks_23_codex_panel.py` (18 test functions, 90 assertions, 0 failures) covers every AC7 item against `devcontainer/codex-panel.mjs` — AC1 exit codes with exact 0/2/2+N vendor-process counts, AC2 ordered argv pin against `vibe-delegate.mjs review codex`'s own recorded golden (path-normalised, since scratch dirs are inherently distinct per process), AC3 blind stdin + concurrency overlap, AC4 collection shape and incomplete-panel handling, AC5 all four `nonce_check` outcomes plus `retention`, AC6 doc strings, no repo writes, no secret forwarding.
Concerns count: 0 blocking; 1 note — the `.jsonl.zst`-only rollout scenario is skipped (not failed) because `zstd` is not installed on this host, per the task's own instruction to skip with a note in that case.
Key concern: none blocking. `python3 code-check.py` clean (23 files) and `python3 smoke-test.py < /dev/null` fully green, including the new suite.
