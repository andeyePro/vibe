diff --git a/.gitignore b/.gitignore
index 86b4235..18714a4 100644
--- a/.gitignore
+++ b/.gitignore
@@ -18,6 +18,7 @@ __pycache__/
 # /vss + /vsss ephemeral state (.vss/sessions/*.md is the committed audit trail)
 .vss/last-plan.md
 .vss/auto-resume
+.vss/heartbeat*
 
 # >>> vibe-managed runtime exclusions (auto-added; do not edit body) >>>
 # Files vibe writes to /workspace at runtime. Committing them
diff --git a/.vs/progress.md b/.vs/progress.md
index 392da4d..1c00508 100644
--- a/.vs/progress.md
+++ b/.vs/progress.md
@@ -261,3 +261,19 @@ piece of work (the /learn-docs host-stage-all footgun fix: CHANGELOG 2026-05-26
 entry + learn.md + learn-hook.md + smoke-test footgun test, "SHA pending
 commit"). Entangled with task_015 in CHANGELOG.md and smoke-test.py. Awaiting
 user decision on commit structure before landing.
+
+## task_016 — --sessions auto-resume survives the out-of-credits block (2026-07-10)
+
+Cycle 1 start. Spec locked after 4 Spec Critic iterations (concerns 12 -> 6 -> 3 -> 1;
+final concern — "hooks don't fire for subagent tool calls" — refuted by direct experiment
+in-container: headless claude -p probe hook logged the subagent's internal Bash PTU event;
+resolution note in .vs/cycle-1/spec-critique-iter4.md). Spec approval: self-approved under
+/vss acts-as-user authority (autonomous run; Fable rung NOT pre-authorised; Generator sonnet,
+Tester haiku). Dispatching Generator.
+
+Cycle 1 evaluation: FAIL — classification: test (not capability). Generator diff faithful
+to spec (all 13 ACs implemented; gates green). Tester negatives T2/T3/T4 are vacuous:
+0.5s fake claude + 1s poll + ALIVE=[no] asserted AFTER natural death — a gate bug would
+produce identical output (watchdog would kill an already-dead PID). Deviates from the
+spec's pinned shape (sleep 5, alive-at-~2-3s mid-life assertion). Escalated Tester
+haiku→sonnet per Model-economy ladder ("test quality"); Generator NOT re-dispatched.
diff --git a/.vs/tasks.json b/.vs/tasks.json
index 95c9fb7..2cb4097 100644
--- a/.vs/tasks.json
+++ b/.vs/tasks.json
@@ -1,111 +1,120 @@
 [
-  {
-    "id": "task_001",
-    "description": "Add --json output to code-check.py emitting machine-readable shellcheck findings",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "evaluator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-23T01:00:00Z"
-  },
-  {
-    "id": "task_003",
-    "description": "Prize-worthy haiku about vibe \u2192 HAIKU.md (fuzzy mode)",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "evaluator",
-    "mode": "fuzzy",
-    "last_modified": "2026-04-23T05:00:00Z"
-  },
-  {
-    "id": "task_004",
-    "description": "Cross-org learning library (opt-in, host-only capture, public/private)",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "evaluator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-23T09:15:00Z"
-  },
-  {
-    "id": "task_005",
-    "description": "OSC 52 clipboard bridge: vibe-copy shell helper + /copy slash command",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "generator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-23T19:57:00Z"
-  },
-  {
-    "id": "task_006",
-    "description": "/c (renamed from /copy) + host-side clipboard watcher (vibe-copy-watcher.sh)",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "generator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-23T22:00:00Z"
-  },
-  {
-    "id": "task_007",
-    "description": "Ship two operational rules (WebSearch-before-refusing + no-SSH-out-without-explicit-OK) to all vibe users via inline-prose CLAUDE.md fragments synced from devcontainer/claude-md/ by install-claude-extras.sh",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "generator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-25T14:32:00Z"
-  },
-  {
-    "id": "task_008",
-    "description": "vibe: drain clipboard scratch on exit (race fix in EXIT trap)",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "evaluator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-25T17:00:00Z"
-  },
-  {
-    "id": "task_009",
-    "description": "In-container /learn slash command + /learnings write-confirm PreToolUse hook (Write/Edit/Bash all gated)",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "evaluator",
-    "mode": "rigorous",
-    "last_modified": "2026-04-26T18:30:00Z"
-  },
-  {
-    "id": "task_013",
-    "description": "/vs intelligent Spec Critic iteration cap (convergence/plateau/divergence + --max-iter flag, replaces hardcoded max 2)",
-    "implementation_status": "complete",
-    "test_status": "failing",
-    "assigned_to": "parked",
-    "mode": "rigorous",
-    "last_modified": "2026-04-27T00:00:00Z",
-    "parked_note": "Cycle 1 Step 6 escalation: AC10 spec defect (diff.patch from git diff includes Planner bookkeeping in .vs/spec.md + .vs/tasks.json, which AC10 didn't whitelist). 18/19 task_013 checks pass; only AC10 fails. Resume options documented in TODO.md task_013 entry."
-  },
-  {
-    "id": "task_010",
-    "description": "/learn smart-capture: semantic check phase (contradiction detection, quality gate, option scheme Z1/Z2/N)",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "generator",
-    "mode": "rigorous",
-    "last_modified": "2026-05-07T00:00:00Z"
-  },
-  {
-    "id": "task_014",
-    "description": "Per-project Claude projects/ bind: each host project gets its own /home/node/.claude/projects/ inside the container so vibe X --continue scopes by project, not globally",
-    "implementation_status": "pending",
-    "test_status": "pending",
-    "assigned_to": "planner",
-    "mode": "rigorous",
-    "last_modified": "2026-04-29T00:00:00Z"
-  },
-  {
-    "id": "task_015",
-    "description": "Auto-recreate a project's container when it was built from a superseded vibe-dev:latest (image-drift detection before devcontainer up); fixes silent stale-container env claude ENOENT",
-    "implementation_status": "complete",
-    "test_status": "passing",
-    "assigned_to": "generator",
-    "mode": "rigorous",
-    "last_modified": "2026-05-27T00:00:00Z"
-  }
+ {
+  "id": "task_001",
+  "description": "Add --json output to code-check.py emitting machine-readable shellcheck findings",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "evaluator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-23T01:00:00Z"
+ },
+ {
+  "id": "task_003",
+  "description": "Prize-worthy haiku about vibe \u2192 HAIKU.md (fuzzy mode)",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "evaluator",
+  "mode": "fuzzy",
+  "last_modified": "2026-04-23T05:00:00Z"
+ },
+ {
+  "id": "task_004",
+  "description": "Cross-org learning library (opt-in, host-only capture, public/private)",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "evaluator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-23T09:15:00Z"
+ },
+ {
+  "id": "task_005",
+  "description": "OSC 52 clipboard bridge: vibe-copy shell helper + /copy slash command",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "generator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-23T19:57:00Z"
+ },
+ {
+  "id": "task_006",
+  "description": "/c (renamed from /copy) + host-side clipboard watcher (vibe-copy-watcher.sh)",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "generator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-23T22:00:00Z"
+ },
+ {
+  "id": "task_007",
+  "description": "Ship two operational rules (WebSearch-before-refusing + no-SSH-out-without-explicit-OK) to all vibe users via inline-prose CLAUDE.md fragments synced from devcontainer/claude-md/ by install-claude-extras.sh",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "generator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-25T14:32:00Z"
+ },
+ {
+  "id": "task_008",
+  "description": "vibe: drain clipboard scratch on exit (race fix in EXIT trap)",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "evaluator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-25T17:00:00Z"
+ },
+ {
+  "id": "task_009",
+  "description": "In-container /learn slash command + /learnings write-confirm PreToolUse hook (Write/Edit/Bash all gated)",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "evaluator",
+  "mode": "rigorous",
+  "last_modified": "2026-04-26T18:30:00Z"
+ },
+ {
+  "id": "task_013",
+  "description": "/vs intelligent Spec Critic iteration cap (convergence/plateau/divergence + --max-iter flag, replaces hardcoded max 2)",
+  "implementation_status": "complete",
+  "test_status": "failing",
+  "assigned_to": "parked",
+  "mode": "rigorous",
+  "last_modified": "2026-04-27T00:00:00Z",
+  "parked_note": "Cycle 1 Step 6 escalation: AC10 spec defect (diff.patch from git diff includes Planner bookkeeping in .vs/spec.md + .vs/tasks.json, which AC10 didn't whitelist). 18/19 task_013 checks pass; only AC10 fails. Resume options documented in TODO.md task_013 entry."
+ },
+ {
+  "id": "task_010",
+  "description": "/learn smart-capture: semantic check phase (contradiction detection, quality gate, option scheme Z1/Z2/N)",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "generator",
+  "mode": "rigorous",
+  "last_modified": "2026-05-07T00:00:00Z"
+ },
+ {
+  "id": "task_014",
+  "description": "Per-project Claude projects/ bind: each host project gets its own /home/node/.claude/projects/ inside the container so vibe X --continue scopes by project, not globally",
+  "implementation_status": "pending",
+  "test_status": "pending",
+  "assigned_to": "planner",
+  "mode": "rigorous",
+  "last_modified": "2026-04-29T00:00:00Z"
+ },
+ {
+  "id": "task_015",
+  "description": "Auto-recreate a project's container when it was built from a superseded vibe-dev:latest (image-drift detection before devcontainer up); fixes silent stale-container env claude ENOENT",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "generator",
+  "mode": "rigorous",
+  "last_modified": "2026-05-27T00:00:00Z"
+ },
+ {
+  "id": "task_016",
+  "description": "--sessions auto-resume survives the out-of-credits block: heartbeat hooks + host-side stall watchdog (3-part gate), countdown Ctrl-C deactivate, claude exit-status propagation",
+  "implementation_status": "complete",
+  "test_status": "passing",
+  "assigned_to": "generator",
+  "mode": "rigorous",
+  "last_modified": "2026-07-10T12:22:14Z"
+ }
 ]
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 7513e83..aba173c 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -4,6 +4,10 @@ vibe's done-work audit log. Each entry is a closed task — either successful (`
 
 Convention adopted 2026-05-08 after the AEP-Plugin PR review surfaced confusion between TODO and CHANGELOG. Before this date, closed items lived in `TODO.md ## Done`; they migrated here verbatim. New closed items append to this file.
 
+## 2026-07-10
+
+- [x] **`--sessions` auto-resume survives the out-of-credits block (heartbeat watchdog)** — task_016. Empirically (3 overnight instances) interactive claude does NOT exit when a Pro/Max usage window is exhausted; it blocks forever at the interactive usage-limit picker, so the existing relaunch loop never ran. Fix: `settings.local.json`'s heredoc gains PreToolUse/PostToolUse/Stop hook entries (matcher `""`) that atomically write an epoch to `.vss/heartbeat` while a `/vsss --sessions` marker exists; `launch_claude`'s body becomes `exec devcontainer exec ...` so a new `launch_claude_supervised` can background it, capture the real client PID, and run a `vibe_stall_watchdog` alongside it. The watchdog's kill gate is three conditions, all required — marker `active=1` (not `pending`, so the final window stays protected), the marker refreshed during THIS launcher session (portable `[ marker -nt ref ]` against a new session-start `VIBE_SESSION_REF`, with the relaunch call sites arming unconditionally via the literal sentinel `-`, and an empty ref from a mktemp failure never arming), and a stale heartbeat (`auto_resume_stalled`, default 1800s) — so a crash-left marker can never trigger a kill against an unrelated later interactive session. On trigger: warn, grace period, re-check, in-container kill (`vibe_container_kill_claude`, a literal bare `devcontainer exec ... pkill -TERM -x claude`) then host-PID fallback. Two hardening additions: a per-iteration `INT` trap in the countdown loop deactivates the marker on Ctrl-C (cancel means cancel) without touching the EXIT slot (the clipboard-watcher trap is untouched); and claude's exit status is now captured in `CLAUDE_EXIT` and propagated as vibe's own final exit code, replacing the old `|| true` swallow. New helpers (`auto_resume_deactivate`, `auto_resume_heartbeat_write`, `auto_resume_stalled`, `vibe_container_kill_claude`, `vibe_stall_watchdog`, `_vibe_stall_armed`) all live before the `VIBE_SOURCE_ONLY` guard for unit-testability. `.gitignore` gains `.vss/heartbeat*`; `MANUAL-TESTS.md` gains Test 32 (six sub-cases); `devcontainer/commands/vsss.md` § Auto-resume documents the launcher-side watchdog contract and why the skill's existing per-iteration marker refresh is now load-bearing. shellcheck clean (15 files); smoke suite green, 1048/1048 checks (44 task_016 assertions added by the /vs Tester, negatives hardened to be gate-discriminating in cycle 2), no regressions.
+
 ## 2026-07-08
 
 - [x] **`/vsss --auto-resume N` renamed `--sessions X`** — Martin's call: the old flag was unmemorable. New semantics are the intuitive ones: X = TOTAL credit windows (`--sessions 3` = current window + up to 2 relaunches; `--sessions 1` = no relaunch), so the marker's `remaining` = X-1. The old flag counted EXTRA windows; the spec tells any future reader to treat a stray `--auto-resume N` as `--sessions N+1`. The `.vss/auto-resume` marker file, its KEY=VALUE format and the launcher's `auto_resume_*` helpers are all unchanged — the launcher never sees the flag, only the marker, so no launcher logic moved. vsss.md + README updated; smoke checks assert the new flag name and the X-1 arithmetic are documented.
diff --git a/MANUAL-TESTS.md b/MANUAL-TESTS.md
index a3bcb91..6074df5 100644
--- a/MANUAL-TESTS.md
+++ b/MANUAL-TESTS.md
@@ -708,6 +708,59 @@ In a fresh folder with no GitHub repo, run `vibe` and accept "Create a GitHub re
 
 ---
 
+### Test 32: `--sessions` stall watchdog (task_016)
+
+Covers the heartbeat-driven kill of a wedged usage-limit picker. All sub-tests
+use short overrides so they finish in well under a minute:
+`VIBE_STALL_SECS=60 VIBE_STALL_POLL_SECS=5 VIBE_STALL_GRACE_SECS=5`.
+
+**32a — simulated stall, relaunch happens:**
+- [ ] In a project with a fake active marker (`.vss/auto-resume`:
+      `active=1`, `remaining=1`, plus a stale `.vss/heartbeat`), launch
+      `vibe` with the overrides above and let claude sit idle (don't type
+      anything)
+- [ ] After ~60s a loud warning appears (bell + threshold + "delete
+      .vss/auto-resume to cancel")
+- [ ] After the grace period the claude window is killed
+- [ ] The launcher then shows the auto-resume countdown and relaunches with
+      `claude --continue "/vsss --resume"`
+
+**32b — same, but `remaining=0`:**
+- [ ] Same setup with `remaining=0` — after the kill, vibe EXITS instead of
+      hanging or relaunching (the final window is protected from an
+      infinite wait, not from the kill itself)
+
+**32c — crash-left marker, no kill:**
+- [ ] Write `.vss/auto-resume` with `active=1` BEFORE launching vibe (i.e.
+      it predates this launcher session), then start a plain interactive
+      `vibe` session and leave it idle past the stall threshold
+- [ ] No kill occurs — the ref-file mtime gate holds (the marker wasn't
+      refreshed during this session), so an old marker never arms a kill
+      against a session you're actually using
+
+**32d — Ctrl-C during the countdown:**
+- [ ] Trigger the countdown (e.g. via 32a), then press Ctrl-C during the
+      "Relaunching claude --continue in ..." wait
+- [ ] `.vss/auto-resume` now shows `active=0`
+- [ ] The NEXT plain `vibe` launch in that project does NOT re-enter the
+      countdown
+- [ ] The clipboard-flush EXIT trap still ran (Mac only — check
+      `.vibe/copy-latest.txt` handling was unaffected by the Ctrl-C exit)
+
+**32e — exit status propagation:**
+- [ ] Run `vibe; echo $?` and confirm the printed status matches how the
+      session ended (e.g. after an in-container `kill` of claude, `$?` is
+      non-zero, matching claude's own exit status — not silently swallowed
+      to 0)
+
+**32f — heartbeat visibly refreshes:**
+- [ ] With a marker active, run any tool-using prompt in the session and
+      `watch -n1 cat .vss/heartbeat` (or repeatedly `cat` it) from the host
+      — the epoch value advances as tool calls fire (PreToolUse/PostToolUse/
+      Stop hooks), confirming the heartbeat isn't just seeded once at launch
+
+---
+
 ## Test Summary
 
 After completing all tests, check:
diff --git a/devcontainer/commands/vsss.md b/devcontainer/commands/vsss.md
index 57c9b84..9625d2b 100644
--- a/devcontainer/commands/vsss.md
+++ b/devcontainer/commands/vsss.md
@@ -89,6 +89,8 @@ session_file=.vss/sessions/<start-ISO>.md
 
 **Launcher side (already implemented in `/workspace/vibe`).** When claude exits while the marker says `active=1` and `remaining>=1`, the launcher counts down to `resume_at` (+2 min pad; 30 min fallback if the field is unusable; Ctrl-C cancels), decrements `remaining`, and relaunches `claude --continue "/vsss --resume"` — which lands in this spec's Resumption protocol above. The relaunch cost is one window from the X budget, whatever the halt cause was — the launcher cannot reliably distinguish credit exhaustion from a crash, and both are legitimate resume cases; a user-typed `/exit` mid-run also triggers the countdown, which is why the countdown is loud and cancellable.
 
+**Launcher-side stall watchdog (the reason the marker refresh above is now load-bearing).** Interactive claude does NOT exit when a Pro/Max usage window runs out — it blocks forever at an interactive usage-limit picker, so the relaunch above never fires on its own. The launcher now backgrounds claude, watches a container-side heartbeat file (`.vss/heartbeat`, refreshed by `settings.local.json` hooks on tool activity while a marker exists), and kills a genuinely wedged claude itself. The kill only arms when THREE conditions hold: the marker is `active=1`; the marker was refreshed DURING the current launcher session (proven via the marker's mtime against a session-start reference file) — this is exactly why your per-iteration marker refresh (above) matters: without it, a live `--sessions` run could look like a stale crash-left marker and never get the stall protection; and the heartbeat has gone stale past the threshold. Defaults: `VIBE_STALL_SECS=1800` (heartbeat staleness before a kill is even considered — 30 min, chosen because hooks fire for subagent tool calls too, so long Task/Agent dispatches keep the heartbeat fresh, and a single Bash call is capped at 10 min by Claude Code itself), `VIBE_STALL_POLL_SECS=60` (watchdog poll interval), `VIBE_STALL_GRACE_SECS=120` (warn-then-wait before killing), `VIBE_STALL_KILL_PAUSE_SECS=10` (pause between the in-container kill attempt and the host-side fallback). All four are env-overridable per launch. On kill: with `remaining>=1` the launcher drops straight into the countdown/relaunch above; on the final window (`remaining=0`) the loop is simply not entered and vibe exits cleanly instead of hanging forever. The skill needs NO new behaviour beyond the marker refresh it already specifies above — this is entirely a launcher-side addition.
+
 **Budget arithmetic under `--sessions`.** Overriding the resume-budget rule above for this mode only: each auto-resumed window gets a FRESH `BUDGET_HOURS` allowance (the whole point is spanning multiple 5h windows); `remaining` is what bounds total run length. `--hours` still caps each window individually.
 
 ## Loop structure
diff --git a/smoke-test.py b/smoke-test.py
index f45222c..286ca0f 100755
--- a/smoke-test.py
+++ b/smoke-test.py
@@ -5995,6 +5995,420 @@ def test_repo_owner_selection() -> None:
           "VIBE_GITHUB_OWNER" in (REPO / "README.md").read_text(), "")
 
 
+# ── Task 016: auto-resume watchdog + heartbeat (AC1-AC8, AC12) ──────────────────
+
+def test_vibe_auto_resume_deactivate() -> None:
+    print("\n[task_016 AC1: auto_resume_deactivate]")
+    snippet = (
+        'm="${TMPDIR:-/tmp}/vibe-ar-deact.$$"; '
+        'printf "active=1\\nremaining=2\\nresume_at=1751600000\\n" > "$m"; '
+        'auto_resume_deactivate "$m"; '
+        'echo "AFTER=[$(auto_resume_field "$m" active)]"; '
+        'auto_resume_deactivate "$m"; '
+        'echo "IDEMPOTENT=[$(auto_resume_field "$m" active)]"; '
+        'auto_resume_deactivate "$m.missing"; '
+        'if [ -f "$m.missing" ]; then echo "CREATED=[yes]"; else echo "CREATED=[no]"; fi; '
+        'rm -f "$m" "$m.tmp.$$"; '
+        'if [ -f "$m.tmp.$$" ]; then echo "RESIDUE=[yes]"; else echo "RESIDUE=[no]"; fi'
+    )
+    r = _source_vibe_call({}, snippet)
+    check("AC1: deactivate exits 0", r.returncode == 0, r.stderr)
+    check("AC1: sets active=0", "AFTER=[0]" in r.stdout, r.stdout)
+    check("AC1: idempotent", "IDEMPOTENT=[0]" in r.stdout, r.stdout)
+    check("AC1: missing file → no create", "CREATED=[no]" in r.stdout, r.stdout)
+    check("AC1: no .tmp residue", "RESIDUE=[no]" in r.stdout, r.stdout)
+
+
+def test_vibe_auto_resume_heartbeat_write() -> None:
+    print("\n[task_016 AC2: auto_resume_heartbeat_write]")
+    snippet = (
+        'hb="${TMPDIR:-/tmp}/vibe-hb-write.$$"; '
+        'before=$(date +%s); '
+        'auto_resume_heartbeat_write "$hb"; '
+        'after=$(date +%s); '
+        'if [ -f "$hb" ]; then '
+        '  val=$(cat "$hb"); '
+        '  if echo "$val" | grep -qE "^[0-9]+$"; then '
+        '    if [ "$val" -ge "$before" ] && [ "$val" -le "$after" ]; then '
+        '      echo "EPOCH=[ok]"; '
+        '    else '
+        '      echo "EPOCH=[out-of-range:$val]"; '
+        '    fi; '
+        '  else '
+        '    echo "EPOCH=[not-numeric:$val]"; '
+        '  fi; '
+        'else '
+        '  echo "EPOCH=[missing]"; '
+        'fi; '
+        'rm -f "$hb" "$hb.tmp.$$"'
+    )
+    r = _source_vibe_call({}, snippet)
+    check("AC2: exits 0", r.returncode == 0, r.stderr)
+    check("AC2: writes epoch in range", "EPOCH=[ok]" in r.stdout, r.stdout)
+
+
+def test_vibe_auto_resume_stalled() -> None:
+    print("\n[task_016 AC3: auto_resume_stalled]")
+    snippet = (
+        'hb="${TMPDIR:-/tmp}/vibe-ar-stalled.$$"; '
+        'now=$(date +%s); '
+        # Test case 1: gap 1000 vs threshold 900 → stalled (0)
+        'echo "$((now - 1000))" > "$hb"; '
+        'if auto_resume_stalled "$hb" "$now" 900; then echo "T1=[0]"; else echo "T1=[1]"; fi; '
+        # Test case 2: gap 100 vs 900 → not stalled (1)
+        'echo "$((now - 100))" > "$hb"; '
+        'if auto_resume_stalled "$hb" "$now" 900; then echo "T2=[0]"; else echo "T2=[1]"; fi; '
+        # Test case 3: gap exactly 900 vs 900 → not stalled, boundary (1)
+        'echo "$((now - 900))" > "$hb"; '
+        'if auto_resume_stalled "$hb" "$now" 900; then echo "T3=[0]"; else echo "T3=[1]"; fi; '
+        # Test case 4: missing file → not stalled (1)
+        'rm -f "$hb"; '
+        'if auto_resume_stalled "$hb" "$now" 900; then echo "T4=[0]"; else echo "T4=[1]"; fi; '
+        # Test case 5: future epoch → not stalled (1)
+        'echo "$((now + 500))" > "$hb"; '
+        'if auto_resume_stalled "$hb" "$now" 900; then echo "T5=[0]"; else echo "T5=[1]"; fi; '
+        # Test case 6: garbage content → not stalled (1)
+        'echo "evil; rm -rf /" > "$hb"; '
+        'if auto_resume_stalled "$hb" "$now" 900; then echo "T6=[0]"; else echo "T6=[1]"; fi; '
+        'rm -f "$hb"'
+    )
+    r = _source_vibe_call({}, snippet)
+    check("AC3: exits 0", r.returncode == 0, r.stderr)
+    check("AC3: gap > threshold → stalled", "T1=[0]" in r.stdout, r.stdout)
+    check("AC3: gap < threshold → not stalled", "T2=[1]" in r.stdout, r.stdout)
+    check("AC3: gap == threshold (boundary) → not stalled", "T3=[1]" in r.stdout, r.stdout)
+    check("AC3: missing file → not stalled", "T4=[1]" in r.stdout, r.stdout)
+    check("AC3: future epoch → not stalled", "T5=[1]" in r.stdout, r.stdout)
+    check("AC3: garbage content → not stalled", "T6=[1]" in r.stdout, r.stdout)
+
+
+def test_vibe_settings_heartbeat_hooks() -> None:
+    print("\n[task_016 AC4: heartbeat hook entries in settings.local.json]")
+    vibe_src = VIBE.read_text()
+
+    # Extract the settings.local.json heredoc between cat > ... << 'EOF' and closing EOF
+    start_marker = 'cat > "$WORKSPACE/.claude/settings.local.json" << \'EOF\''
+    start_idx = vibe_src.find(start_marker)
+    if start_idx == -1:
+        check("AC4: heredoc found", False, "could not locate settings.local.json heredoc start")
+        return
+
+    start_idx = vibe_src.find('\n', start_idx) + 1
+    end_idx = vibe_src.find('\nEOF', start_idx)
+    if end_idx == -1:
+        check("AC4: heredoc end found", False, "could not locate heredoc end")
+        return
+
+    json_str = vibe_src[start_idx:end_idx]
+    try:
+        config = json.loads(json_str)
+    except json.JSONDecodeError as e:
+        check("AC4: valid JSON", False, f"JSON parse error: {e}")
+        return
+
+    check("AC4: valid JSON", True, "")
+
+    # Verify the pinned heartbeat command
+    heartbeat_cmd = "[ -f /workspace/.vss/auto-resume ] && { date +%s > /workspace/.vss/heartbeat.tmp.$$ && mv /workspace/.vss/heartbeat.tmp.$$ /workspace/.vss/heartbeat; } || true"
+
+    # Check PreToolUse hooks
+    pre_hooks = config.get("hooks", {}).get("PreToolUse", [])
+    pre_heartbeat = any(
+        block.get("matcher") == "" and
+        any(cmd.get("command") == heartbeat_cmd for cmd in block.get("hooks", []))
+        for block in pre_hooks
+    )
+    check("AC4: PreToolUse heartbeat hook present", pre_heartbeat,
+          f"PreToolUse blocks: {len(pre_hooks)}")
+
+    # Check PostToolUse hooks
+    post_hooks = config.get("hooks", {}).get("PostToolUse", [])
+    post_heartbeat = any(
+        block.get("matcher") == "" and
+        any(cmd.get("command") == heartbeat_cmd for cmd in block.get("hooks", []))
+        for block in post_hooks
+    )
+    check("AC4: PostToolUse heartbeat hook present", post_heartbeat,
+          f"PostToolUse blocks: {len(post_hooks)}")
+
+    # Check Stop hooks
+    stop_hooks = config.get("hooks", {}).get("Stop", [])
+    stop_heartbeat = any(
+        block.get("matcher") == "" and
+        any(cmd.get("command") == heartbeat_cmd for cmd in block.get("hooks", []))
+        for block in stop_hooks
+    )
+    check("AC4: Stop heartbeat hook present", stop_heartbeat,
+          f"Stop blocks: {len(stop_hooks)}")
+
+    # Check that guard-bash, guard-fs, bell, etc. are still present
+    has_guard_bash = any(
+        block.get("matcher") == "Bash" and
+        any("/usr/local/bin/guard-bash.sh" in cmd.get("command", "") for cmd in block.get("hooks", []))
+        for block in pre_hooks
+    )
+    check("AC4: guard-bash entry preserved", has_guard_bash, "")
+
+    has_guard_fs = any(
+        block.get("matcher") == "Write|Edit|MultiEdit" and
+        any("/usr/local/bin/guard-fs.sh" in cmd.get("command", "") for cmd in block.get("hooks", []))
+        for block in pre_hooks
+    )
+    check("AC4: guard-fs entry preserved", has_guard_fs, "")
+
+    # Check bell on Stop
+    bell_stop = any(
+        block.get("matcher") == "" and
+        any("printf '\\a'" in cmd.get("command", "") for cmd in block.get("hooks", []))
+        for block in stop_hooks
+    )
+    check("AC4: bell Stop command present", bell_stop, "")
+
+    # Check bell on Notification
+    notif_hooks = config.get("hooks", {}).get("Notification", [])
+    bell_notif = any(
+        block.get("matcher") == "" and
+        any("printf '\\a'" in cmd.get("command", "") for cmd in block.get("hooks", []))
+        for block in notif_hooks
+    )
+    check("AC4: bell Notification entry present", bell_notif, "")
+
+    # Check forceLoginMethod and permissions.defaultMode
+    check("AC4: forceLoginMethod=claudeai",
+          config.get("forceLoginMethod") == "claudeai",
+          f"forceLoginMethod={config.get('forceLoginMethod')}")
+    check("AC4: permissions.defaultMode=bypassPermissions",
+          config.get("permissions", {}).get("defaultMode") == "bypassPermissions",
+          f"permissions.defaultMode={config.get('permissions', {}).get('defaultMode')}")
+
+
+def test_vibe_supervised_launch_text() -> None:
+    print("\n[task_016 AC5/AC7/AC8: supervised launch and trap text]")
+    vibe_src = VIBE.read_text()
+
+    # AC5a: launch_claude has exec devcontainer exec
+    launch_claude_def = re.search(r'^launch_claude\(\) \{(.+?)^}', vibe_src, re.MULTILINE | re.DOTALL)
+    if launch_claude_def:
+        body = launch_claude_def.group(1)
+        has_exec = "exec devcontainer exec" in body
+        check("AC5: launch_claude body has 'exec devcontainer exec'", has_exec, "")
+    else:
+        check("AC5: launch_claude function found", False, "function definition not found")
+
+    # AC5b: At least one bare devcontainer exec line exists (non-exec-prefixed)
+    lines = vibe_src.split('\n')
+    no_exec_lines = [l for l in lines
+                     if l.strip().startswith('devcontainer exec')
+                     and not l.strip().startswith('exec ')]
+    check("AC5: at least one bare 'devcontainer exec' line", len(no_exec_lines) > 0,
+          f"found {len(no_exec_lines)} lines")
+
+    # AC5c: launch_claude_supervised exists
+    check("AC5: launch_claude_supervised function exists",
+          "launch_claude_supervised()" in vibe_src, "")
+
+    # AC5d: VIBE_SESSION_REF is created with mktemp
+    check("AC5: VIBE_SESSION_REF created with mktemp",
+          'VIBE_SESSION_REF=$(mktemp' in vibe_src, "")
+
+    # AC5e: AUTO_RESUME_MARKER defined before first supervised launch
+    check("AC5: AUTO_RESUME_MARKER defined",
+          'AUTO_RESUME_MARKER="$WORKSPACE/.vss/auto-resume"' in vibe_src, "")
+
+    # AC7: INT trap is re-armed every iteration and reset after sleep
+    has_int_trap = "trap 'auto_resume_deactivate" in vibe_src and "INT" in vibe_src
+    check("AC7: INT trap armed for deactivate", has_int_trap, "")
+
+    # AC7: trap reset after sleep
+    has_trap_reset = "trap - INT" in vibe_src
+    check("AC7: trap - INT reset present", has_trap_reset, "")
+
+    # AC8: final exit statement with CLAUDE_EXIT
+    has_exit_statement = re.search(r'^\s*exit "\$CLAUDE_EXIT"', vibe_src, re.MULTILINE)
+    check("AC8: final 'exit \"$CLAUDE_EXIT\"' statement", has_exit_statement is not None, "")
+
+
+def test_vibe_stall_watchdog_functional() -> None:
+    print("\n[task_016 AC6: watchdog functional tests]")
+    # All four cases stub vibe_container_kill_claude to write a killfile marker
+    # instead of asserting on process death alone (a broken kill gate would
+    # otherwise look identical to a correct one: an already-timed-out fake
+    # claude dies "on its own" regardless of whether the gate ever armed).
+    # POLL=1/GRACE=1/SECS=1/KILL_PAUSE=0 per spec AC6's pinned functional-test
+    # env; negatives sample the live PID mid-lifetime (~t=3s, before its
+    # natural 5s death) so the assertion is discriminating, not a race against
+    # natural process exit.
+
+    # Test 1: POSITIVE kill - marker active=1, ref file older (unconditional-
+    # session case), heartbeat stale. Long-lived fake claude (sleep 300) so
+    # any death can only be the watchdog's doing, never natural exit.
+    snippet_t1 = (
+        'set +e; '
+        'marker="${TMPDIR:-/tmp}/marker-t1.$$"; '
+        'hb="${TMPDIR:-/tmp}/hb-t1.$$"; '
+        'ref="${TMPDIR:-/tmp}/ref-t1.$$"; '
+        'killfile="${TMPDIR:-/tmp}/killfile-t1.$$"; '
+        # Stub vibe_container_kill_claude to track invocation (never actually
+        # kills - proves the host-side PID fallback is what finishes the job)
+        'vibe_container_kill_claude() { echo "killed" > "$killfile"; }; '
+        # Backdate ref file first so the marker (written next, "now") is
+        # unambiguously newer - no same-second race.
+        'touch -t 202001010000 "$ref"; '
+        'printf "active=1\\nremaining=2\\nresume_at=1751600000\\n" > "$marker"; '
+        'echo "$(($(date +%s) - 2000))" > "$hb"; '
+        'sleep 300 & fake_pid=$!; '
+        'VIBE_STALL_POLL_SECS=1 VIBE_STALL_GRACE_SECS=1 VIBE_STALL_SECS=1 VIBE_STALL_KILL_PAUSE_SECS=0 vibe_stall_watchdog "$fake_pid" "$marker" "$hb" "$ref" 2>/dev/null & '
+        'wd_pid=$!; '
+        'sleep 2.3; '
+        'if [ -f "$killfile" ]; then echo "KILLED=[yes]"; else echo "KILLED=[no]"; fi; '
+        'sleep 0.7; '
+        'if kill -0 "$fake_pid" 2>/dev/null; then echo "DEAD=[no]"; else echo "DEAD=[yes]"; fi; '
+        'kill "$fake_pid" 2>/dev/null; '
+        'kill "$wd_pid" 2>/dev/null; '
+        'wait "$fake_pid" 2>/dev/null; '
+        'wait "$wd_pid" 2>/dev/null; '
+        'rm -f "$marker" "$hb" "$ref" "$killfile" "$marker.tmp.$$" "$hb.tmp.$$"; '
+        'set -e'
+    )
+    r = _source_vibe_call({}, snippet_t1)
+    check("AC6 T1: positive kill case exits 0", r.returncode == 0, r.stderr)
+    check("AC6 T1: vibe_container_kill_claude invoked", "KILLED=[yes]" in r.stdout, r.stdout)
+    check("AC6 T1: fake claude actually terminated (host-PID fallback)",
+          "DEAD=[yes]" in r.stdout, r.stdout)
+
+    # Test 2: NEGATIVE active=0 - gate condition (1) fails. Short-lived fake
+    # claude (sleep 5) sampled at t=3s (mid-lifetime, well before its natural
+    # death) so "still alive" is evidence the gate declined to arm, not a
+    # coincidence of timing. killfile must not exist: the stub was never called.
+    snippet_t2 = (
+        'set +e; '
+        'marker="${TMPDIR:-/tmp}/marker-t2.$$"; '
+        'hb="${TMPDIR:-/tmp}/hb-t2.$$"; '
+        'ref="${TMPDIR:-/tmp}/ref-t2.$$"; '
+        'killfile="${TMPDIR:-/tmp}/killfile-t2.$$"; '
+        'vibe_container_kill_claude() { echo "killed" > "$killfile"; }; '
+        'printf "active=0\\nremaining=2\\nresume_at=1751600000\\n" > "$marker"; '
+        'echo "$(($(date +%s) - 2000))" > "$hb"; '
+        'touch "$ref"; '
+        'sleep 5 & fake_pid=$!; '
+        'VIBE_STALL_POLL_SECS=1 VIBE_STALL_GRACE_SECS=1 VIBE_STALL_SECS=1 VIBE_STALL_KILL_PAUSE_SECS=0 vibe_stall_watchdog "$fake_pid" "$marker" "$hb" "$ref" 2>/dev/null & '
+        'wd_pid=$!; '
+        'sleep 3; '
+        'if kill -0 "$fake_pid" 2>/dev/null; then echo "ALIVE=[yes]"; else echo "ALIVE=[no]"; fi; '
+        'if [ -f "$killfile" ]; then echo "KILLFILE=[yes]"; else echo "KILLFILE=[no]"; fi; '
+        'kill "$fake_pid" 2>/dev/null; '
+        'kill "$wd_pid" 2>/dev/null; '
+        'wait "$fake_pid" 2>/dev/null; '
+        'wait "$wd_pid" 2>/dev/null; '
+        'rm -f "$marker" "$hb" "$ref" "$killfile" "$marker.tmp.$$" "$hb.tmp.$$"; '
+        'set -e'
+    )
+    r = _source_vibe_call({}, snippet_t2)
+    check("AC6 T2: negative active=0 exits 0", r.returncode == 0, r.stderr)
+    check("AC6 T2: fake claude still alive after two live polls",
+          "ALIVE=[yes]" in r.stdout, r.stdout)
+    check("AC6 T2: kill never invoked", "KILLFILE=[no]" in r.stdout, r.stdout)
+
+    # Test 3: NEGATIVE stale ref gate - gate condition (2) fails: marker
+    # predates ref (crash-left marker from before this launcher session).
+    # Same live-PID-at-t=3s + killfile-absent discrimination as T2.
+    snippet_t3 = (
+        'set +e; '
+        'marker="${TMPDIR:-/tmp}/marker-t3.$$"; '
+        'hb="${TMPDIR:-/tmp}/hb-t3.$$"; '
+        'ref="${TMPDIR:-/tmp}/ref-t3.$$"; '
+        'killfile="${TMPDIR:-/tmp}/killfile-t3.$$"; '
+        'vibe_container_kill_claude() { echo "killed" > "$killfile"; }; '
+        'printf "active=1\\nremaining=2\\nresume_at=1751600000\\n" > "$marker"; '
+        # Backdate the MARKER (crash-left case) then create ref fresh - ref
+        # unambiguously newer than marker, so [ marker -nt ref ] is false.
+        'touch -t 202001010000 "$marker"; '
+        'touch "$ref"; '
+        'echo "$(($(date +%s) - 2000))" > "$hb"; '
+        'sleep 5 & fake_pid=$!; '
+        'VIBE_STALL_POLL_SECS=1 VIBE_STALL_GRACE_SECS=1 VIBE_STALL_SECS=1 VIBE_STALL_KILL_PAUSE_SECS=0 vibe_stall_watchdog "$fake_pid" "$marker" "$hb" "$ref" 2>/dev/null & '
+        'wd_pid=$!; '
+        'sleep 3; '
+        'if kill -0 "$fake_pid" 2>/dev/null; then echo "ALIVE=[yes]"; else echo "ALIVE=[no]"; fi; '
+        'if [ -f "$killfile" ]; then echo "KILLFILE=[yes]"; else echo "KILLFILE=[no]"; fi; '
+        'kill "$fake_pid" 2>/dev/null; '
+        'kill "$wd_pid" 2>/dev/null; '
+        'wait "$fake_pid" 2>/dev/null; '
+        'wait "$wd_pid" 2>/dev/null; '
+        'rm -f "$marker" "$hb" "$ref" "$killfile" "$marker.tmp.$$" "$hb.tmp.$$"; '
+        'set -e'
+    )
+    r = _source_vibe_call({}, snippet_t3)
+    check("AC6 T3: negative stale ref gate exits 0", r.returncode == 0, r.stderr)
+    check("AC6 T3: crash-left marker - fake claude still alive after two live polls",
+          "ALIVE=[yes]" in r.stdout, r.stdout)
+    check("AC6 T3: kill never invoked", "KILLFILE=[no]" in r.stdout, r.stdout)
+
+    # Test 4: NEGATIVE empty ref - gate condition (2) fails via the mktemp-
+    # failure fail-safe (empty ref must never satisfy the sentinel or the
+    # exists-and-newer branch). Same discrimination as T2/T3.
+    snippet_t4 = (
+        'set +e; '
+        'marker="${TMPDIR:-/tmp}/marker-t4.$$"; '
+        'hb="${TMPDIR:-/tmp}/hb-t4.$$"; '
+        'killfile="${TMPDIR:-/tmp}/killfile-t4.$$"; '
+        'vibe_container_kill_claude() { echo "killed" > "$killfile"; }; '
+        'printf "active=1\\nremaining=2\\nresume_at=1751600000\\n" > "$marker"; '
+        'echo "$(($(date +%s) - 2000))" > "$hb"; '
+        'sleep 5 & fake_pid=$!; '
+        'VIBE_STALL_POLL_SECS=1 VIBE_STALL_GRACE_SECS=1 VIBE_STALL_SECS=1 VIBE_STALL_KILL_PAUSE_SECS=0 vibe_stall_watchdog "$fake_pid" "$marker" "$hb" "" 2>/dev/null & '
+        'wd_pid=$!; '
+        'sleep 3; '
+        'if kill -0 "$fake_pid" 2>/dev/null; then echo "ALIVE=[yes]"; else echo "ALIVE=[no]"; fi; '
+        'if [ -f "$killfile" ]; then echo "KILLFILE=[yes]"; else echo "KILLFILE=[no]"; fi; '
+        'kill "$fake_pid" 2>/dev/null; '
+        'kill "$wd_pid" 2>/dev/null; '
+        'wait "$fake_pid" 2>/dev/null; '
+        'wait "$wd_pid" 2>/dev/null; '
+        'rm -f "$marker" "$hb" "$killfile" "$marker.tmp.$$" "$hb.tmp.$$"; '
+        'set -e'
+    )
+    r = _source_vibe_call({}, snippet_t4)
+    check("AC6 T4: negative empty ref exits 0", r.returncode == 0, r.stderr)
+    check("AC6 T4: empty ref - fake claude still alive after two live polls",
+          "ALIVE=[yes]" in r.stdout, r.stdout)
+    check("AC6 T4: empty ref never arms kill", "KILLFILE=[no]" in r.stdout, r.stdout)
+
+
+def test_vibe_gitignore_heartbeat_pattern() -> None:
+    print("\n[task_016 AC12: .gitignore heartbeat pattern]")
+    gitignore_path = REPO / ".gitignore"
+    if not gitignore_path.exists():
+        check("AC12: .gitignore exists", False, "file not found")
+        return
+
+    content = gitignore_path.read_text()
+    has_heartbeat = ".vss/heartbeat*" in content
+    check("AC12: .gitignore contains '.vss/heartbeat*'", has_heartbeat,
+          "pattern not found")
+
+
+def test_vibe_task016_docs() -> None:
+    print("\n[task_016 AC11/AC13: documentation presence]")
+
+    # AC11: MANUAL-TESTS.md gains a --sessions stall watchdog section
+    manual_tests = (REPO / "MANUAL-TESTS.md").read_text()
+    has_stall_section = "--sessions stall watchdog" in manual_tests or "stall watchdog" in manual_tests
+    check("AC11: MANUAL-TESTS.md has stall watchdog section", has_stall_section, "")
+
+    # AC13: vibe auto-resume comment block (around line 479-486)
+    vibe_src = VIBE.read_text()
+    has_heartbeat_docs = "heartbeat" in vibe_src and "stall-watchdog" in vibe_src
+    check("AC13: vibe comment block mentions heartbeat and watchdog", has_heartbeat_docs, "")
+
+    # AC13: devcontainer/commands/vsss.md documents Auto-resume section
+    vsss_md = (REPO / "devcontainer" / "commands" / "vsss.md").read_text()
+    has_auto_resume_docs = "Auto-resume" in vsss_md or "auto-resume" in vsss_md
+    check("AC13: vsss.md documents Auto-resume", has_auto_resume_docs, "")
+    has_stall_secs = "VIBE_STALL_SECS" in vsss_md
+    check("AC13: vsss.md documents VIBE_STALL_SECS", has_stall_secs, "")
+
+
 def main() -> int:
     test_help()
     test_version()
@@ -6049,6 +6463,14 @@ def main() -> int:
     test_vibe_model_args_set()
     test_vibe_fable_billing_phase()
     test_vibe_auto_resume_helpers()
+    test_vibe_auto_resume_deactivate()
+    test_vibe_auto_resume_heartbeat_write()
+    test_vibe_auto_resume_stalled()
+    test_vibe_settings_heartbeat_hooks()
+    test_vibe_supervised_launch_text()
+    test_vibe_stall_watchdog_functional()
+    test_vibe_gitignore_heartbeat_pattern()
+    test_vibe_task016_docs()
     test_vibe_help_mentions_fable_and_model()
     test_ac1_no_container()
     test_ac2_matching_image()
diff --git a/vibe b/vibe
index 49728f6..aa81a9c 100755
--- a/vibe
+++ b/vibe
@@ -484,6 +484,42 @@ fable_billing_phase() {
 # relaunches `claude --continue "/vsss --resume"`. The skill sets active=0 at
 # any clean Final-state exit, so finished loops never relaunch. Manual
 # override: delete .vss/auto-resume, or Ctrl-C during the countdown.
+#
+# Stall watchdog (heartbeat-driven kill of a wedged usage-limit picker):
+# interactive claude does NOT exit when a Pro/Max usage window runs out — it
+# blocks forever at an interactive picker, so the relaunch loop above never
+# runs on its own. settings.local.json's PreToolUse/PostToolUse/Stop hooks
+# (see the heredoc below) write an epoch to .vss/heartbeat while the marker
+# exists; launch_claude_supervised backgrounds claude, captures its PID, and
+# runs vibe_stall_watchdog alongside it. The watchdog's kill gate is THREE
+# conditions, ALL required, so a crash-left marker in an unrelated later
+# interactive session is never mistaken for a live --sessions run: (1) marker
+# active=1 (auto_resume_field, not auto_resume_pending, so the FINAL window —
+# remaining=0 — is still protected from an unwanted kill); (2) the marker was
+# refreshed DURING THIS launcher session — proven portably via
+# `[ marker -nt ref ]` against a session-start reference file (VIBE_SESSION_REF)
+# — OR the call site is a relaunch, which arms unconditionally via the literal
+# sentinel "-" (a freshly relaunched window may be stalled at the picker
+# before the skill gets a chance to refresh anything); an EMPTY ref (e.g.
+# mktemp failure) NEVER arms — fail-safe toward not killing; (3) the
+# heartbeat is provably stale (auto_resume_stalled, default 1800s — 30 min of
+# no hook events, including subagent tool calls, reliably means the picker,
+# not a legitimately long-running command). On trigger: warn, wait a grace
+# period, re-check all three, then kill in-container first
+# (vibe_container_kill_claude), pause for that to land, and fall back to the
+# host client PID. With relaunches remaining this drops straight into the
+# existing countdown/relaunch loop below; on the final window (remaining=0)
+# the loop is simply not entered and the launcher exits cleanly instead of
+# hanging forever. The watchdog's own lifetime is bounded by claude-PID
+# liveness alone (kill -0 in its poll loop) — no EXIT-trap dependency, so the
+# pre-existing clipboard-watcher EXIT trap is untouched; an orphaned watchdog
+# (launcher died mid-wait) notices claude's death within one poll interval.
+# Ctrl-C during the countdown deactivates the marker (auto_resume_deactivate)
+# via a per-iteration INT trap — cancel means cancel, but crash-left markers
+# must still resume, so nothing else touches the marker on any other exit
+# path. Claude's exit status is captured past the old `set -e`-guarding
+# `|| true` swallow and propagated as vibe's own final exit code
+# ($CLAUDE_EXIT).
 
 # auto_resume_field <file> <key> — echoes the last value for key, digits only
 # (missing file, missing key, or non-numeric value → echoes nothing). Keeping
@@ -516,6 +552,117 @@ auto_resume_decrement() {
     "$file" > "$tmp" && mv "$tmp" "$file"
 }
 
+# auto_resume_deactivate <file> — rewrite the marker with active=0, preserving
+# every other line. Same awk-to-temp+mv pattern as auto_resume_decrement.
+# Idempotent (running it twice leaves active=0). Missing file → return 0
+# WITHOUT creating one (cancelling a run that never wrote a marker is a
+# no-op, not an error).
+auto_resume_deactivate() {
+  local file="$1" tmp
+  [ -f "$file" ] || return 0
+  tmp="${file}.tmp.$$"
+  awk -F= 'BEGIN{OFS="="} $1=="active" {$2="0"} {print}' \
+    "$file" > "$tmp" && mv "$tmp" "$file"
+}
+
+# auto_resume_heartbeat_write <file> — atomically writes the current epoch
+# (date +%s) as the file's only content; same temp+mv pattern. Guarded so it
+# can NEVER return non-zero or trip `set -e` — an unwritable .vss dir (or any
+# other failure) is a silent no-op, and the temp file is cleaned up rather
+# than left behind if the write or rename fails partway.
+auto_resume_heartbeat_write() {
+  local file="$1" tmp
+  tmp="${file}.tmp.$$"
+  if ! { date +%s > "$tmp" 2>/dev/null && mv "$tmp" "$file" 2>/dev/null; }; then
+    rm -f "$tmp" 2>/dev/null || true
+  fi
+  return 0
+}
+
+# auto_resume_stalled <hb_file> <now_epoch> <stall_secs> — 0 (stalled) iff
+# the file's first line is a digits-only epoch AND now-epoch > stall_secs
+# (strict). Fail-safe returns 1 (not stalled) for a missing/empty file,
+# non-numeric content, a future epoch, and the exact boundary (gap ==
+# stall_secs) — a malformed or absent heartbeat must never look stale enough
+# to justify a kill.
+auto_resume_stalled() {
+  local file="$1" now="$2" thresh="$3" epoch gap
+  [ -f "$file" ] || return 1
+  epoch=$(head -1 "$file" 2>/dev/null)
+  case "$epoch" in
+    ''|*[!0-9]*) return 1 ;;
+  esac
+  gap=$(( now - epoch ))
+  [ "$gap" -gt "$thresh" ]
+}
+
+# vibe_container_kill_claude — best-effort SIGTERM to the claude process
+# inside the project's container. A named function (not inlined) so
+# smoke-test can stub it after sourcing, mirroring the existing docker()
+# stub precedent. Every failure mode is swallowed (`|| true`) — the
+# container may already be gone, or claude may have already exited. MUST
+# stay a literal, bare, non-`exec`-prefixed `devcontainer exec` invocation:
+# this is the surviving bare `devcontainer exec` line that keeps
+# test_vibe_final_line_no_exec's assertion (b) passing now that
+# launch_claude's body below is `exec devcontainer exec ...` (assertion a).
+vibe_container_kill_claude() {
+  devcontainer exec --workspace-folder "$WORKSPACE" --override-config "$OVERRIDE_CONFIG" pkill -TERM -x claude || true
+}
+
+# _vibe_stall_armed <marker_file> <ref_file> — kill-gate conditions 1+2 (see
+# vibe_stall_watchdog): marker active=1 AND either the literal relaunch
+# sentinel "-" (unconditional arm) or a non-empty, existing session-start ref
+# file that the marker is newer than (portable POSIX `-nt`, no stat -c/-f).
+# An empty or missing ref never arms (mktemp-failure fail-safe). Split out
+# from auto_resume_stalled (condition 3) so the watchdog can re-check the
+# cheap conditions first without a fresh heartbeat read every retry.
+_vibe_stall_armed() {
+  local marker="$1" ref="$2"
+  [ "$(auto_resume_field "$marker" active)" = "1" ] || return 1
+  [ "$ref" = "-" ] && return 0
+  [ -n "$ref" ] && [ -f "$ref" ] && [ "$marker" -nt "$ref" ]
+}
+
+# vibe_stall_watchdog <claude_pid> <marker_file> <hb_file> <ref_file> —
+# backgrounded by launch_claude_supervised alongside claude. Polls every
+# ${VIBE_STALL_POLL_SECS:-60}s while claude is alive (kill -0). On the first
+# poll where all three kill-gate conditions hold (_vibe_stall_armed plus a
+# stale heartbeat via auto_resume_stalled, default threshold 1800s): prints a
+# loud stderr warning (bell + threshold + cancel instruction), sleeps
+# ${VIBE_STALL_GRACE_SECS:-120}, re-checks all three conditions, and only if
+# STILL met kills in-container first (vibe_container_kill_claude), pauses
+# ${VIBE_STALL_KILL_PAUSE_SECS:-10} for that to land, then falls back to the
+# host client PID (`kill "$claude_pid"`) — never a host-side pkill. Either
+# way the function returns after handling this one escalation. All sleeps
+# are individually guarded so a signal or interrupted syscall can't trip
+# `set -e`; lifetime is bounded solely by claude-PID liveness, so an orphaned
+# watchdog (launcher died mid-wait) exits on its own within one poll interval.
+vibe_stall_watchdog() {
+  local claude_pid="$1" marker="$2" hb="$3" ref="$4"
+  local poll="${VIBE_STALL_POLL_SECS:-60}"
+  local grace="${VIBE_STALL_GRACE_SECS:-120}"
+  local stall_secs="${VIBE_STALL_SECS:-1800}"
+  local kill_pause="${VIBE_STALL_KILL_PAUSE_SECS:-10}"
+
+  while kill -0 "$claude_pid" 2>/dev/null; do
+    sleep "$poll" 2>/dev/null || true
+    if _vibe_stall_armed "$marker" "$ref" \
+       && auto_resume_stalled "$hb" "$(date +%s)" "$stall_secs"; then
+      printf '\n  \a⚠  auto-resume: no activity for over %ss — assuming the usage-limit picker is stuck.\n' "$stall_secs" >&2
+      printf '     Waiting %ss then killing this claude window (delete .vss/auto-resume to cancel).\n' "$grace" >&2
+      sleep "$grace" 2>/dev/null || true
+      if _vibe_stall_armed "$marker" "$ref" \
+         && auto_resume_stalled "$hb" "$(date +%s)" "$stall_secs"; then
+        vibe_container_kill_claude
+        sleep "$kill_pause" 2>/dev/null || true
+        kill "$claude_pid" 2>/dev/null || true
+      fi
+      return 0
+    fi
+  done
+  return 0
+}
+
 # ── Learning library helpers ──────────────────────────────────────────────────
 # All helpers are defined here, BEFORE the VIBE_SOURCE_ONLY guard, so that
 # smoke-test.py can unit-test them via VIBE_SOURCE_ONLY=1.
@@ -1423,7 +1570,14 @@ fi
 #                                               force-push; Stop/Notification
 #                                               ring the terminal bell so the
 #                                               outer terminal tab badges when
-#                                               Claude goes idle or needs input
+#                                               Claude goes idle or needs
+#                                               input; PreToolUse/PostToolUse/
+#                                               Stop also refresh
+#                                               .vss/heartbeat while a /vsss
+#                                               --sessions marker is active,
+#                                               feeding the launcher's stall
+#                                               watchdog (see the auto-resume
+#                                               comment block above)
 mkdir -p "$WORKSPACE/.claude"
 cat > "$WORKSPACE/.claude/settings.local.json" << 'EOF'
 {
@@ -1444,13 +1598,28 @@ cat > "$WORKSPACE/.claude/settings.local.json" << 'EOF'
         "hooks": [
           { "type": "command", "command": "/usr/local/bin/guard-fs.sh" }
         ]
+      },
+      {
+        "matcher": "",
+        "hooks": [
+          { "type": "command", "command": "[ -f /workspace/.vss/auto-resume ] && { date +%s > /workspace/.vss/heartbeat.tmp.$$ && mv /workspace/.vss/heartbeat.tmp.$$ /workspace/.vss/heartbeat; } || true" }
+        ]
+      }
+    ],
+    "PostToolUse": [
+      {
+        "matcher": "",
+        "hooks": [
+          { "type": "command", "command": "[ -f /workspace/.vss/auto-resume ] && { date +%s > /workspace/.vss/heartbeat.tmp.$$ && mv /workspace/.vss/heartbeat.tmp.$$ /workspace/.vss/heartbeat; } || true" }
+        ]
       }
     ],
     "Stop": [
       {
         "matcher": "",
         "hooks": [
-          { "type": "command", "command": "printf '\\a' >&2" }
+          { "type": "command", "command": "printf '\\a' >&2" },
+          { "type": "command", "command": "[ -f /workspace/.vss/auto-resume ] && { date +%s > /workspace/.vss/heartbeat.tmp.$$ && mv /workspace/.vss/heartbeat.tmp.$$ /workspace/.vss/heartbeat; } || true" }
         ]
       }
     ],
@@ -1639,23 +1808,72 @@ CLAUDE_MODEL_ARGS=$(build_claude_model_args)
 #   $2 — optional initial prompt. Only ever a FIXED LITERAL from this script
 #        (currently "/vsss --resume") — never user input — so single-quoting
 #        it into the container command string is injection-safe.
+#   Body is `exec devcontainer exec ...`: launch_claude is only ever invoked
+#   backgrounded (from launch_claude_supervised below), so the subshell it
+#   runs in replaces itself with the devcontainer-exec client process, and
+#   `$!` at the call site is that client's real PID — required for the
+#   stall watchdog's `kill -0`/kill-fallback to target the right process.
 launch_claude() {
-  devcontainer exec \
+  exec devcontainer exec \
     --workspace-folder "$WORKSPACE" \
     --override-config "$OVERRIDE_CONFIG" \
     /bin/bash --noprofile --norc -c "exec env SHELL=/bin/bash LC_ALL=C.UTF-8 LANG=C.UTF-8 claude ${1:+$1 }${CLAUDE_MODEL_ARGS:+$CLAUDE_MODEL_ARGS }--permission-mode bypassPermissions${2:+ '$2'}"
 }
 
-# `|| true`: claude's exit status must not kill the launcher under set -e —
-# the auto-resume loop below is exactly for the died-mid-run case.
-launch_claude "$CLAUDE_RESUME_ARGS" "" || true
+# VIBE_SESSION_REF — a session-start reference file, used solely for its
+# mtime: the stall watchdog only arms its kill for a marker that was
+# (re)written AFTER this launcher session began (portable `[ marker -nt ref ]`
+# — no stat -c/-f). That's what stops a crash-left marker from an earlier
+# --sessions run arming a kill against an unrelated interactive session
+# started later in the same project. On mktemp failure the variable stays
+# EMPTY, and the empty-ref case never arms (fail-safe toward not killing).
+VIBE_SESSION_REF=$(mktemp "${TMPDIR:-/tmp}/vibe-session-ref.XXXXXX" 2>/dev/null) || VIBE_SESSION_REF=""
+
+# AUTO_RESUME_MARKER / AUTO_RESUME_HEARTBEAT — paths the whole auto-resume +
+# stall-watchdog machinery reads/writes, defined once before the first
+# supervised launch.
+AUTO_RESUME_MARKER="$WORKSPACE/.vss/auto-resume"
+AUTO_RESUME_HEARTBEAT="$WORKSPACE/.vss/heartbeat"
+
+# CLAUDE_EXIT — global, holds the exit status of the most recent claude run.
+# Set by launch_claude_supervised; the launcher's final statement propagates
+# it as vibe's own exit code (see the bottom of this script).
+CLAUDE_EXIT=0
+
+# launch_claude_supervised <resume-args> <initial-prompt> <ref-file>
+#   Seeds the heartbeat (if a marker exists), backgrounds launch_claude,
+#   captures its PID, runs vibe_stall_watchdog alongside it, waits for
+#   claude, stores its exit status in CLAUDE_EXIT, then reaps the watchdog.
+#   Every intermediate command that can fail is individually guarded — this
+#   function must be un-trippable under `set -e` and always return 0.
+#   $3 (ref-file): "" (e.g. VIBE_SESSION_REF after a mktemp failure) never
+#   arms the watchdog's kill; the literal sentinel "-" (relaunch call sites)
+#   always arms (a freshly relaunched window may already be stalled at the
+#   picker before the skill can refresh the marker); anything else is a real
+#   ref-file path gated by vibe_stall_watchdog's marker-mtime check.
+launch_claude_supervised() {
+  local resume_args="$1" prompt="$2" ref="$3" claude_pid wd_pid rc=0
+  if [ -f "$AUTO_RESUME_MARKER" ]; then
+    auto_resume_heartbeat_write "$AUTO_RESUME_HEARTBEAT" || true
+  fi
+  launch_claude "$resume_args" "$prompt" &
+  claude_pid=$!
+  vibe_stall_watchdog "$claude_pid" "$AUTO_RESUME_MARKER" "$AUTO_RESUME_HEARTBEAT" "$ref" &
+  wd_pid=$!
+  wait "$claude_pid" || rc=$?
+  CLAUDE_EXIT="$rc"
+  kill "$wd_pid" 2>/dev/null || true
+  wait "$wd_pid" 2>/dev/null || true
+  return 0
+}
+
+launch_claude_supervised "$CLAUDE_RESUME_ARGS" "" "$VIBE_SESSION_REF"
 
 # ── Auto-resume loop ──────────────────────────────────────────────────────────
 # Engages only when /vsss --sessions X left an active marker (see the
 # auto_resume_* helpers above). Waits out the 5h-window reset, then relaunches
 # the conversation with a literal "/vsss --resume" prompt. Ctrl-C during the
 # countdown cancels; deleting .vss/auto-resume deactivates permanently.
-AUTO_RESUME_MARKER="$WORKSPACE/.vss/auto-resume"
 while auto_resume_pending "$AUTO_RESUME_MARKER"; do
   _ar_remaining=$(auto_resume_field "$AUTO_RESUME_MARKER" remaining)
   _ar_resume_at=$(auto_resume_field "$AUTO_RESUME_MARKER" resume_at)
@@ -1673,7 +1891,20 @@ while auto_resume_pending "$AUTO_RESUME_MARKER"; do
   echo ""
   echo "  ◆ auto-resume: /vsss marker active, ${_ar_remaining} relaunch(es) left."
   echo "    Relaunching claude --continue in $(( _ar_wait / 60 ))m $(( _ar_wait % 60 ))s — Ctrl-C to cancel, or delete .vss/auto-resume."
+  # Re-armed at the top of EVERY iteration, immediately before the countdown
+  # sleep; reset right after. Registers ONLY the INT slot — must never touch
+  # the EXIT slot, so the pre-existing clipboard-watcher EXIT trap still
+  # fires on this exit-130 path. Cancel means cancel: deactivate the marker
+  # so a stale "active=1" doesn't linger, but crash paths (no trap fires)
+  # deliberately leave the marker alone so a crash-left marker still resumes.
+  trap 'auto_resume_deactivate "$AUTO_RESUME_MARKER"; echo ""; echo "  ◆ auto-resume: cancelled (Ctrl-C) — marker deactivated."; exit 130' INT
   sleep "$_ar_wait"
+  trap - INT
   auto_resume_decrement "$AUTO_RESUME_MARKER"
-  launch_claude "--continue" "/vsss --resume" || true
+  launch_claude_supervised "--continue" "/vsss --resume" "-"
 done
+
+# Propagate the last claude run's exit status as vibe's own — the old
+# `|| true` swallow is gone, superseded by launch_claude_supervised capturing
+# CLAUDE_EXIT past `set -e`.
+exit "$CLAUDE_EXIT"
