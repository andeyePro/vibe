# Spec — task_028: CLAUDE.md fragment merges + single Fable-grant definition

## Task summary
Every vibe session's `~/.claude/CLAUDE.md` managed block injects all `devcontainer/claude-md/*.md` fragments (8,060 words ≈ 11k tokens today). Three groups restate one subsystem several times. Merge them so each rule appears once, cut content-guard.md to a summary with a README pointer, and define the `--fable-subagents` grant semantics once (in vs.md § Model economy) with vss.md and vsss.md pointing at it. Target ≥1,900 words (≈2,500 tokens) removed per session with no behavioural rule lost. Baselines at commit 5125e3c+: learnings.md 469 w, learn-hook.md 610 w, feedback-auto-promote.md 652 w, auto-memory-scope.md 225 w, conversation-history.md 509 w, content-guard.md 1,146 w; fragments total 8,060 w; vs.md+vss.md+vsss.md 13,062 w.

## Acceptance criteria
- AC1 `devcontainer/claude-md/` holds exactly 13 `.md` files. `learn-hook.md`, `feedback-auto-promote.md`, `conversation-history.md` do not exist. `learnings.md`, `auto-memory-scope.md`, `content-guard.md` exist. No other fragment file is added, removed, or renamed.
- AC2 `cat devcontainer/claude-md/*.md | wc -w` ≤ 6,160.
- AC3 `wc -w devcontainer/claude-md/content-guard.md` is between 400 and 600 inclusive, and the file contains the literal strings `README.md` and `Content guard`.
- AC4 Sentinel strings survive. `learnings.md` contains ALL of: `/learnings`, `vibe learn --init`, `.no-learn`, `grep -r`, `vibe learn "`, `/learn`, `permissionDecision`, `ask`, `realpath -m`, `guard-fs.sh`, `guard-bash.sh`, `bypass the hook`, `VIBE_AUTO_PROMOTE`, `Y / n / never-ask`, `YES:`, `NO:`, `cross-repo`, `One prompt per`, `vibe learn --push`, `VIBE_LEARNING_PATH`. `auto-memory-scope.md` contains ALL of: `task_014`, `~/.vibe/projects/`, `/home/node/.claude/projects/`, `--continue`, `/learnings`, `*.jsonl`, `-workspace`, `jq -r`, `type=="user"`, `type=="assistant"`, `tool_result`, `thinking`, `tool_use`, `ls -t`, `vibe-claude-config`, `Do not duplicate`. `content-guard.md` contains ALL of: `BLOCK`, `WARN`, `commit-identity`, `users.noreply.github.com`, `VIBE_CONTENT_GUARD=off`, `VIBE_ALLOW_COMMIT=1`, `--no-verify`, `.vibe-content-guard-off`, `.vibe-content-allow`, `path-warn:`, `vibe audit`, `--history`, `--staged`, `Co-Authored-By`, `pre-push`. All checks are case-sensitive literal substring matches.
- AC5 `python3 code-check.py` exits 0 and `python3 smoke-test.py` exits 0 (run in the foreground with a 600000 ms timeout).
- AC6 A sandboxed installer run — mirroring smoke-test.py's `_isolate_extras_env` (HOME set to a fresh temp dir `T`, `GIT_CONFIG_GLOBAL` into it, `VIBE_AUTO_GITIGNORE=0`) AND `CLAUDE_CONFIG_DIR=T/.claude` (the installer's `DEST_ROOT` prefers `CLAUDE_CONFIG_DIR`, which is set ambiently in-container; omitting it would write into the live `~/.claude`) with `VIBE_EXTRAS_SRC_ROOT=/workspace/devcontainer` — exits 0, and the resulting `T/.claude/CLAUDE.md` contains the per-fragment markers `<!-- vibe-md: learnings.md -->`, `<!-- vibe-md: auto-memory-scope.md -->`, `<!-- vibe-md: content-guard.md -->` inside the `vibe-managed` block, and does NOT contain the strings `learn-hook.md`, `feedback-auto-promote.md`, or `conversation-history.md` anywhere.
- AC7 Fable grant defined once. `devcontainer/commands/vs.md` § Model economy contains a subsection whose heading includes `Fable grant` and whose body contains ALL of: `--fable-subagents`, `--fable`, `permits`, `never forces`, `mechanical roles`, `vibe --fable`, `chair`. In `devcontainer/commands/vss.md` and `devcontainer/commands/vsss.md`: every line containing `--fable-subagents` also contains `/vs § Model economy`; the strings `never forces`, `mechanical roles`, `sets only the chair`, `chair model only`, `authorises no subagent spend` each occur ZERO times; and `--fable-subagents` occurs at most 3 times per file. The vs.md § Flags entry for `--fable-subagents` may remain but must be ≤ 40 words and contain `§ Model economy` (the 'never for mechanical roles / scoped small generations' caveat moves into the § Model economy subsection, it is not dropped). Note vss.md has 4 occurrences today, so one must go, not merely gain pointer text.
- AC8 `cat devcontainer/commands/vs.md devcontainer/commands/vss.md devcontainer/commands/vsss.md | wc -w` ≤ 12,700 (≥ 360 words removed). Trimming the other Fable-grant restatements inside vs.md itself (Escalation ladder bullet, Model-plan bullets, the 'Honor --fable-subagents at spec-writing time' line) counts as relocation and is permitted; vsss.md's grant line must gain the pointer text, so the saving comes from vs.md and vss.md.
- AC9 `CLAUDE.md` line listing the shipped fragments no longer names `learn-hook`; `README.md` and `ONBOARDING.md` contain no reference to `learn-hook.md`, `feedback-auto-promote.md`, or `conversation-history.md` (grep the three filenames across `README.md ONBOARDING.md CONTRIBUTING.md CLAUDE.md MANUAL-TESTS.md devcontainer/ smoke-test.py CHANGELOG.md` → the only permitted hits are in CHANGELOG.md and in smoke-test.py test-function names or check labels that describe the deletion; no smoke-test.py constant may still point at a deleted file).
- AC10 Scope lock: `git diff --name-only c68707d` (HEAD at task start, the `vs: archive task_027` commit) lists only paths under `devcontainer/claude-md/`, `devcontainer/commands/vs.md`, `devcontainer/commands/vss.md`, `devcontainer/commands/vsss.md`, `smoke-test.py`, `CLAUDE.md`, `README.md`, `ONBOARDING.md`, `MANUAL-TESTS.md`, `TODO.md`, `CHANGELOG.md`, `.vs/`.
- AC11 (Evaluator-owned, not Tester-testable) Rule inventory: every imperative rule in the three deleted fragments and in the pre-merge content-guard.md is present in the merged text or is explicitly delegated to a named README section. The Evaluator diffs `git show c68707d:<old file>` against the merged fragment rule-by-rule. Named must-survive clauses: the never-auto-write-to-/learnings rule; the one-prompt-per-memory rule; the `never-ask` → `VIBE_AUTO_PROMOTE=0` opt-out; the hook-is-not-optional / no-split-writes rule; the transcripts-are-the-log / do-not-duplicate rule; the per-project memory split and its 'ask the user, memory may predate task_014' consequence; the BLOCK-always-fires-under-path-warn rule; vsss.md's clause that the Fable grant persists across auto-resume relaunches (its one long line may keep both `--fable-subagents` mentions provided the line also contains `/vs § Model economy`).
- AC12 Per-file floors (guards against gutting prose around sentinels): `wc -w devcontainer/claude-md/learnings.md` ≥ 650 and `wc -w devcontainer/claude-md/auto-memory-scope.md` ≥ 450.
- AC13 The ten untouched fragments (web-research, ssh-discipline, brain2, shared-repos, harness-routing, output-consolidation, project-hygiene, todo-changelog, vibe-cli, workspace-is-the-repo) contain zero occurrences of `learn-hook.md`, `feedback-auto-promote.md`, `conversation-history.md`, and are byte-identical to their `c68707d` versions.

## Out of scope
- Any other fragment (web-research, ssh-discipline, brain2, shared-repos, harness-routing, output-consolidation, project-hygiene, todo-changelog, vibe-cli, workspace-is-the-repo) — do not edit, rename, merge, or reorder.
- `install-claude-extras.sh` and the installer's fragment gates (ssh-discipline/brain2/shared-repos by filename) — no changes.
- Content changes to README's content-guard section beyond none (the pointer targets the existing `### Content guard` heading in README.md; do not rewrite README prose).
- Any behavioural change to vs/vss/vsss beyond relocating the Fable-grant text and replacing restatements with pointers.
- Rewriting rules while merging: compress and deduplicate, never soften, drop, or reinterpret a rule (e.g. the `/learnings` hook trust model, the never-auto-write rule, the one-prompt-per-memory rule, the transcripts-are-the-log rule, the BLOCK-always-fires rule).
- brain2 notes (the hub note lists fragments; chair updates it after the pass, outside this task).

## Test location
`smoke-test.py` (repo convention: single host-side suite; no `tests/` dir). Tester appends `test_task028_*` functions after the `test_workspace_is_the_repo_fragment` definition and registers them in `main()` immediately after `test_workspace_is_the_repo_fragment()`. Generator's own edits to `smoke-test.py` are limited to updating constants and existing tests that reference the deleted fragment files, and happen in cycle 1 before Tester lands; from the moment Tester commits, `smoke-test.py` is Tester-only. Tester never edits pre-existing tests except where a pre-existing test hard-codes a deleted filename (list any such edit in `summary.md`).

## Proposed budget
3 cycles. Cycle 1 is the merge; cycles 2–3 absorb sentinel or rule-inventory misses.

## Model plan
- Planner + Evaluator: session model (Fable 5.1 chair via `vibe --fable`).
- Spec Critic: sonnet.
- Generator: opus (user `--gen opus`; lossy prose compression under a no-rule-lost constraint is judgment-heavy), ceiling Fable.
- Tester: haiku, ceiling sonnet.
- Fable rung: pre-authorised (--fable-subagents, granted by the user in prose for this /vsss run: "use optimal agents ... 27% of Fable weekly allowance"), NOT started — Opus is near-parity on a bounded, well-specified edit.
