# task_035 — Tester report (cycle 1)

Wrote the four assigned tests (`smoke/checks_12_harness_lints.py`, new part — checks_06 was already 1201 lines): `test_harness_spawns_never_inherit_stdin` (AC3), `test_harness_survives_open_stdin` (AC4), `test_extras_invocations_isolated` successor (AC6, moved out of checks_06), `test_no_fixed_sha_baselines` (AC7), plus `test_claude_md_testing_sentinels` (AC8) and `test_gitignore_managed_block_and_site_gitignore` (AC9). Registered in `runner.py` and `smoke-test.py`. `code-check.py` exits 0; `smoke-test.py < /dev/null` exits 1 — one new check fails.

**Finding (genuine, not a test bug):** the AC6 successor's own AST data-flow scan finds **29 unrouted `env=` call sites** that predate/exceed the Generator's cycle-1 diff — 27 `vibe learn` argv sites in `checks_02_image_drift_and_learning.py` and 2 `SETUP_GIT_SH` sites in `checks_08_credential_and_contentscan.py` (a 3rd, in checks_06, I fixed in place while relocating the test). All build `env` as a bare `{**os.environ, "HOME": ...}` literal, never through `_isolate_extras_env`. AC5's `_isolate_extras_env` was only ever wired up for `INSTALL_EXTRAS` + the one named offender (`checks_11`'s gh-meta test) — both fully compliant and verified. `SETUP_GIT_SH`/`vibe-learn` are functionally already safe (every site overrides HOME to a fresh temp dir; neither script touches `GH_META_CACHE`), but AC6's text is unconditional ("anything else … fails"), so this reports honestly rather than narrowing the check to pass. Cycle 2 needs either: (a) route these 29 sites through the builder too, or (b) re-scope AC5/AC6 to the categories that actually carry the risk.
