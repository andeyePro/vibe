# Cycle 4 test summary (task_017 AC16-AC21)

Totals: smoke-test.py 1426/1426 checks passing (0 failures) - the full frozen C1-C3 suite (1334 checks) plus 28 new Cycle-4 test functions (92 checks) appended after the last C3 call, covering AC16 (`devcontainer/credential-helper.sh` driven directly as a stdin->stdout filter across the full pinned attack list: project-slug exact match, shared-slug match gated by the launcher-exported `VIBE_SHARED_SLUG_*` twin, unknown-repo silence, `.git` suffix, subdir suffix (`owner/repo/info/refs`), trailing-slash-only on both an unmatched repo and the project's own slug, case-sensitivity, `..` traversal, embedded control byte, shell-metacharacter injection with a no-execution sentinel file check, the post-security-review sanitisation-collision hardening (twin-mismatch silence is the gate-discriminating fixture; twin-match still serves correctly; twin-entirely-unset also silences), non-`get` ops, non-github.com host, lookalike subdomain host, http-not-https, empty-shared-token-never-served, and a consolidated never-widen attack battery asserting `$GITHUB_TOKEN` never appears on stdout across 8 simultaneous non-project attempts), AC17 (`credential.useHttpPath` registered at the same `--global` scope as `credential.helper` in `setup-git.sh`, plus a functional run of `setup-git.sh` against a fresh sandbox `HOME` confirming both settings actually land in the resulting gitconfig), AC18 (structural: `GITHUB_REPO_SLUG` / shared-token / shared-slug-twin exports all sit at column-0 top-level scope, strictly outside and before the `_build_override_config` command-substitution call; functional: `render_devcontainer_with_mounts` injects matching `VIBE_SHARED_TOKEN_*`/`VIBE_SHARED_SLUG_*` remoteEnv passthroughs, confirmed remoteEnv-only and never in containerEnv), and AC19 (CLAUDE.md + README text checks for the amended single-repo-PAT / launch-header-blast-radius / never-multi-repo-token wording). code-check.py shellcheck clean across 15 files.

Regressions: none - every frozen C1/C2/C3 test still passes unmodified; only new functions appended and their `main()` call lines added after the last C3 call (`test_task017_c3_vibe_project_name_export_plumbing`). No sanctioned frozen-test amendment was needed this cycle (see Key note).

Key note: mid-cycle, a security-review pass (verdict CLEAR) closed one LOW by hardening the credential helper to re-verify a routed shared-slug request against a launcher-exported `VIBE_SHARED_SLUG_*` twin (the exact declared slug) before serving its token, because sanitisation is lossy (`foo-bar/baz` and `foo/bar-baz` both sanitise to `..._FOO_BAR_BAZ`). This cycle's AC16 tests were written/extended against that final hardened behaviour, with the twin-mismatch case as the explicit gate-discriminating fixture (pre-hardening it would have leaked the wrong repo's shared token). Separately: AC18's sanctioned C4 relaxation - a missing token for an acked+registered ro repo should WARN and still MOUNT ro rather than hard-block - is **UNIMPLEMENTED**. `shared_repos_scan` still emits `B <slug> no token configured (run: vibe repos add <slug>)` and skips both binds, exactly as it did in C1. Because the relaxed behaviour was never built, the C1 frozen test `test_task017_delta_scan_b_token_absent` is untouched and still passes unmodified - there is nothing for the chair to amend under the spec's sanctioned-frozen-test-amendment clause yet; this is a plain unimplemented-AC gap to pick up, not a test conflict. AC21's MANUAL-TESTS.md end-to-end push section (push to a shared repo with its own PAT / push to the project repo unchanged / push to an undeclared repo fails) has not been authored yet - per spec MANUAL-TESTS.md sections are Generator-authored docs, not Tester-owned, so this was left for the chair rather than fabricated here.
