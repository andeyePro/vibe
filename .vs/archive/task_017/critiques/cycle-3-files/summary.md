# Cycle 3 test summary (task_017 AC12-AC15)

Totals: smoke-test.py 1334/1334 checks passing (0 failures) - the full frozen C1/C2 suite (1273 checks) plus 61 new Cycle-3 test functions appended after the last C2 call, covering AC12 (/repo command file shape + host/container boundary + claim path/content shape + documented-flow runnability incl. last-claim-wins overwrite), AC13 (statusLine rw segment: structural regression guard on forceLoginMethod/defaultMode/guard-bash.sh/guard-fs.sh, byte-identical spot-check of two frozen fixtures with no signals tree, and functional holder/non-holder/no-request fixtures via VIBE_REPOS_DIR + VIBE_PROJECT_NAME), AC14 (shared-repos.md fragment shape + install-claude-extras.sh manifest-gating, missing/empty/non-empty), and VIBE_PROJECT_NAME plumbing (top-level export site before the _build_override_config subshell, remoteEnv-only in devcontainer.json). code-check.py shellcheck clean across 15 files.

Regressions: none - every frozen C1/C2 test still passes unmodified; only new functions appended and their main() call lines added after the last C2 call (test_task017_c2_repos_add_rw_flag_cli_level).

Key note: AC12's /repo claim has no sourced vibe helper (confirmed by grep - pure documentation, Claude runs Bash per repo.md's prose), so its test substitutes concrete values into repo.md's own path/content templates rather than extracting a fenced literal command; validated the AC13(c) not-the-holder branch is gate-discriminating by hand-running a deliberately broken statusLine variant that skips the holder-equality check and confirming the assertion fails against it. No AC12-AC15 mechanism was found unimplemented this cycle.
