diff --git a/.vs/tasks.json b/.vs/tasks.json
index e15294e..116ad34 100644
--- a/.vs/tasks.json
+++ b/.vs/tasks.json
@@ -390,5 +390,14 @@
     "assigned_to": "evaluator",
     "mode": "rigorous",
     "last_modified": "2026-09-12T00:33:01Z"
+  },
+  {
+    "id": "task_051",
+    "description": "$vs/$vss/$vsss aliases in Claude Code via a synced CLAUDE.md fragment plus one alias line per command file; hook route Martin-gated",
+    "implementation_status": "complete",
+    "test_status": "pending",
+    "assigned_to": "generator",
+    "mode": "rigorous",
+    "last_modified": "2026-09-12T00:34:06Z"
   }
 ]
diff --git a/README.md b/README.md
index 78f6ec8..dc8d8c9 100644
--- a/README.md
+++ b/README.md
@@ -188,7 +188,7 @@ Claude delegation has its own configurable billing route, independent of the sub
 
 **Codex-led sessions: how the backstops hold.** Everything above is Claude Code leading and Codex answering a single question. When Codex itself leads a session, the same two backstops have to hold with a different runtime behind them, and they do — through a root-owned system policy baked into the image at `/etc/codex/`, not through anything in your Mac's `~/.codex`. `requirements.toml` there is a set of constraints, not defaults: it clamps whatever a user or project config asks for, pins the unhooked tool surfaces off (unified exec's `write_stdin`, the JS REPL, sub-agents, apps), forbids every MCP server with an empty allowlist, disables hosted web search — which the firewall cannot see, since it is fetched by OpenAI rather than by this container — and declares `/etc/codex/hooks` the only place hooks may come from, so a project cannot add or disable one. Those managed hooks run `codex-guard-adapter`, which feeds Codex's `PreToolUse` JSON into the **same, unmodified** `guard-bash.sh` and `guard-fs.sh` the Claude Code hooks use (Codex's hook names are already Claude-shaped: `Bash` with `tool_input.command`, `apply_patch` aliased `Write`/`Edit`), and turns every `ask` into a `deny` — an unattended session has nobody to answer a prompt, so the safe answer is no. Stated plainly: **Codex's own sandbox cannot run here.** It is bubblewrap, it needs unprivileged user namespaces, and the container does not have them, so a mode below `danger-full-access` would fail every tool call rather than contain it. What holds instead is the container boundary, the firewall, root ownership the `node` user cannot touch, an argv-prefix exec policy for force-pushes, and those hooks — exactly the set that already holds a Claude Code session under `--permission-mode bypassPermissions`. Because Codex fails *open* when a hook binary cannot be spawned, `codex-guard-liveness` exists to refuse that silence: it checks every file in the chain for root ownership and permissions, then pushes real known-bad calls through the real adapter and requires real denials before a Codex-led session is considered safe to start. `docs/codex-tool-inventory.md` lists every Codex tool at the pinned version against the layer that mediates it, including the two rows that are honestly marked unmediated.
 
-**Skills in a Codex-led container.** `$vs`, `$vss` and `$vsss` are Codex skills baked into the image at `/etc/codex/skills/{vs,vss,vsss}/SKILL.md` — the Admin scope root, so a project cannot add, shadow or disable one, and nothing is ever written into your Mac's `~/.codex` or `~/.agents` to make this work. Each SKILL.md tells Codex to read the single-sourced command body at `/usr/local/share/vibe/commands/<name>.md` and follow it verbatim, substituting a fixed table of Codex equivalents for the handful of Claude-only primitives those bodies use (`Agent`, `Skill`, `ScheduleWakeup`, and so on). The one primitive with no Codex equivalent at all is subagent dispatch — Codex's own `multi_agent` is pinned off in this image (see above) — so `/vs`'s Planner/Spec Critic/Generator/Tester/Evaluator roles become `vibe-delegate role <role> --model <astra|opus|sonnet|haiku|fable> --cwd <workspace>`: one fresh `codex exec` or `claude -p` process per role, run under the same hooks and firewall as the lead, never resuming or sharing a session with it. This ships the plumbing and its offline tests only; the first live Codex-led `/vs` trial is a later, separate item (see **MANUAL-TESTS Test 55**). `/vs --panel N` has no subagents to fan out to either, so the image also ships `/usr/local/bin/codex-panel`: `codex-panel run --n N < diff` starts N independent, tool-less `codex exec` reviewers — one process each, a private temp cwd each, the same diff and a per-reviewer nonce on stdin — and prints one JSON object of verdicts for the chair to adjudicate. Its `--verify` mode drops `--ephemeral` so each reviewer's rollout is kept and can be proved to hold only its own nonce, which costs N live calls and leaves the diff and the nonces in `~/.codex/sessions` until you delete them, so it is an opt-in proof rather than the default.
+**Skills in a Codex-led container.** `$vs`, `$vss` and `$vsss` are Codex skills baked into the image at `/etc/codex/skills/{vs,vss,vsss}/SKILL.md` — the Admin scope root, so a project cannot add, shadow or disable one, and nothing is ever written into your Mac's `~/.codex` or `~/.agents` to make this work. Each SKILL.md tells Codex to read the single-sourced command body at `/usr/local/share/vibe/commands/<name>.md` and follow it verbatim, substituting a fixed table of Codex equivalents for the handful of Claude-only primitives those bodies use (`Agent`, `Skill`, `ScheduleWakeup`, and so on). The one primitive with no Codex equivalent at all is subagent dispatch — Codex's own `multi_agent` is pinned off in this image (see above) — so `/vs`'s Planner/Spec Critic/Generator/Tester/Evaluator roles become `vibe-delegate role <role> --model <astra|opus|sonnet|haiku|fable> --cwd <workspace>`: one fresh `codex exec` or `claude -p` process per role, run under the same hooks and firewall as the lead, never resuming or sharing a session with it. This ships the plumbing and its offline tests only; the first live Codex-led `/vs` trial is a later, separate item (see **MANUAL-TESTS Test 55**). `/vs --panel N` has no subagents to fan out to either, so the image also ships `/usr/local/bin/codex-panel`: `codex-panel run --n N < diff` starts N independent, tool-less `codex exec` reviewers — one process each, a private temp cwd each, the same diff and a per-reviewer nonce on stdin — and prints one JSON object of verdicts for the chair to adjudicate. Its `--verify` mode drops `--ephemeral` so each reviewer's rollout is kept and can be proved to hold only its own nonce, which costs N live calls and leaves the diff and the nonces in `~/.codex/sessions` until you delete them, so it is an opt-in proof rather than the default. The same `$vs`/`$vss`/`$vsss` spelling also works when Claude Code is the lead: a synced CLAUDE.md fragment (`devcontainer/claude-md/dollar-prefix.md`) tells it that a prompt whose first token is one of those means invoke the matching command with the rest of the prompt as its arguments, verbatim.
 
 **The unattended supervisor.** A `$vsss` run is meant to outlive its own quota window, and Codex's CLI has no durable loop of its own, so the image ships one: `/usr/local/bin/codex-supervisor` (root-owned, like the guard chain). It speaks Codex's app-server protocol on stdio — newline-delimited JSON, not JSON-RPC 2.0 — starting ONE thread with `approvalPolicy: "never"` and `sandbox: "danger-full-access"` (the only mode that works here; the container has no user namespaces for Codex's own bubblewrap sandbox, and the real boundary is the container, firewall and hooks described above), then re-entering that same thread with the literal word `continue` until the run's exit report ends with `VSSS-EXIT: <reason>`. It runs the vendor binary as `codex app-server` with no `-c` flags and a four-variable environment, so the system requirements and managed hooks apply unaltered. The reason it uses the protocol rather than `codex exec` is that only the protocol carries `codexErrorInfo` and the rate-limit snapshot: a quota failure names itself and reports when the window resets, so the supervisor sleeps until then plus two minutes rather than guessing, backs off on transient failures, declines every approval request it is sent (nobody is there to answer), and writes its thread id and counters to `.vss/codex-supervisor.json` after every change — so a killed supervisor resumes the same thread instead of starting a new one. Ceilings on turns, resumes, quota waits, retries and wall time bound the whole thing, and `codex-supervisor status --state <file>` prints where a run got to. It is wired into a Codex-led launch by a later item; today it is shipped and tested offline against a stub app-server.
 
diff --git a/TODO.md b/TODO.md
index 383ea5e..af3900d 100644
--- a/TODO.md
+++ b/TODO.md
@@ -88,7 +88,9 @@ Two vibe-wide behaviour changes shipped and took effect on this rebuild — surf
 
 - [ ] **Phase 1a follow-up (ii) from the security review (2026-09-10)** — the Fable/credits gate is `--consent-credits` in argv, i.e. the lead passing a flag after following `ask.md` prose; an autonomous `/vss` run can satisfy it itself. Consider a launcher- or hook-level gate for autonomous runs, and stop describing it as human consent until then (hook/settings edit: Martin-gated). Items (i) `codex=off` honoured by `/ask astra`, (iii) `claude -p` belt-and-braces flags, and (iv) the `~/.codex` mode warning closed by task_044 (2026-09-11, CHANGELOG).
 
-- [ ] **Codex-in-vibe phase 1b: supervised `/vs`, `/vss`, `/vsss` with both command prefixes** — PLAN WRITTEN AND ASTRA-REVIEWED 2026-09-11: `docs/codex-integration-plan.md` (facts F1-F10 at tag `rust-v0.154.0`, decisions D1-D9, a 10-item queue; Astra's five BLOCKING findings folded in, reply verbatim in `.vss/sessions/2026-09-11T17-11-32Z.md`). The `/vsss` run on `astra` works the queue top-down, one commit and one Astra diff review per item: 2 helper hardening (`codex=off` also gates `/ask astra`; `claude -p` argv), 3 host-side `~/.vibe/codex-allow` registry, 4 `/etc/codex/requirements.toml` policy layer + guard adapter + liveness runner + tool inventory, 5 Codex skills `$vs/$vss/$vsss`, 6 supervisor as an app-server JSON-RPC client (exec `--json` drops `codex_error_info` and never emits rate limits, so a `codex exec` wrapper cannot classify quota), 7 `vibe --agent codex` (SHIPPED task_049 — code, gate and docs; **live trial is Martin's**: run the Codex-led block of MANUAL-TESTS Test 55 on real Docker and report what you see, since nothing in the suite has ever started a real Codex session), 8 Codex panel runner, 9 usage ledger + `/budget`, 10 Claude-side `$vs` fragment. Settled by source: the Codex TUI rejects unknown slash commands before submission, so `/vs` typed in Codex's own terminal has one route only, a custom prompt in the user's `~/.codex/prompts` (`/prompts:vs`) — Martin's call, posted to fromClaude. Original entry: phase 1a implementation is recorded in CHANGELOG (2026-09-10); its real-container acceptance run remains open above. Claude remains lead until the Codex-led trial passes. Other commands/agents/fragments remain deferred. Billing changes must stay configuration-only and consent-gated. Brief: `/brain2/andeye/vibe-ChatGPT.md` §10.
+- [ ] **Codex-in-vibe phase 1b: supervised `/vs`, `/vss`, `/vsss` with both command prefixes** — PLAN WRITTEN AND ASTRA-REVIEWED 2026-09-11: `docs/codex-integration-plan.md` (facts F1-F10 at tag `rust-v0.154.0`, decisions D1-D9, a 10-item queue; Astra's five BLOCKING findings folded in, reply verbatim in `.vss/sessions/2026-09-11T17-11-32Z.md`). The `/vsss` run on `astra` works the queue top-down, one commit and one Astra diff review per item: 2 helper hardening (`codex=off` also gates `/ask astra`; `claude -p` argv), 3 host-side `~/.vibe/codex-allow` registry, 4 `/etc/codex/requirements.toml` policy layer + guard adapter + liveness runner + tool inventory, 5 Codex skills `$vs/$vss/$vsss`, 6 supervisor as an app-server JSON-RPC client (exec `--json` drops `codex_error_info` and never emits rate limits, so a `codex exec` wrapper cannot classify quota), 7 `vibe --agent codex` (SHIPPED task_049 — code, gate and docs; **live trial is Martin's**: run the Codex-led block of MANUAL-TESTS Test 55 on real Docker and report what you see, since nothing in the suite has ever started a real Codex session), 8 Codex panel runner, 9 usage ledger + `/budget`, 10 Claude-side `$vs` fragment (SHIPPED task_051). Settled by source: the Codex TUI rejects unknown slash commands before submission, so `/vs` typed in Codex's own terminal has one route only, a custom prompt in the user's `~/.codex/prompts` (`/prompts:vs`) — Martin's call, posted to fromClaude. Original entry: phase 1a implementation is recorded in CHANGELOG (2026-09-10); its real-container acceptance run remains open above. Claude remains lead until the Codex-led trial passes. Other commands/agents/fragments remain deferred. Billing changes must stay configuration-only and consent-gated. Brief: `/brain2/andeye/vibe-ChatGPT.md` §10.
+
+- [ ] **Martin-gated: make the `$vs`/`$vss`/`$vsss` alias in Claude Code deterministic with a `UserPromptSubmit` hook** (task_051 follow-up) — the fragment shipped in task_051 (`devcontainer/claude-md/dollar-prefix.md`) only works if the lead notices and follows it; a hook would make it work every time, but that means editing `settings.local.json` (what commands Claude Code is allowed to run automatically), which is one of the invariants Claude cannot change without your say-so. The edit would add exactly this to the `hooks` object: `"UserPromptSubmit": [{"matcher": "", "hooks": [{"type": "command", "command": "/usr/local/bin/dollar-prefix-hook.sh"}]}]`, plus a new root-owned script that reads the prompt text, checks whether its first word is exactly `$vs`, `$vss` or `$vsss`, and if so tells the model (via the hook's `additionalContext` output) to run that slash command with the rest of the prompt as its arguments. Say the word if you want this built; until then the fragment (instruction, not enforcement) is what ships.
 
 - [ ] **`/vs` guidance: "a control that cannot be observed failing is not a control"** (2026-09-08) — four fail-open test designs were caught in one session's adversarial review: a nonce sweep pointed at a directory an isolated panel never writes to (vacuous pass); an `rg -l 'A|B|C'` alternation that self-matches so a perfectly blind panel fails and the natural "fix" makes it pass forever; an A/B for Fable-reverts-to-Opus specified on an Opus session, where the reroute could not fire and the null read as evidence of absence; and a same-content two-directory A/B defeated by content-hash-keyed sticky routing. Each would have REPORTED SUCCESS while measuring nothing, and each was caught by the reviewer rather than the author. A fifth failure had the same disease in a different form: a PREMISE nobody checked (sticky routing was taken as applying to `claude -p`, and two rounds of design hardening were built on it, before its own author found it is documented only for requests carrying the API `fallbacks` parameter, which `claude -p` does not send). Add BOTH halves to `/vs`'s Tester/Spec-Critic guidance: **(a) state what result the test would produce if the mechanism under test were entirely absent** — that result must differ from a pass, and include a positive control where the mechanism is expected to fire; **(b) state what the test assumes**, and mark any assumption not verified against an artefact, so a design rule is never stacked on an unchecked premise. Working examples in `/brain2/andeye/vibe-ChatGPT-chat.md`.
 
diff --git a/devcontainer/commands/vs.md b/devcontainer/commands/vs.md
index 40cde30..5d7ef69 100644
--- a/devcontainer/commands/vs.md
+++ b/devcontainer/commands/vs.md
@@ -3,6 +3,7 @@ description: Adversarial agentic harness — Planner + Evaluator (session model)
 ---
 
 # /vs — adversarial harness
+Alias: `$vs …` is the same command (see `claude-md/dollar-prefix.md`).
 
 You (top-level, whatever model this session launched on — Opus by default, Fable 5 via `vibe --fable`) play two roles: **Planner** (Step 1–2, plus revision in Step 3) and **Evaluator** (Step 6–7). Between them, you dispatch independent subagents: a Sonnet **Spec Critic** that audits the spec before any code runs, a **Generator** that writes the feature (tier per the Model plan, Sonnet default), and — depending on mode — either a Haiku 4.5 **Tester** (default rigorous mode) or a Sonnet **Reviewer** (`--fuzzy` mode). Generator never sees Tester's / Reviewer's output; Tester / Reviewer never sees Generator's report. That separation is the point.
 
diff --git a/devcontainer/commands/vss.md b/devcontainer/commands/vss.md
index b5013b9..116341a 100644
--- a/devcontainer/commands/vss.md
+++ b/devcontainer/commands/vss.md
@@ -3,6 +3,7 @@ description: Versus Solo — autonomous one-shot. No args = pick from TODO.md (o
 ---
 
 # /vss — versus solo
+Alias: `$vss …` is the same command (see `claude-md/dollar-prefix.md`).
 
 You are the top-level session model (Opus by default; Fable 5 if launched via `vibe --fable`). `/vss` runs autonomously: no mid-flow approvals from the user except when an item on the **hard-escalate list** is hit. The point is one bounded unit of work end-to-end without back-and-forth.
 
@@ -170,7 +171,7 @@ Write the file ATOMICALLY at exit — not incrementally — so a partial-write d
 
 **Do NOT `git push` autonomously after `/vss` or `/vsss` completes.** Local commits land on the working branch (typically `main`). The user reviews the session audit at `.vss/sessions/<ISO>.md`, optionally inspects `git log` and per-commit diffs, and pushes manually with `git push` (or rejects and `git reset --hard <pre-session SHA>`).
 
-**Rationale**: autonomous push leaks unreviewed AI work to the remote. Local commits are reversible (`git reset HEAD~N`); pushed commits are not without force-push semantics, which the hard-escalate list forbids. The audit-trail-first / push-second order is the trust model.
+**Rationale**: autonomous push leaks unreviewed AI work to the remote; pushed commits aren't reversible the way local commits are. Audit-trail-first, push-second is the trust model.
 
 **Override**: `/vss --push-on-pass <task>` or `/vsss --push-on-pass <task>` opts in to autonomous push for that single invocation, only if the run completes without escalation triggers AND the optimiser (in `/vsss`) reaches a clean perfection-gate exit. A user passing `--push-on-pass` is explicitly accepting the un-reviewed-push trade-off for that run.
 
@@ -178,9 +179,9 @@ Default is no push. Always.
 
 ## Why `/vss` exists alongside `/vs` and `/sp`
 
-`/vs` is rigorous adversarial review for a single feature with verifiable criteria. `/sp` is Superpowers discipline applied broadly. Both still expect the human to direct the flow turn-by-turn.
+`/vs` is rigorous adversarial review for one feature with verifiable criteria; `/sp` is Superpowers discipline applied broadly. Both expect the human to direct the flow turn-by-turn.
 
-`/vss` is the autonomous wrapper: pick the right tool, run it, deal with the predictable in-flow choices the way Martin would. Use `/vss` when you want to step away. Use `/vs` or `/sp` directly when you want to drive.
+`/vss` is the autonomous wrapper: pick the right tool, run it, decide in-flow the way Martin would. Use `/vss` to step away; use `/vs` or `/sp` directly to drive.
 
 ## Running under Codex
 
diff --git a/devcontainer/commands/vsss.md b/devcontainer/commands/vsss.md
index c58c4ed..d44aa6e 100644
--- a/devcontainer/commands/vsss.md
+++ b/devcontainer/commands/vsss.md
@@ -3,6 +3,7 @@ description: Versus Super Solo — runs /vss on the given args, then optimises a
 ---
 
 # /vsss — versus super solo
+Alias: `$vsss …` is the same command (see `claude-md/dollar-prefix.md`).
 
 Autonomous loop wrapper around `/vss`. Higher blast radius — read this whole file before invoking.
 
diff --git a/docs/codex-integration-plan.md b/docs/codex-integration-plan.md
index 373929f..b3f83f5 100644
--- a/docs/codex-integration-plan.md
+++ b/docs/codex-integration-plan.md
@@ -313,7 +313,7 @@ before conveniences; the host registry before any credential-bearing trial.
 | 7 | **DELIVERED, pending live trial (task_049).** `vibe --agent codex`: launcher flag + untracked `.vibe/agent` file + `VIBE_AGENT`; refusal before Docker when the login mount is not granted; `launch_codex` / `launch_codex_plain` (no heartbeat, marker or stall watchdog — auto-resume stays Claude-only); root-owned `/usr/local/bin/codex-entry` runs the liveness gate and refuses to start Codex on any failure; `agent   :` header line; `codex-entry` added to the gate's ownership list. `devcontainer.json` NOT changed — the entry script is reached through `devcontainer exec`, not `postStartCommand`. Live trial is MANUAL-TESTS Test 55's Codex-led block (Martin) | `vibe`, `devcontainer/codex-entry.sh`, `devcontainer/codex-guard-liveness.sh`, `devcontainer/Dockerfile`, `devcontainer/install-claude-extras.sh`, `.gitignore`, README, MANUAL-TESTS (Test 55) | launcher fixtures: flag parsing, refusal without registry+marker, argv golden; entry script refuses on liveness failure | opus |
 | 8 | Codex panel runner `codex-panel.mjs` for `/vs --panel` on Codex: N tool-less processes, private cwds, nonce, verify/ship argv symmetric difference `{--ephemeral}` | `devcontainer/codex-panel.mjs`, `vs.md`, `checks_18` | argv pins; tool-disable set pinned; nonce assertion against stub rollouts; live proof recorded as Martin-gated | opus |
 | 9 | **DELIVERED (task_052).** Usage ledger: helper appends one JSONL line per call (runtime, model, billing, tokens, ISO time; no payload, no ids) to `.vibe/delegate-usage.jsonl` (untracked); `/budget` reports `claude -p` and Astra separately | `vibe-delegate.mjs`, `budget.md`, `.gitignore` block, `checks_17` | record shape asserted; ledger contains no payload bytes; failed calls log `error` rows | sonnet |
-| 10 | `$vs`, `$vss`, `$vsss` in Claude Code: CLAUDE.md fragment synced by `install-claude-extras.sh`; command files gain a one-line "alias form" note | `devcontainer/claude-md/dollar-prefix.md`, `vs.md`, `vss.md`, `vsss.md`, `checks_13` | fragment synced by the installer test; each command file names its `$` form | sonnet |
+| 10 | **DELIVERED (task_051).** `$vs`, `$vss`, `$vsss` in Claude Code: CLAUDE.md fragment synced by `install-claude-extras.sh`; command files gain a one-line "alias form" note | `devcontainer/claude-md/dollar-prefix.md`, `vs.md`, `vss.md`, `vsss.md`, `checks_13` | fragment synced by the installer test; each command file names its `$` form | sonnet |
 
 Items 4-8 depend on 1; 7 depends on 3, 4, 5; 6 is independent of 4-5 except
 for the prefix rewrite table. Items 9 and 10 are independent of everything.
