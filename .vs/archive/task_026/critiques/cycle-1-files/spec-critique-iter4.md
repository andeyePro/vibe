# Spec Critique — task_026 iteration 4 (`vibe pat [repo]` PAT rotation + launch-time 401 reprompt)

Re-audit against the current `/workspace/.vs/spec.md`, verifying disposition of both iteration-3 findings, then a fresh full sweep at the same adversarial standard. Cross-checked against `/workspace/vibe` (line refs below) and `/workspace/smoke-test.py`.

## Iteration-3 finding disposition

1. **BLOCKING (AC8(c)/(d) never asserted the wrapper's own return-0) — RESOLVED, genuinely.** AC8 now reads: "(c) ... AND the wrapper itself returns 0; (d) ... AND the wrapper itself returns 0 (all four branches a–d assert return 0 — the bare `set -e` call site makes any non-zero return a launch-killer)." This closes exactly the gap: the counter-example from iteration 3 (a `stored_token_rejected "$repo" "$token"; rc=$?; ...; return "$rc"` implementation, which passes stub-call/message assertions but returns 1 in branch (d) — the common case — and would kill every ordinary launch under `set -e`) now fails AC8(d) outright. Verified the "Test location" section's blanket `set +e`/`if fn; then` bracketing instruction already covers "the stubs' callers in AC8," so asserting a non-zero return in branch (d) is mechanically testable without the test script itself dying. Closed.

2. **minor (launch-site static check was placement-blind) — RESOLVED.** AC8's static assertion now reads "the call line appears in the launch path inside the stored-token branch (i.e. between that branch's opening and its `fi` — a placement-aware grep, not bare text presence, so an unconditional placement that would probe a freshly-pasted token fails the test)." Checked the actual launch path (`vibe:2774-2784`): today there is only `if [ -z "$GITHUB_TOKEN" ]; then setup_token "$GITHUB_REPO"; fi` with no `else` — bullet 14's prose ("the branch where `lookup_token` returned a value") combined with this AC gives the Generator a concrete, matchable target (add an `else` clause; put the call there), and a Tester grep scoped to "between `else` and the next `fi`" would correctly accept that placement and correctly reject both failure modes iteration 3 raised: the call sitting after the closed `if`/`fi` (unreachable to the else/fi grep) and the call sitting inside the wrong (`if [ -z ... ]`, post-`setup_token`) branch (no `else` present there to match against). Adequately closed for the concrete double-probe failure mode; the residual generic risk that a haiku-tier Tester implements the grep sloppily is execution risk, not a spec gap — the same class of risk iteration 3 already accepted as acceptable when it downgraded this to minor.

Both iteration-3 findings are genuine fixes, not reword-only. No regression in either fix when cross-checked against the actual `vibe` source structure.

## Fresh sweep — no new BLOCKING found

Checked: subcommand dispatch pattern (`vibe:2621-2646`, consistent with the `vibe learn`/`vibe repos`/`vibe audit` precedent the contract cites), the `_learning_capture` errexit-guarded-read idiom at `vibe:1438` (matches the AC3 citation), the `set +e` precedent at `smoke-test.py:7649-7653` (matches the Test location citation), `remoteEnv.GITHUB_TOKEN` in `devcontainer/devcontainer.json:49-50` (backs the task summary's no-rebuild claim), `lookup_token`/`save_token`'s prefix-match `awk` logic (backs AC2's trailing-`=`-padding and reserved-key-collision claims), and the `VIBE_SOURCE_ONLY` guard at `vibe:2616` (confirms the launch-site call is genuinely unreachable to dynamic sourcing, which is why AC8 correctly falls back to a static assertion rather than a dynamic one). All check out against the real files; nothing in the pinned contract or ACs makes a claim about the codebase that the codebase contradicts.

One residual worth naming for completeness, **minor, not new in substance**: AC8's static check requires the call inside an explicit branch ("between that branch's opening and its `fi`"), but the wrapper's own guard already makes an unconditional call placed immediately after `GITHUB_TOKEN=$(lookup_token ...)` (before the `if [ -z ... ]` at all, no branch) behaviorally identical and equally safe — the wrapper no-ops on empty token per its own contract, and this placement runs before `setup_token` could ever mutate `GITHUB_TOKEN`. AC8 would fail that behaviorally-correct implementation for lack of a branch wrapper. In practice bullet 14's prose ("the branch where `lookup_token` returned a value") steers the Generator toward the branch-based structure anyway, so this is unlikely to bite — flagging only so it's on record, not as a blocker.

## Verdict

pass (one minor residual noted above, explicitly accepted)
