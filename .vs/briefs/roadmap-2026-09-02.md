# vibe roadmap brief — 2026-09-02 (paused for `vibe --rebuild`)

Status: Martin asked for this at ~23:30 UTC 2026-09-02, then paused before the research
agents produced anything (all seven were stopped; no results exist). Resume by re-spawning
the seven agents below in ONE batch, then synthesising the roadmap (write it to
`/brain2/andeye/vibe-roadmap-2026-09.md`, summarise in the terminal, propose TODO.md
entries for approval — don't add them to TODO until Martin approves).

Model routing: chair = session model; agents on subscription tiers only (opus for
judgment/codebase reading, sonnet for web research). Never Fable subagents. Every agent
is read-only: no edits, commits, pushes, ssh, `vibe`; foreground commands only.

## Martin's ten questions (verbatim intent)

1. Port vibe to Linux + Windows and maintain all three? Is an RPi5 16GB a suitable Linux
   test box or is a full desktop distro needed (which)? Windows 11 on an old Intel NUC?
   Check brain2 for his earlier NUC conversations (he owns two Intel NUCs; idea: one Linux
   test box, one Windows). Goal: every vibe can build/test multi-platform software on the
   NUCs, an RPi, his `claude` userspace on this Mac, or a Mac mini/studio (for non-Mac
   builders targeting Mac).
2. `--sessions`: historically runs stopped early by misjudging time. Lately overnight
   /vsss runs work fine without it. Retire `--sessions`, or was the success just never
   crossing a credit-window reset?
3. Find other now-redundant features in the codebase and prune them.
4. Website's vibe-window image looks nothing like real vibe. Find a way to perform the
   demoed tasks in a real screen-recorded window so the site matches pixel-for-pixel,
   WITHOUT playing an actual video.
5. Website: use a normal-case scenario, not first-launch (no leaving vibe to set up
   tokens). Key message: PAT set once per expiry (90/365 days); rarely re-authorise Claude.
6. Competitive landscape: is vibe still the best harness for Claude Pro/Max users? If
   not, what capabilities are missing?
7. What else to perfect vibe? Expand the GreenPT line to any agent the user chooses incl.
   local LLMs? Are the other TODO items still relevant — explain each fully.
8. /vsss fromClaude/fromMartin async IO: he re-explains his format to every model. Wants:
   bare-minimum headmatter; link to counterpart from<User> file + link to Q&A-archive;
   then ONE ordered list of action points for him (questions to answer, tests to run);
   no information unless answering a question he asked; NO "Session report". Is that
   irresponsible? Make it user-editable; should his preference be the default?
9. Branding: fully brand as Vibe&I / vibeandeye / vibe.andeye.com like the other andeye
   apps, keeping `vibe` as the command?
10. Code review: integrate Anthropic's code-review options so the codebase never gets too
    large to review; plus third-party review — Codex, and Gemini via his Google Workspace
    Standard account (limited interfaces — research exactly which are included).

## The seven agent briefs (re-spawn verbatim)

A (opus) Platform port + test boxes — Q1. Audit Mac-specific assumptions in vibe,
  devcontainer/*, install.sh, MANUAL-TESTS.md (OrbStack/Docker Desktop, host.docker.internal,
  mDNS, fakeowner, ~/.ssh sanitising, pbcopy//c watcher, Ghostty/zshrc, BSD vs GNU utils,
  arm64/amd64). Linux state; Windows via WSL2 vs native. grep brain2 for NUC/raspberry/pi5/
  mac mini/test box. RPi5 vs x86 distro recommendation. Generalise the Mac test bridge
  (/brain2/andeye/pro-mac-test.sh, /brain2/meta/mac-test-account.md) into a multi-target
  build bridge (registry of targets, ssh discipline, firewall allowlist). Effort in days.
B (opus) /vsss — Q2 + Q8. What --sessions does today (post 2026-08-29 persist-until-
  complete; marker remaining=9999; --sessions X = cap); is the time-fit stop fully gone;
  evidence in .vss/sessions/*.md and brain2 of any run that actually crossed a window
  reset and auto-relaunched vs runs that finished in one window; recommendation (retire /
  cap-only / rename) + which MANUAL-TEST proves cross-window relaunch. Q8: compare his
  preferred fromClaude format with vsss.md § fromto spec and real files
  (/brain2/andeye/*Q&A-archive*.md, fromClaude*/fromMartin*); where "Session report"
  comes from; propose ONE user-editable location (brain2 note vs ~/.vibe/config vs
  project file) + draft default template ≤15 lines; session log = audit trail,
  fromClaude = action list.
C (opus) Redundancy audit — Q3. Prioritised table (≤40 rows): candidate | file:line |
  superseded by | references | confidence SAFE/LIKELY/UNSURE | lines. Cover launcher flags
  vs usage, alias flags, VIBE_* env knobs unread/undocumented, uncalled functions, old
  GITHUB_TOKEN local helper, RETIRED_COMMANDS, hooks, claude-md fragment overlap (wc -w each;
  merge proposals with token savings), vs/vss/vsss superseded sections + duplication,
  web/vibe-andeye.md, MANUAL-TESTS for removed behaviour, smoke tests pinning removed
  behaviour, stale .gitignore.
D (opus) Website + branding — Q4, Q5, Q9. Inspect site/ demo build + site-check.mjs
  copy guards. Evaluate asciinema rec + asciinema-player (DOM replay, not video), animated
  SVG (termtosvg/svg-term), xterm.js replay, static frames; Claude Code TUI redraw
  fidelity; redaction pass over .cast; re-record script + site-check assertions; host-side
  recording steps. Scenario set: hero = everyday launch, first-launch demoted. Branding:
  brain2 conventions, collision check (WebSearch "Vibe&I"/"vibeandeye"), rename blast
  radius (CLI/repo/domain stay?). Effort in hours.
E (sonnet, WebSearch) Competitive landscape — Q6. Anthropic first-party (sandbox,
  Agent Teams, Routines, web, Dynamic Workflows, code-review ultra, Managed Agents) vs
  vibe's jobs; 8–12 third-party harnesses relevant to Pro/Max subscribers (Docker
  sandboxes, Codex CLI, Conductor, Vibe Kanban, Sculptor, Superset, Claude Squad,
  ccmanager, Ralph, opencode, Goose, Aider…) ranked table; gap list with invariant
  conflicts; verdict + 3 things that would change it.
F (opus) TODO relevance + GreenPT/any-LLM — Q7. Table of EVERY [ ] and [!] TODO item:
  title | date | plain-English explanation | still relevant (superseded by …) | action.
  GreenPT: what it is, the three shapes, then "any agent incl. local LLMs" (Ollama/LM
  Studio/OpenAI-compatible, ANTHROPIC_BASE_URL proxies like LiteLLM/claude-code-router)
  against invariants (subscription auth, no credit spend, firewall, ToS); recommended
  shape/scope/effort; core vs opt-in plugin. 5–8 highest-leverage gaps.
G (sonnet, WebSearch) Code review — Q10. Anthropic surfaces (/code-review levels,
  ultra, /security-review, Claude Code Security, claude-code-action, web) with size
  limits + cost for Max + wiring into vibe; "never too large" policy with numbers (split
  15k-line launcher, 14k-line smoke-test); third-party on free/subscription tiers only:
  Codex review (plan needed, in-container non-interactive?), Gemini for Google Workspace
  STANDARD (Gemini app, Gemini in Docs/Gmail, NotebookLM, Code Assist individual vs
  Workspace, Gemini CLI auth rules for Workspace accounts, Code Assist GitHub app, Jules,
  AI Studio keys = credit spend?) as a table; Copilot review/CodeRabbit one-liners;
  integration design (/review fan-out merged by chair; firewall + auth volume + ToS),
  effort in days.
