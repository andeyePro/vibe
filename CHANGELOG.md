# CHANGELOG

vibe's done-work audit log. Each entry is a closed task — either successful (`[x]`) or abandoned with failure context (`[!]`). Append-only. The active backlog lives in `TODO.md`.

Convention adopted 2026-05-08 after the AEP-Plugin PR review surfaced confusion between TODO and CHANGELOG. Before this date, closed items lived in `TODO.md ## Done`; they migrated here verbatim. New closed items append to this file.

## 2026-07-11

- [x] **`--sessions` auto-resume countdown is now rate-limit-aware** — live UX failure: three projects sat in ~3.5h relaunch countdowns while the real 5h window was 4% used, because the marker's `resume_at` is a blind worst-case estimate (skill session start + 5h) and nothing ever consulted the actual rate-limit state. Fix in two halves. Capture: the `settings.local.json` statusLine command now, as a pure side effect (display output byte-identical — the six exact-output smoke fixtures pass unchanged), atomically (tmp+mv, `$$`-suffixed like the heartbeat hooks) writes `${VIBE_VSS_DIR:-/workspace/.vss}/rate-limit` with `epoch=<now>` / `used=<floored %>` whenever Claude Code supplies `.rate_limits.five_hour.used_percentage` and the `.vss` dir exists (`VIBE_VSS_DIR` is new, so fixtures can aim it at a temp dir). Consult: new launcher helpers `auto_resume_rate_headroom` + `auto_resume_effective_wait` (before the `VIBE_SOURCE_ONLY` guard, digits-only fail-safe parsing via `auto_resume_field`) reproduce the historical resume_at wait exactly (1800s default / +120s future pad / 120s past grace), then cap it at the 120s grace when the reading is fresh (`VIBE_RATE_READING_MAX_AGE`, default 1800s) with headroom (`used` ≤ `VIBE_RESUME_USED_MAX`, default 50) — a wrong guess just re-enters the countdown; missing/stale/garbage input anywhere falls back to the conservative estimate, never a crash. Countdown message says why when the cap fires (`window ~N% used per last reading — relaunching ...`). `.gitignore` gains `.vss/rate-limit*`; vsss.md § Auto-resume + the launcher comment blocks document the cap and both knobs; MANUAL-TESTS Test 32 gains sub-case 32g. Smoke: 12-case unit test of the wait/headroom helpers (cap, high-usage, stale, missing, garbage, knob overrides incl. garbage knobs, all three base cases) + statusLine side-effect fixtures (file present with correct content, floored fractional, absent without the field or the dir, display identical throughout); both gates green.
- [x] **`--fable-subagents` (alias `--fable`) on /vs, /vss, /vsss: standing per-run Fable pre-authorisation** — built the switch Martin's live request implied ("use Fable for any subagents that will get better or faster results" — and no, `/vsss --fable` didn't previously do anything: `--fable` only existed as the vibe LAUNCHER flag, chair model only). The new flag is per-invocation consent, not a default: `/vs` Model plans record `Fable rung: pre-authorised (--fable-subagents)`, the Planner may START the Generator at Fable for genuinely huge/ambiguous specs and the escalation ladder may take the Fable rung without a fresh mid-run ask; task-class routing is preserved verbatim — mechanical roles (Tester, Spec Critic) never route to Fable, the flag buys permission, not blanket routing. `/vss` threads it into whatever tool it picks (its hard-escalate credit bullet gains the carve-out); `/vsss` propagates it into every wrapped iteration and must record the grant in the session audit plus each enabled dispatch in per-iter Notes. All three docs cross-reference the launcher flag as a different thing. 18 smoke checks guard the consent semantics. A blind Fable-tier reviewer (Martin's standing grant applied to the review itself) returned 6 findings pre-commit — headline: the `--fable` alias was claimed but undocumented in vs.md (a future chair would have found only `--fable-gen`, which spends MORE than granted), and /vss's Mode-B planner brief still said 'never pick a credit-billed tier' unconditionally, silently dropping the grant. All fixed, incl. an explicit line that the grant persists across `--sessions` relaunches (same user invocation; the session file's recorded Initial-plan grant is the authority).
- [x] **HOTFIX: fresh `vibe` launches died with claude's `--print` error** — the task_016 supervised launch backgrounds claude, and POSIX hands an async (`&`) command `/dev/null` as stdin when job control is off, so claude saw a non-TTY stdin, assumed print mode, and exited instantly on every fresh launch after yesterday's push (first live launch caught it — the smoke suite structurally cannot see TTY behaviour; this is MANUAL-TESTS Test 32's class). Fix: the launcher dups its real stdin to FD 9 once and every supervised launch re-attaches it explicitly (`<&9 &` — an explicit redirection overrides the /dev/null default). Reproduced and proven under a real pty (`script -qec`): plain `&` → probe reports NOTTY, fixed shape → TTY.
- [x] **shared-repos cycle 4: per-repo credential routing + invariant amendment** — task_017 C4, the security-critical slice (Generator: opus; security-review agent mandatory — verdict CLEAR with the never-widen guarantee proven path-by-path, its one LOW closed anyway). `credential-helper.sh` is now a per-repo token router on `credential.useHttpPath` (set in setup-git.sh at the helper's own scope): the project's `$GITHUB_TOKEN` is served ONLY on case-sensitive exact match against the new `GITHUB_REPO_SLUG` remoteEnv; each declared shared repo gets its own `VIBE_SHARED_TOKEN_*` PAT (exported launcher-top-level — never inside the `_build_override_config` subshell — and passed via remoteEnv only, mirroring GITHUB_TOKEN); everything else gets NOTHING, including the no-path compat branch whenever any shared token is configured. Post-review hardening: sanitisation is lossy (`foo-bar/baz` and `foo/bar-baz` both route to `..._FOO_BAR_BAZ`), so the helper re-verifies the routed request against a `VIBE_SHARED_SLUG_*` twin (the exact declared slug) before serving — request-vs-config collisions now serve nothing. The sanctioned AC18 relaxation landed with it: a tokenless acked ro repo now WARNS and still mounts (ro reads of a local checkout need no token; `vibe repos list` carries the no-PAT nudge), with the C1-era frozen test amended in the same commit per the spec's recorded sanction. CLAUDE.md's one-repo-PAT invariant rewritten honestly: every PAT stays single-repo; a container's blast radius = exactly the repos announced in its launch header. Tester (sonnet): 92 new checks incl. a consolidated never-widen battery (8 simultaneous non-project attempts, injection/traversal/case/subdomain/port attacks) and the twin-mismatch gate-discriminator; suite 1426/1426, no regressions. MANUAL-TESTS gains Test 34 (end-to-end push routing).
- [x] **shared-repos cycle 3: `/repo` claim/handoff + seam-discipline fragment + statusLine surrender signal** — task_017 C3. `devcontainer/commands/repo.md` ships the in-session `/repo add|remove|claim` command: add/remove edit the committed `.vibe-repos` declaration (with the honest note that the container cannot touch `~/.vibe` on the host — registration/ack/PAT stay a Mac-side `vibe repos add`), and `/repo claim` writes a KEY=VALUE `rw-request` into the shared checkout's `.vibe-signals/` sidecar — writable even from a read-only code mount, which is the whole reason the sidecar exists. The rw HOLDER's status line (extended settings-heredoc command, byte-identical output when no signals exist — frozen fixtures prove it) gains a ` · ⚠ rw:<requesting-project>` segment, holder-gated by comparing the new `VIBE_PROJECT_NAME` remoteEnv against the lock meta, so only the session that can actually surrender the lock gets nagged; handoff = holder exits, claimant relaunches (Docker fixes mount modes at creation — no live transfer exists, so the tooling does discovery, not magic). `devcontainer/claude-md/shared-repos.md` is the new managed fragment, gated on the runtime manifest being non-empty (brain2.md precedent): it teaches the session the manifest, ro etiquette, claim etiquette, and the freemium-load-bearing seam rule — code under `/repos/<name>` is proprietary, NEVER copied into `/workspace`, consumed only via the project's declared interface/feature-flag seam. Tester (sonnet): 61 new checks incl. functional statusLine holder/non-holder/no-request fixtures (discrimination validated against a deliberately broken variant) and install-extras manifest-gating; suite 1334/1334, no regressions.
- [x] **shared-repos cycle 1: registry + declaration + read-only mounts + honest header** — task_017, first of 4 planned `/vs` cycles (rw lock lands C2, `/repo` claim/handoff + CLAUDE.md seam fragment C3, per-repo credential routing via `useHttpPath` C4). The problem: a public `<x>andeye` project sometimes needs live cross-repo development against a private repo (e.g. `andeyePro/andeyePro`) without either repo's blast radius widening. Mechanics: a project commits `.vibe-repos` (`owner/repo [ro|rw]` lines, charset-validated, comments/blanks allowed — deliberately NOT gitignored, since declaring WHAT a project uses is meant to travel with it); each machine separately registers WHERE that repo actually lives via `vibe repos add <owner/repo> [path]` into `~/.vibe/repos` (`slug=path`, chmod 600 like `~/.vibe/tokens`, which now also serves shared-repo PATs keyed by slug — reusing `lookup_token`/`setup_token` as-is). New `vibe repos` subcommand (`add` / `remove [--purge]` / `list`) dispatches in its own pre-`parse_vibe_args` block, mirroring `learn`'s exact placement, so `vibe repos add owner/repo` can never be swallowed by the positional-project-name catch-all. `shared_repos_scan` is the single classification pass (used by `list`, the launch header, the runtime manifest, AND `_build_override_config`'s mount-triple assembly, so none of the four can ever disagree): it emits `M`/`B`/`N` per declared slug — mounted, registered-but-broken-with-reason, or never-registered — and the header renders the never-registered case as TOTAL SILENCE (the community-contributor property: someone without the private repo sees nothing) versus a loud named warning for anything registered-but-broken (missing checkout, missing token, reserved dot-prefixed basename, basename collision). Every shared repo contributes TWO bind mounts from this cycle onward: the checkout itself (hard-pinned read-only in C1 — the declaration's `rw` field already parses so C2's lock logic has somewhere to read from, it just isn't wired to the actual bind flag yet) and its `.vibe-signals/` sidecar, always read-write even on a read-only code mount — that's what will let an ro session file an rw claim once C3 lands `/repo claim`. The sidecar directory is created host-side (`shared_repo_ensure_signals`, guarded/idempotent) at both `vibe repos add` time and defensively at mount assembly, specifically so Docker never auto-vivifies it as a root-owned bind target. A runtime manifest (`/workspace/.vibe/shared-repos.manifest`, one `name mode slug` line per mount) is written strictly before `devcontainer up` so `install-claude-extras.sh` can register `git config --global --add safe.directory /repos/<name>` for each one in the SAME launch — critical for the relaunch immediately after `vibe repos add`. `GIT_OPTIONAL_LOCKS=0` ships as a plain `devcontainer.json` containerEnv/remoteEnv constant (applied container-wide as a safe simplification — it's a read-path optimisation toggle, never a correctness concern). `ensure_project_gitignore`'s managed block gains `.vibe-signals/` (NOT `.vibe-repos`, which stays committed by design); `shared_repo_ensure_signals` is gitignore owner #2, for shared checkouts that aren't vibe projects themselves. Post-generation, an advisory security review found two MEDIUMs, both fixed pre-commit: slug lookups used grep BREs (a committed `myor./secret` could regex-match a registered `myorg/secret`, resolving its path and PAT — all six lookup sites now use fixed-string awk matching), and — the design-level one — the committed `.vibe-repos` is PR-controllable while the registry is machine-global, so a malicious PR line could have mounted any repo the maintainer ever registered; fixed with a per-workspace ack layer (`~/.vibe/repos-acks`, written ONLY by `vibe repos add` run in that project — the scan's new `U` tag renders loud in header/list, mounts nothing, writes nothing host-side). shellcheck clean (15 files); smoke suite 1189/1189 including 96 task_017 checks from two independent Testers (haiku, then a sonnet delta after the haiku pass skipped the scan/manifest/override-JSON surface as 'not unit-testable' — it is, and the delta also locks in the security regressions). README gains a Shared repos bullet + `~/.vibe/repos` host-state row + the `vibe repos add` usage line.
- [x] **shared repos C2: rw intent + single-writer lock + EXIT-trap consolidation** — task_017 cycle 2 (AC8-AC10; SHA pending commit). The `.vibe-repos` `rw` mode field is now honoured: the launcher's main shell attempts lock acquisition per rw-declared repo BEFORE mount assembly runs (`shared_repos_apply_rw_intents` — it must run outside the `$(...)` that invokes `_build_override_config`, where lock side effects and hook registrations would die with the subshell), and mount assembly re-reads the answer via `shared_repo_resolve_mode <slug> <checkout> <declared-mode>` (a single call-site line in C1's loop; acquire is re-entrant on project+pid, and `$$` in a command-substitution subshell is the parent's pid, so the two passes can't disagree or double-acquire). The lock is a DIRECTORY at `<checkout>/.vibe-signals/rw-lock.d/` — `mkdir` is the atomic acquire — with a `meta` file (`project=`/`pid=`/`since=`, pid/since digits-only on read); stale-reclaim fires exactly once when the holder pid is dead, and missing/malformed meta is held-by-unknown, never stale (the mkdir-then-write window must not be stealable). Contention falls back to ro with a loud header warning naming the holder project + since-time; release (`shared_repo_lock_release`) only ever removes a lock whose meta matches BOTH our project and pid. EXIT handling is consolidated into a single `vibe_on_exit` dispatcher with an appendable `VIBE_EXIT_HOOKS` array (`vibe_exit_hook_add` installs the one and only `trap ... EXIT`); every hook runs individually `|| true`-guarded — under `set -euo pipefail` one refusing hook (lock release in the routine never-held case) would otherwise abort the handler and silently skip later hooks, the exact clipboard-flush regression task_016 refused to risk. The Darwin clipboard-flush trap body migrated into the dispatcher byte-unchanged, and `vibe learn`'s commit-message tempfile cleanup moved its EXIT slot there too (keeping a direct INT/TERM trap). `vibe repos add --rw` wires into the new `shared_repo_declaration_set_mode` helper (validating slug charset, ro|rw, and the reserved dot-prefixed basenames). All helpers live before the `VIBE_SOURCE_ONLY` guard for unit-testability; MANUAL-TESTS.md gains Test 33 (contended fallback with named holder; stale-reclaim after `kill -9`; clipboard-flush regression guard).

## 2026-07-10

- [x] **`/vs --panel [N]`: blind review panel + sycophancy check** — ports the two agent-review-panel mechanisms the 2026-07 harness-landscape audit flagged as `/vs`'s clearest QA gap. N panellists (default 3, odd, Sonnet — deliberately not the upstream all-Opus: the mechanism's value is independence, not depth) dispatch as read-only `code-reviewer` agents in one concurrent batch with byte-identical briefs (no assigned personas — differentiation must emerge), each blind to the others' verdicts. The Evaluator then runs a mandatory correlated-agreement check: unanimous near-identical rationales = LOW-information consensus (logged as a weakness, optional single Opus depth-probe on high-blast-radius diffs); independent convergence = earned confidence; splits get written chair adjudication, and an unrefuted BLOCKING dissent from any one panellist blocks a pass regardless of majority. Composes with `--fuzzy` (replaces the single Reviewer) and rigorous mode (adds judgment beside the mechanical gate, which still governs); orthogonal to `--verbosity`/`--plain`/`--cost` (`role: "panel_reviewer"`). New vs.md § Step 5c + Flags + Model economy + Rules + artifact paths (`cycle-N/panel/`); 24 smoke checks guard the load-bearing properties. Authored chair-side per the /vss planner's hybrid mode; independently reviewed pre-commit by a blind read-only Sonnet reviewer, whose 7 findings (Step 6 never mandated reading the panel files; 5b/5c double-dispatch ambiguity; flow diagram omission; unvalidated N; overclaimed byte-identity; unannotated divergence from upstream's 4-6 panellists; one non-discriminating smoke check) were all fixed pre-commit.
- [x] **statusLine: one-glance model letter in every vibe window** — Martin's live ask mid-/vsss ("always show the current model, ideally single letter F/O/S/H"). The vibe-written `settings.local.json` heredoc now ships a `statusLine` command: model display name mapped to F/O/S/H (full name fallback, `?` when absent), a `vibe` cue to tell vibe windows from plain claude ones, plus `ctx N%` (context used) and `5h N%` (Pro/Max window used) segments that appear once Claude Code supplies them — e.g. `F · vibe · ctx 42% · 5h 63%`. Lands in every project on its next `vibe` launch, no rebuild. Schema verified against current docs (statusLine key valid in project settings.local.json; stdin carries `.model.display_name`, `.context_window.used_percentage`, `.rate_limits.five_hour.used_percentage`). Smoke: structural JSON assertions + six exact-output fixture runs of the real command string through `sh` (jq 1.6 `split` empty-string gotcha caught and fixed during dev — percentages use a type-guarded `floor`).
- [x] **`--sessions` auto-resume survives the out-of-credits block (heartbeat watchdog)** — task_016. Empirically (3 overnight instances) interactive claude does NOT exit when a Pro/Max usage window is exhausted; it blocks forever at the interactive usage-limit picker, so the existing relaunch loop never ran. Fix: `settings.local.json`'s heredoc gains PreToolUse/PostToolUse/Stop hook entries (matcher `""`) that atomically write an epoch to `.vss/heartbeat` while a `/vsss --sessions` marker exists; `launch_claude`'s body becomes `exec devcontainer exec ...` so a new `launch_claude_supervised` can background it, capture the real client PID, and run a `vibe_stall_watchdog` alongside it. The watchdog's kill gate is three conditions, all required — marker `active=1` (not `pending`, so the final window stays protected), the marker refreshed during THIS launcher session (portable `[ marker -nt ref ]` against a new session-start `VIBE_SESSION_REF`, with the relaunch call sites arming unconditionally via the literal sentinel `-`, and an empty ref from a mktemp failure never arming), and a stale heartbeat (`auto_resume_stalled`, default 1800s) — so a crash-left marker can never trigger a kill against an unrelated later interactive session. On trigger: warn, grace period, re-check, in-container kill (`vibe_container_kill_claude`, a literal bare `devcontainer exec ... pkill -TERM -x claude`) then host-PID fallback. Two hardening additions: a per-iteration `INT` trap in the countdown loop deactivates the marker on Ctrl-C (cancel means cancel) without touching the EXIT slot (the clipboard-watcher trap is untouched); and claude's exit status is now captured in `CLAUDE_EXIT` and propagated as vibe's own final exit code, replacing the old `|| true` swallow. New helpers (`auto_resume_deactivate`, `auto_resume_heartbeat_write`, `auto_resume_stalled`, `vibe_container_kill_claude`, `vibe_stall_watchdog`, `_vibe_stall_armed`) all live before the `VIBE_SOURCE_ONLY` guard for unit-testability. `.gitignore` gains `.vss/heartbeat*`; `MANUAL-TESTS.md` gains Test 32 (six sub-cases); `devcontainer/commands/vsss.md` § Auto-resume documents the launcher-side watchdog contract and why the skill's existing per-iteration marker refresh is now load-bearing. shellcheck clean (15 files); smoke suite green, 1048/1048 checks (44 task_016 assertions added by the /vs Tester, negatives hardened to be gate-discriminating in cycle 2), no regressions.

## 2026-07-08

- [x] **`/vsss --auto-resume N` renamed `--sessions X`** — Martin's call: the old flag was unmemorable. New semantics are the intuitive ones: X = TOTAL credit windows (`--sessions 3` = current window + up to 2 relaunches; `--sessions 1` = no relaunch), so the marker's `remaining` = X-1. The old flag counted EXTRA windows; the spec tells any future reader to treat a stray `--auto-resume N` as `--sessions N+1`. The `.vss/auto-resume` marker file, its KEY=VALUE format and the launcher's `auto_resume_*` helpers are all unchanged — the launcher never sees the flag, only the marker, so no launcher logic moved. vsss.md + README updated; smoke checks assert the new flag name and the X-1 arithmetic are documented.

- [x] **licence: staying MIT — AGPL/CLA drafts withdrawn; brain2 misrecording corrected** — Martin clarified he never confirmed the AGPL-3.0+CLA move; he confirmed the item after it in brain2 (the andeyePro transfer + vibe.andeye.com publish). Correction appended to brain2 coordination.md at source. His lean, which the analysis supports: MIT best drives adoption and contribution for a dev tool; "AGPL for consistency" reads to coders as not picking the right tool for the job; no vibe monetisation path needs a CLA (MIT already lets andeye — and anyone — build paid things on top; the candidate paid offerings, app/training/support, need no copyleft core). `LICENSE-AGPL3-DRAFT` + `CLA-DRAFT.md` removed (git history keeps them), `test_licence_drafts` → `test_licence_state` (asserts MIT + drafts absent + no "settled on moving" claims), README/CONTRIBUTING/web copy rewritten to MIT-and-set-aside, TODO item now asks Martin only to close the question.

- [x] **web copy: reclaim the vibe verb + smoke hermeticity fix** — per Martin: tagline stays "Vibe coding, verified." but the anti-slop CONTRAST returns to the body ("code you can actually ship – not slop") plus his reclaim passage: you don't know every nuance of how your car drives — that doesn't make you a sloppy driver; you haven't reviewed every binary digit of your machine code — that doesn't make you a sloppy coder; the abstraction was never the problem, shipping unreviewed guesses was. Separately, the long-standing `[brain2] gardener+no-zotero` smoke failure is fixed: an environment leak, not a launcher bug — OP-configured vibe containers stage `OPENPROJECT_MCP_URL`/`BEARER` (remoteEnv, since 2026-06-04), `_source_vibe_call` inherits the host env, so `_build_override_config` correctly emitted an `--add-host`-only override where the test expected base-config fallback. The mount-gating test now blanks the OP vars in all three cases; suite fully green for the first time in an OP-configured container (997+ checks, 0 failures).

- [x] **repo creation: choose the GitHub owner (org support)** — Martin's ask: creating a repo through vibe could only land on the personal account. `create_github_repo` now prompts `Owner (account or org) [<default>]` — default is `VIBE_GITHUB_OWNER` from `~/.vibe/config` (new, documented in the README config table) falling back to the logged-in gh user. A one-line `Your orgs:` hint (first 8, via `gh api user/orgs`) aids discovery without a picker, which would be unwieldy for many-org accounts. Error handling per the ask: owners are validated with `gh api users/<name>` (resolves both users and orgs) before creation, a miss re-prompts with a spelling nudge, and after 3 misses you may proceed unvalidated — the check itself can fail on a blocked network while the name is fine. Non-default choices print the config tip; a failed org create adds a repo-creation-permission hint and still launches without GitHub. TDD: `test_repo_owner_selection` (10 checks — `default_repo_owner` precedence unit-tested by sourcing the launcher, flow/validation/docs at grep level) written failing first. MANUAL-TESTS Test 31 covers the interactive path a host-side suite can't.

- [x] **docs purge: stale non-goals + README drift** — Martin flagged the repo carrying out-of-date positioning. CLAUDE.md `## Non-goals` rewritten as `## Non-goals and boundaries`: Windows reframed from "no native Windows" to unmaintained-not-unwelcome (a contributor owning it is welcome if the macOS/Linux path stays simple); the remote-host-containers and parallel-sessions non-goals dropped entirely (early claudebox-era scoping, no longer defended); the billing principle restated around the shipped `vibe --fable` consent pattern instead of reading like credit billing is banned outright. README: Fable free-window paragraph collapsed to present tense (the window ended 7 Jul 2026); Green-AI backend removed from Coming soon per Martin (no launcher support exists — `GreenPT/` is a Mac-side key-test tool only — and it's not a priority); firewall description updated from the stale "npm, GitHub, Claude API" trio to the real allowlist pointer; licence line aligned with the confirmed AGPL3+CLA direction; Contributing section links CONTRIBUTORS.md and adds the not-a-developer ask-your-Claude line; "Two house rules" un-counted (there are more now).

- [x] **agent-led non-coder onboarding + CONTRIBUTORS ledger + copy revisions** — Martin's direction (2026-07-08). (1) `ONBOARDING.md`: written FOR the user's assisting Claude (claude.ai/Desktop/app/Code) — ground rules (one verified step at a time, user runs everything, read errors don't guess) then eight steps from empty Mac to first session (Homebrew → OrbStack → node/gh/devcontainers-cli → `gh auth login` → installer → first `vibe` run) with a check per step; honest about the Pro/Max prerequisite. (2) `CLAUDE.md` gains an `## Onboarding` section routing any Claude that arrives via the public URL to ONBOARDING.md — this makes "ask your Claude to look at our CLAUDE.md and take you through vibe onboarding" (Martin's phrasing, now in the web copy) actually work, and is the interim non-coder path until a packaged app exists. (3) `CONTRIBUTORS.md`: the public ledger andeye's revenue-share promise operates on — maintainer, listed-on-first-merged-PR rule, honest today-state (no revenue accruing; assessment mechanism to be defined openly before money flows; never per-customer extraction). (4) `web/vibe-andeye.md` revisions: tagline `Secure, slop-free vibe coding.` → `Vibe coding, verified.` (Martin dislikes "slop" in the tagline; body de-slopped too), non-coder section rewritten around the ask-your-Claude path + app/training/support roadmap, contribute section links CONTRIBUTING (how) + CONTRIBUTORS (who), guinea-pig typo fixed. (5) Martin's three-pillar monetisation framing (app because coders won't feel the GUI pain / training / support) + a draft GitHub Discussion post recorded in the non-coder design-doc addendum; posting is Martin-gated in TODO. 7 new smoke checks under `[onboard]`.

- [x] **repo home is now `andeyePro/vibe`** — Martin executed the GitHub transfer (the fine-grained PAT survived it; verified with an authenticated API read). In-repo references swept `Aqueum/vibe` → `andeyePro/vibe`: `install.sh` (REPO_URL + curl one-liner), README (install command + footer repo link; Martin's personal `github.com/Aqueum` link intentionally kept), CONTRIBUTING.md clone URL, MANUAL-TESTS.md, `web/vibe-andeye.md` install command, and the container's git remote. Transfer item closed out of TODO.

- [x] **vibe: FOSS contributor-onboarding artifacts (SECURITY.md + `.github` issue/PR templates)** — the repo had a CONTRIBUTING.md and RELEASING.md but no security policy and no issue/PR scaffolding, which an andeye product inviting outside contributors needs. `SECURITY.md`: disclosure policy grounded in the actual threat model (bypassPermissions container held safe by the fail-closed firewall in `init-firewall.sh` + the `guard-bash.sh`/`guard-fs.sh` tool-call hooks; per-repo fine-grained PAT blast radius; subscription-auth-never-API-key; secrets no further than `~/.vibe/tokens`). In scope = firewall bypass, hook bypass, credential leakage, PAT scope escalation, host-mutation outside bind mounts; out of scope = bugs in Claude Code / Docker / OrbStack / base image (report upstream) and the container running arbitrary code inside its allowlist (that's the design). Reporting points at "this repository's Security tab" (GitHub private vulnerability reporting) so the path survives the Aqueum→andeyePro transfer, [redacted-email] as fallback, and no response-time SLA a solo maintainer can't keep. `.github/ISSUE_TEMPLATE/bug_report.md` (asks `vibe --version`, OS, Docker Desktop/OrbStack, what-happened-vs-expected, redacted launcher output) + `feature_request.md` (problem / proposed shape / willing-to-PR → CONTRIBUTING.md) as low-ceremony markdown, not YAML forms. `.github/PULL_REQUEST_TEMPLATE.md`: checklist for both gates green, CHANGELOG-in-same-commit, MANUAL-TESTS if lifecycle touched, CONTRIBUTING read. New `test_contributor_onboarding_artifacts()` (12 grep-level checks — SECURITY names the Security tab path + the Claude-Code/Docker upstream carve-out + the in-scope classes; the three templates exist; PR template names both gate scripts). Deliberately did NOT touch the existing `.github/workflows/ci.yml`. shellcheck clean; the one pre-existing smoke failure (`[brain2] gardener+no-zotero`) is known-broken on clean main.

- [x] **vibe: versioning + release scaffolding (`--version`/`-V`, VERSION file, RELEASING.md, CONTRIBUTING.md)** — the launcher had no version surface; every install was an untagged `git pull`. Now a bare-semver `VERSION` file at the repo root (`0.1.0`) is the single source of truth; `vibe --version`/`-V` reads it via `_vibe_version()` (resolved from the script's realpath through `VIBE_REPO_DIR`, so it works from any project dir and via the `~/bin/vibe` symlink; missing/empty VERSION → `vibe 0.0.0-dev`, never an error), and `--help` now footers the version line. New `RELEASING.md` (maintainer cut-a-release process: bump VERSION, CHANGELOG heading, tag `v<semver>`, push, `gh release create`; releases source-only, binary packaging parked) and `CONTRIBUTING.md` (dogfood pitch — contribute to vibe using vibe; gates before PR; TODO/CHANGELOG convention; MIT-today / AGPL3+CLA-under-consideration notice). README gains Versioning + Contributing pointers. TDD: `test_version()` (7 checks — `--version`/`-V` exit 0, single-line `vibe <semver>` regex, alias parity, VERSION file bare-semver + match) written failing first, then wired. shellcheck clean (15 files); the one pre-existing smoke failure (`[brain2] gardener+no-zotero`) is known-broken on clean main.

- [x] **vibe: FOSS-release positioning — vibe.andeye.com copy draft, non-coder-install design doc, install.sh preflight** — three things toward repositioning vibe as an andeye-family product. (1) `web/vibe-andeye.md`: draft page copy for vibe.andeye.com in Martin's voice, grounded in brain2 (coordination.md/andeye-strategy.md) — one-command containerised Claude Code, subscription-not-API, per-repo PAT blast radius, firewall+hooks make bypassPermissions safe, `/vs` adversarial rigor as the anti-slop story, developers-today / non-coders-soon (honest about Docker+terminal prerequisites), revenue-share + anti-enshittification ethos. Leads with a `<!-- DRAFT – not for publication -->` sentinel; licence line kept honest to repo reality (MIT today, AGPL3+CLA as the confirmed-but-not-executed direction). (2) `.vs/audits/foss-release-and-noncoder-install-2026-07-08.md`: design doc — current developer-only install reality, the non-coder gap, four options (Homebrew cask / curl-preflight / Mac .app / guided first-run) with effort + invariant-risk each, recommends preflight-now + Mac-.app-later with the strongest counter-argument named (a .app doubles the launch surface and every security invariant must then hold in two places — so the wrapper must call the launcher, never reimplement it), and the explicit Martin-gated list. (3) `install.sh`: a `preflight_deps()` block near the top that checks git/docker/node/devcontainer/gh before any clone or symlink and exits non-zero with one-line install hints for whatever's missing — no-op when all present, so the existing flow is unchanged. TDD: `test_vibe_andeye_page_draft` (sentinel) + `test_install_preflight` (grep + a stripped-PATH functional run asserting exit≠0, guidance printed, symlink step not reached). TODO gains the four Martin-gated items (repo transfer, relicence, vibe.andeye.com deploy, non-coder installer). shellcheck clean; pre-existing `[brain2] gardener+no-zotero` smoke failure untouched.

- [x] **vibe: AGPL-3.0 + CLA relicence drafts (placeholders, not in force)** — brain2 records the AGPL-3.0 + CLA move as confirmed (Martin, coordination.md 2026-07-07, "tidy-up/consistency"), so two clearly-marked draft artifacts land ahead of the actual relicence: `LICENSE-AGPL3-DRAFT` (a NOT-IN-FORCE placeholder with the recorded rationale, the andeye Ltd copyright line, and an instruction to drop in the canonical FSF AGPL-3.0 text verbatim at execution — deliberately not hand-transcribing 30KB of legal text into a draft) and `CLA-DRAFT.md` (individual contributor terms — copyright + patent licence to andeye Ltd with relicence rights, contributor keeps ownership, DCO-style sign-off; explicitly "contribute under MIT as normal until this is in force"). The active `LICENSE` is untouched and stays MIT; a smoke check (`test_licence_drafts`) asserts both drafts carry their not-in-force marker AND that LICENSE still reads MIT, so the drafts can never silently be mistaken for the active licence. Relicence execution + repo transfer remain Martin-gated (TODO).

- [x] **firewall: allowlist `download.swift.org`** (db45180) — user-requested single leaf host so Swift toolchain downloads work from vibe containers; sits in the existing non-fatal resolve loop, security-review agent pass (no injection surface, no wildcard — deliberately not all of `swift.org`). CDN-fronted, so the boot-time IP-snapshot caveat shared by the VS Code/sentry entries applies. Effective on next image rebuild. CHANGELOG line landed one commit late to avoid tangling a concurrent executor's in-flight CHANGELOG edits.

- [x] **check-sp-current.sh wired into container start** — the superpowers-drift probe (sp.md's hardcoded skill list vs upstream obra/superpowers `skills/`) previously only ran manually or in smoke tests; a renamed/added upstream skill would drift silently. Now the Dockerfile ships it to `/usr/local/bin/` and `install-claude-extras.sh` runs it at the end of every container start via a new `check_sp_drift()` — informational only (always exit 0, 10s network cap, silent when upstream unreachable), `SP_MD` pointed at the synced `~/.claude/commands/sp.md`, guarded `[ -x ]` so mixed old-image/new-script states skip quietly, and sharing the existing `VIBE_PLUGINS=0` opt-out. Plugin-freshness context that motivated it: superpowers itself auto-updates via the claude-plugins-official marketplace on session start (verified 6.0.3→6.1.0→6.1.1 in the plugin cache), so the sp.md list was the one unmonitored drift surface. 7 new smoke checks (`[sp-wire]`); shellcheck clean; the one smoke failure (`[brain2] gardener+no-zotero`) pre-exists on clean main.

## 2026-07-04

- [x] **vibe: revive `--fable` + billing gate, cutover corrected to 8 Jul** (632d3f1) — the archive/fable-5 implementation restored verbatim (flag, `fable_billing_phase()`, ask-before-credits gate, `VIBE_FABLE_CREDITS_OK`, README section, 9 smoke tests) with the cutover date moved 23 Jun → 8 Jul 2026 (Anthropic extended the free window; from 8 Jul Fable bills usage credits at $10/$50 per MTok on top of Pro/Max). CLAUDE.md non-goals amended per Martin's decision: credit-billed features are allowed when off-by-default behind explicit per-launch consent.
- [x] **vibe: auto-resume across credit-exhaustion halts** — closes the launcher half of the 2026-05-07 ask. `/vsss --auto-resume N` maintains a `.vss/auto-resume` marker (KEY=VALUE, digits-only values, fail-safe parsing); when `claude` exits with the marker active, the launcher counts down to the marker's estimated 5h-window reset (Ctrl-C cancels), decrements `remaining`, and relaunches `claude --continue "/vsss --resume"` into the existing `/vsss` Resumption protocol. `launch_claude()` wrapper extracted from the exec line; `auto_resume_field/pending/decrement` helpers unit-tested in smoke (9 new checks incl. injection-shaped values); `vsss.md` § Auto-resume rewritten from "out of scope" to the shipped skill-side contract (marker refresh per iter, `active=0` on any clean exit, fresh per-window budget). Mac-side zshrc wrapper interaction stays open in TODO.
- [x] **`/vs` model economy — Model plan, capability-gated escalation ladder, ask-before-credits** — model choice becomes part of the plan: spec.md gains a Model plan (starting tier + ceiling per role, Fable rung pre-authorised or not, user approves with the spec); Step-7 failures are classified `capability|spec|test|scope` and only repeated capability failures climb the ladder (Generator sonnet→opus→ask-before-Fable-on-locked-spec; Tester haiku→sonnet on test-quality flags); `--gen <tier>` / `--fable-gen` flags; escalations logged in progress.md and cost.json; passing tier recorded to calibrate future starting tiers. Chair stays the session model (Opus normally, `vibe --fable` for huge/ambiguous tasks — route by task class, not role prestige). Stale 2026-04-23 "chair tokens not trackable" claim superseded: chair usage is summed from the session's own transcript JSONL; cost schemas extended (per-tier lines, `est_credits_usd`). Structural-independence hardening from the landscape audit: `--fuzzy` Reviewer now dispatches as the read-only `code-reviewer` agent type, and the Evaluator must read raw `test-output.log` (never the Tester's summary alone) before a pass.
- [x] **`/vs` superpowers integration + `/vss`/`/vsss` alignment** — superpowers skills folded into role briefs (Planner: brainstorming/writing-plans; Generator: TDD with scratch tests + systematic-debugging; Tester + Evaluator: verification-before-completion; requesting-code-review heuristics on diff reads). `/vss`: session-model phrasing, `/loop` added to the planner's tool menu, Generator-tier choice per Model economy, and a new hard-escalate item — credit-billed dispatch without pre-authorisation. Settles the "/vs vs /sp" design decision: keep `/vs` as the adversarial structure, superpowers as per-role craft.
- [x] **`/budget` skill — month-to-date spend/quota rollup** — new `devcontainer/commands/budget.md`, self-contained (embedded python over `~/.claude/projects/*/*.jsonl` `message.usage` blocks): per-model tokens month-to-date + past-7-days, Fable priced at list rates (incl. cache read/write) as estimated credit spend, subscription tiers reported as quota pressure. Three disclosed constraints: estimates-not-invoices, Claude-Code-only (chat quota invisible), this-Mac-only. Per-task budgets live in `/vs`'s Model plan; `/budget` is the observability half.
- [x] **harness-routing CLAUDE.md fragment** — new `devcontainer/claude-md/harness-routing.md` synced into every container: plain prompts get routed (answer / direct edit / offer `/vs` / suggest `/vss`/`/vsss --auto-resume`/`/loop`-or-Routines) with explicit invocations always winning, one offer per task max, and the never-credits-without-consent rule restated at routing level.
- [x] **Harness landscape audit** — `.vs/audits/harness-landscape-2026-07.md` (two parallel web-research agents): no paid harness beats staying (April 2026 third-party-OAuth ban); Anthropic natively covers ~60–70% of vibe's headline (container story commoditised; Routines overlap overnight runs) but not the launcher/PAT/harness/auto-resume/billing-gate remainder; vibe is best-in-class on containment among subscription-compatible orchestrators. Two upstream patterns adopted into `/vs` now, two queued in TODO (Agent Teams migration, `--panel` anti-anchoring review).

## 2026-06-22

- [x] **vibe: consolidate reader-facing output into the final message + flag multi-list replies** — two coupled changes to stop vibe sessions smearing the text the user needs across several margin markers (the white dots). (1) New CLAUDE.md fragment `devcontainer/claude-md/output-consolidation.md`, synced into every container by `install-claude-extras.sh`: the final message of a turn is the complete deliverable; no reader-facing prose between tool calls; reasoning stays in the thinking channel — explicitly "never shorten reasoning to comply" plus a guard clause to prefer correctness when extended thinking is unavailable, so the rule can't degrade answer quality the way an old "work silently" instruction would; at most one numbered list per reply so a bare "1" is unambiguous to answer. (2) Extended the `check-numbering.sh` Stop hook to also warn when a reply contains more than one top-level "1." outside code fences — multiple restarting numbered lists, the case the original mixed-alphabet check missed. Non-blocking warning, same shape as the existing check; hook README updated. Token note: the fragment is the only standing cost (loaded every turn, cache-cheap) and it suppresses inter-tool narration, which is generated output, so the net token effect is neutral-to-down; the user's own replies get shorter (bare "1"). `code-check.py` shellcheck clean. Behavioural half is a soft steer not a hard guarantee (model interleaving can't be config-forced off); live effect lands on next container start/rebuild — verification TODO added.

## 2026-06-09

- [x] **vibe: model selection — `--model <id>` / `VIBE_MODEL`** — first model-selection mechanism in the launcher (previously every session launched on whatever default Claude Code had persisted in the shared config volume). `--model <id>` takes any Claude model id; `VIBE_MODEL="<id>"` in `~/.vibe/config` sets a standing default (flags win). The id is appended to the in-container `claude` invocation as `--model <id>` — per-launch only, never persisted. **Injection safety:** `is_model_id()` restricts ids to `^[A-Za-z0-9][A-Za-z0-9._-]*$` before they're embedded in the `bash -c` command string (same pattern as `RESUME_UID`), and the leading-alphanumeric rule stops `--model` from eating a following flag. Verified: shellcheck clean; smoke tests for parse, missing-value/metachar/flag-value rejection, `is_model_id` charset cases, `build_claude_model_args`, and help text. **Fable 5 split out:** a `--fable` sugar plus a billing-aware Fable 5 gate (`fable_billing_phase()`, `VIBE_FABLE_CREDITS_OK`) originally shipped alongside this on 2026-06-09. It was archived 2026-06-22 when Fable 5's Pro/Max free window closed (it began billing usage credits at API list rates on 23 Jun, colliding with vibe's subscription-only-spend non-goal). The full implementation is preserved at tag `archive/fable-5` and on branch `fable-option`; revive notes are in brain2. This branch keeps only the model-agnostic machinery.

## 2026-06-04

- [x] **brain2 reader fragment: user outranks unauthorised brain2 (cite + ask, don't assume)** — added a rule to `devcontainer/claude-md/brain2.md` (synced into every brain2-mounted container's `~/.claude/CLAUDE.md`): when the user contradicts a brain2 note or a recalled memory, quote the conflicting claim, cite its path, and **ask which is correct** — never silently defer to stored knowledge. brain2 still earns its keep by *challenging* the user with a cited counter-claim, but it's ask-don't-assume, not defer-to-either; grey/`authored` content is provisional by definition and can't outrank a live human. Once resolved, fix the note at the source immediately; never make the user re-prove a correction. Triggered this session: a stale "PR #3 stacked/pending" line in brain2 made the assistant insist #3 was unmerged after Martin had merged it — twice. Mirrored into `/brain2/CLAUDE.md` (gardener) under the trust model, and saved as a feedback memory. Docs-only; re-bakes into reader containers via `install-claude-extras.sh` on next rebuild.

- [x] **vibe: make `/op` functional in-container — Mac-side forwarder + reachability-gated HTTP MCP registration** — completes the OpenProject MCP integration (the credential half shipped earlier today). The container isn't on the tailnet, so a vibe-spawned **unprivileged** TCP forwarder (`op-mcp-forwarder.py`, `127.0.0.1:18443` → sidecar `:443`) bridges it; the container resolves the sidecar's real hostname to the host gateway via an injected `--add-host` (so TLS/SNI/cert stay valid end-to-end — Tailscale's cert is publicly trusted, no cert plumbing), and `register-op-mcp.sh` registers the HTTP MCP at postStart with an explicit bare `Host:` header (so the sidecar's DNS-rebinding guard accepts the non-443 port) plus `Authorization: Bearer`. **Reachability-gated:** registration probes `/healthz` through the forwarder first and skips (removing any stale entry) if it's down — never wires a dead endpoint, never errors on start. **Firewall:** `init-firewall.sh` now resolves `host.docker.internal` and `ACCEPT`s it when it falls outside the auto-allowed host `/24` (observed `192.168.65.254` on Docker Desktop) — **fail-closed**, wrapped exactly like the existing non-fatal domain loop (a resolution miss warns and carries on, never `exit 1` above the `-P OUTPUT DROP`). **No root daemon, no NAS change:** Claude Code's MCP client forwards a custom `Host` header verbatim (verified empirically), so the bare-`Host` trick sidesteps both a privileged 443 bind and a server-side allowlist edit. The forwarder is idempotent (self-exits if the port is already bound) and only starts when OP creds are staged; `--add-host` is injected into the per-session override config (derived from the URL, so no private hostname is committed). Verified host-side: the forwarder bridges a real bearer'd `initialize` (200) and re-launch is idempotent; `register-op-mcp.sh` reachable→registers the correct URL/Host/scope, unreachable→remove-only, no-creds→no-op; `--add-host` injection covered by new smoke test `test_op_mcp_addhost_injection`; shellcheck clean (15 files); `security-review` on the diff. Live in-container `/op` confirmation is a `vibe --rebuild` manual step (MANUAL-TESTS.md § OpenProject MCP).

- [x] **vibe: stage OpenProject MCP creds into the container (`$OPENPROJECT_MCP_URL` / `$OPENPROJECT_MCP_BEARER`)** — the OP MCP is now deployed live (tailnet-only sidecar at `https://openproject-mcp.tail09c06e.ts.net`; see brain2 `_OP-MCP-WIRING.md` §6). Wires the **credential half** into vibe containers, mirroring the `ZOTERO_API_KEY` passthrough: the launcher reads `OPENPROJECT_MCP_URL` + `OPENPROJECT_MCP_BEARER` from `~/.vibe/tokens` (reserved slash-free keys, no `owner/repo` collision) and exports them via `devcontainer.json` **`remoteEnv` only** (never `containerEnv`), so the bearer never reaches the firewall/postStart layer. Also hardened `lookup_token` (`cut -d= -f2` → `-f2-`) so a base64 bearer with `=` padding isn't truncated — benefits all token types; values without `=` unaffected. **Deliberately NOT done (parked, honest):** registering the HTTP MCP / making `/op` functional in-container. The container isn't on the tailnet, so reaching the sidecar needs a Mac-side host-routed forwarder that cannot be built from inside a container; that stays a TODO with the exact shape — this PR does not fake reachability. Verified: `lookup_token` returns the URL + bearer (incl. trailing `=`) whole while repo PATs still resolve independently; `devcontainer.json` valid; shellcheck clean (14 files); new smoke test `test_op_mcp_creds_lookup`; full suite green; `security-review` on the diff. (Desktop + Mac-terminal Claude already reach OP directly; this is purely the vibe-container credential staging.)

## 2026-06-06

- [x] **vibe: recover the orphaned brain2 skills-sync step; let triage/email-triage reach vibe** — `/triage` and `/email-triage` weren't installing in containers despite the skills-sync feature having been "merged". Root cause: PR #3 (`skills-sync-from-brain2`, merge `789ed97`) was merged on GitHub but its merge is **not an ancestor of `main`** — `main` was subsequently rebuilt through PRs #4/#5 on a parent chain that branched off before #3, orphaning it. So `install-claude-extras.sh` never actually gained a skills step; the `er`/`is`/`ol`/`op`/`md`/`script` skills present in containers were volume-persisted from earlier manual placement, not installed by the (missing) sync. Recovery: cherry-picked the orphaned feature commit `19a41d6` back onto `main` (`install_brain2_skills()` + `surfaces_has_vibe()`, gated on the brain2 mount, tag-filtered, overwrites only the skills it copies). Also retagged `triage` + `email-triage` to `surfaces: [desktop, vibe]` in brain2 — their original `[desktop]`-only tag rested on the now-disproven premise that a vibe container has no Gmail/Calendar access, but the first-party claude.ai connectors ride into any interactive vibe session (verified live; see brain2 `meta/mcp-in-vibe.md`), so both now sync and function (interactive sessions; headless/cron may lack the OAuth). Corrections folded in: the installer comment and the recovered CHANGELOG/MANUAL-TESTS text named triage/email-triage as *excluded* examples (true on 2026-06-03, stale now) — updated to cite `accounts-check`/`monthly-finance`; the synthetic smoke fixture named `triage` for an excluded skill was renamed `deskonly` to avoid the same confusion. Verified: `code-check.py` shellcheck clean (15 files); `test_install_extras_brain2_skills_synced` green (vibe-tagged synced, desktop-only + untagged excluded, pre-existing preserved, no-mount no-op). MANUAL-TESTS Test 29 (re-added, updated to expect triage/email-triage present) + Test 30 (pre-existing, kept). Live in-container confirmation is a `vibe --rebuild` then `/triage` (manual). Pre-existing unrelated smoke failure `[brain2] gardener+no-zotero falls back to base devcontainer.json` confirmed failing on baseline `eb999c0` too — not touched here.

## 2026-06-03

- [x] **vibe firewall: fail CLOSED instead of open (`init-firewall.sh`); allowlist `api.zotero.org`** — While verifying OpenProject reachability for a container I found vibe containers running with **no firewall at all** (kernel-default `OUTPUT ACCEPT`). Root cause: `init-firewall.sh` resolves an allowlist of domains and did `exit 1` on the first resolution failure — and that exit sits *above* the `iptables -P OUTPUT DROP` policy line. `statsig.anthropic.com` (a telemetry entry in that list) has been decommissioned and no longer has an A record, so the script bailed before ever applying DROP, on every boot, leaving the box wide open. (This is also why direct Zotero/OpenProject HTTP appeared to "work" — it was the absence of a firewall, not a capability.) Fix: (1) a `fail_closed()` handler on `trap … EXIT` forces INPUT/FORWARD/OUTPUT policy to DROP on any non-zero exit, so the backstop can only ever fail closed (egress drops to the DNS/SSH/loopback exceptions already set — SSH stays open for recovery); (2) per-domain resolution made non-fatal (warn + `continue`, `|| true` on the `dig` pipeline) so one dead domain no longer aborts the whole allowlist build; (3) removed the defunct `statsig.anthropic.com` (kept `statsig.com`); (4) added `api.zotero.org` so the Zotero key feature shipped earlier today keeps working now that the firewall actually enforces. Verified with a stubbed-command harness (no root needed): a domain that fails to resolve is warned+skipped and the script still reaches `OUTPUT DROP`; a GitHub-fetch failure trips the trap to `OUTPUT DROP` with no allow-rule added (locked down); the happy path allowlists `api.zotero.org` and omits `statsig.anthropic.com`. shellcheck clean (14 files); `security-review` agent clean (strictly hardens, no new exposure). MANUAL-TESTS Test 28 added. Live verification needs a `vibe --rebuild` (the running `/usr/local/bin/init-firewall.sh` is image-baked). **NOTE:** now that egress actually enforces, anything previously reachable only through the fail-open hole (arbitrary LAN / HTTP, including direct OpenProject at `[redacted-ip]`) is now blocked unless explicitly allowlisted — by design. OpenProject access is being routed via Tailscale as a separate, deliberate change (TODO).
- [x] **vibe: optional Zotero web-API key passthrough (`$ZOTERO_API_KEY`) into the container** — closed the gap where a vibe container couldn't reach Martin's Zotero library: the Zotero MCP server is Claude-Desktop-only, but `api.zotero.org` is reachable from inside the container (confirmed empirically), so the only genuinely-missing piece was the API key via a channel the container could read without the key transiting chat or git. Rejected the per-workspace gitignored `.zotero-key` file (point solution, secret proliferates across workspaces) in favour of mirroring the existing `GITHUB_TOKEN` path exactly: key lives in `~/.vibe/tokens` (the chmod-600 secrets file) under the reserved slash-free key `ZOTERO_API_KEY=...` (can't collide with an `owner/repo` PAT line); launcher resolves it with `lookup_token` and `export`s it beside `GITHUB_TOKEN` (`vibe` ~1213); `devcontainer.json` passes `${localEnv:ZOTERO_API_KEY}` via **`remoteEnv` only** — not `containerEnv` — so, like the GitHub PAT, the secret reaches the shell where Claude runs but never the firewall/postStart layer (`/proc/1/environ`). Added a conditional discovery line to the managed `~/.claude/CLAUDE.md` env-hint (`write-env-hint.sh`) so the in-container agent learns it can hit `api.zotero.org` via `$ZOTERO_API_KEY` with no MCP server. README host-state table updated; MANUAL-TESTS Test 27 added (env reaches Claude shell, absent from PID 1, silent no-op when unset). Verified host-side: JSON valid, shellcheck clean across 14 files, full smoke suite green, and the `lookup_token` data path resolves the 24-char key while repo PATs still resolve independently. **End-to-end confirmed live** (2026-06-03): after one image rebuild a sibling vibe session read all 116 Zotero collections and performed writes against `api.zotero.org` via the in-container `$ZOTERO_API_KEY`, and the env-hint discovery line baked into the container's `~/.claude/CLAUDE.md` through the rebuild.
- [x] **brain2 reader-stanza fragment: knowledge writes go direct-to-note, inbox is Zotero/OP-only** — `devcontainer/claude-md/brain2.md` still told reader containers "to record something… append to `/brain2/inbox/<source>.md`", framing inbox as a general intake tray. That predates the gardener-side model now canonical in brain2's own `CLAUDE.md`: readers write any update **directly** into the relevant note (immediate — visible to every local agent + Obsidian at once; lands `authored`/grey, provisional until Martin authorises), and `inbox/` holds **only** the Zotero/OP stockpile queue for firewalled containers. Updated the fragment to match; it re-bakes into reader containers via `install-claude-extras.sh` on next rebuild. The mounted `Brain2.md` home hub is Martin's and was left untouched (the brain2-vs-Brain2 filename collision caused a first-pass mis-edit, since reverted). Docs-only; no code or test change.

## 2026-06-02

- [x] **vibe launcher: canonicalise the workspace path before the bind mount (case bug)** — `vibe --rebuild` (and plain `vibe`) failed with `docker: ... invalid mount config for type "bind": bind source path does not exist: /users/martincurrie/<proj>` whenever the user's shell `$PWD` carried a lowercased volume name (e.g. `/users` instead of `/Users`). macOS APFS is case-insensitive-but-preserving, so `cd` into a lowercased path succeeds and plain `pwd` returns the logical lowercased string; Docker Desktop's bind-mount source validation is case-sensitive and rejects it. The `.ssh`/`.gitconfig` mounts use `${localEnv:HOME}` (canonical) so only the workspace mount failed, and every `--rebuild` re-derived the same bad path — hence the recurrence. Fix: resolve `WORKSPACE` with `pwd -P` (kernel `getcwd()` → canonical on-disk case + symlinks) at both workspace-resolution sites (`vibe` ~978/983), matching the existing `_canonical_workspace` helper. shellcheck clean; smoke suite green. Verified by mechanism + lint; not yet exercised against a live Docker daemon (the failing path is host-side mount validation, reproducible only on the affected Mac).
- [x] **vibe: config-driven shared-brain2 + Zotero bind mounts ("the /learnings way")** — generalised the learning-mount plumbing so every container can also mount Martin's cross-project second-brain repo (read-write at `/brain2`) and a Zotero store (read-only at `/zotero`), driven by `VIBE_BRAIN2_PATH` / `VIBE_ZOTERO_PATH` in `~/.vibe/config` (defaults `~/brain2` / `~/Zotero/storage`; `=off` to disable). Mounts are injected into the per-session `--override-config` JSON only when the resolved dir exists, so a generic vibe user without those dirs gets a silent no-op — nothing hardcoded into the committed `devcontainer.json`. The brain2 self-mount is skipped when the workspace *is* the brain2 repo (the gardener), avoiding a redundant double-mount. Refactor: replaced the single-purpose `learning_render_devcontainer_config` with a general `render_devcontainer_with_mounts <src> <dst> [<source> <target> <ro>]...` (paths passed as argv, never interpolated into JSON — spaces/quotes safe) and renamed `_learning_build_override_config` → `_build_override_config`, which now collects learning + brain2 + zotero triples. Added `_brain2_source`/`_zotero_source` resolvers and a `brain2 :` / `zotero :` startup banner. Shipped `devcontainer/claude-md/brain2.md` (read-the-hub-first + inbox filing conventions, the no-push credential boundary, the authored→checked→reviewed→authorised trust state with `authorised:` reserved for Martin, and zotero read-only + DOI-queue); it is gated in `install-claude-extras.sh` on the `/brain2` mount existing (`VIBE_BRAIN2_MOUNT_DIR` override for tests), so the shared `CLAUDE.md` stays brain2-free for users who don't opt in. README host-state table documents the two vars. Tests: 5 new smoke groups (source resolution incl. `off`, render incl. space-in-path, `_build_override_config` gating incl. gardener-skip and zotero-only, install-extras fragment gating both ways, fragment content); 4 existing learning tests updated to the new function names. shellcheck clean across 14 files; full smoke suite green. End-to-end mount not yet verified on a live container (see TODO).

## 2026-05-28

- [x] **task_015 manual verification PASSED on real Docker** – closes the 2026-05-27 ship caveat and the `## Open` MANUAL-TEST item. Ran MANUAL-TESTS Test 26 against Docker Desktop's containerd image store using a leaner single-project reproduction (supersede `vibe-dev:latest` in place with a `FROM vibe-dev:latest` / `RUN true` child build instead of the two-project `--rebuild` dance). Evidence: baseline container built from image `76526f`; the child build moved the tag to `e5b76b`; the next `vibe` launch printed `image moved on since this project's container was built - recreating it.` and created a new container (`831772` then `045e95`); an immediate third launch with no image change printed no drift line and reused `045e95`. The third launch is the load-bearing check: it confirms the step-4 re-resolve collapses the new container's image ref to the same canonical id as the tag under the containerd store, so there is no false-positive recreate-every-launch loop. Added the leaner repro to Test 26 as a documented variant.
- [x] **For-Martin pile hygiene** – retired the stale `### AEP-Plugin PR #16` subsection: PR amy-bo/electroPioreactor#16 merged and approved 2026-05-08T17:04Z, 36 minutes after the vibe-side rules commit `953db1a`, so all of gniezen's review items were addressed. Tracking another repo's merged PR inside vibe's TODO was the exact cross-lifecycle coupling that `953db1a`'s project-hygiene fragment warns against. Also cleared the `### Push and CI` backlog item (actioned by this push) and the completed GreenPT credit top-up item (€5 acquired 2026-05-28).

## 2026-05-27

- [x] **vibe launcher: auto-recreate containers built from a superseded image (task_015)** — before this fix, when project A rebuilt `vibe-dev:latest` every other project's existing container kept running from the old image until it happened to `--rebuild` itself. The observed failure mode was `env: 'claude': No such file or directory` on launch because the container's rootfs predated the current claude-code install baked into the image. Fix: two new helper functions defined above the `VIBE_SOURCE_ONLY` guard so they are harness-testable. `image_drift_needs_recreate <workspace> <image_tag>` queries Docker for the workspace's devcontainer, resolves both the container's source image and the current tag to their canonical config ids (using a second `docker image inspect` pass to neutralise the containerd manifest-vs-config digest false-positive), and emits `1` when they differ or when the container's source image has been pruned from the local store. `remove_existing_flag <rebuild_bool> <drift_marker>` is a pure function that echoes `--remove-existing-container` exactly once when either `--rebuild` or drift is active, guaranteeing the flag is never doubled. Launch-path wiring replaces the old single-condition append with calls to both helpers; a distinct status line ("image moved on since this project's container was built - recreating it.") distinguishes drift-triggered recreates from partial-state-retry recreates. shellcheck clean; existing smoke suite green. MANUAL-TESTS auto-recreate path (incl. the containerd false-positive guard, Test 26) must be exercised on real Docker before shipping.

## 2026-05-26

- [x] **`/learn` docs: remove the host-side `cd $VIBE_LEARNING_PATH && git add .` push footgun** — user-reported (high severity, potential secret exfiltration). The manual push fallback in `devcontainer/commands/learn.md` step 7 (and a duplicate in `devcontainer/claude-md/learn-hook.md`) told the user to paste `cd $VIBE_LEARNING_PATH && git add . && git commit && git push` into their **Mac host** shell. But `VIBE_LEARNING_PATH` is a container/config variable — the `vibe` launcher only sets it by sourcing `~/.vibe/learning.config`; it is **not** present in an interactive host shell. So `cd $VIBE_LEARNING_PATH` silently became `cd ~` and `git add .` staged the entire home directory; with a stray `~/.git` plus a configured remote this would commit and push secrets (`~/.ssh`, tokens). Observed 2026-05-26, interrupted one `&&` before commit. Compounding: in the common private-mode setup the library auto-syncs (Dropbox/iCloud) and is not a git repo at all, so the documented git flow never applied. Fix: dropped the dangerous one-liner from both files. `learn.md` now distinguishes auto-syncing libraries (nothing to do) from git-backed public-mode libraries (push is host-only via the upcoming `vibe learn --push`); the interim manual fallback uses a literal `/path/to/your/learning-library` placeholder and stages only the single new entry file (`git add <new-entry-filename>.md`), never the whole tree, and explicitly warns against pasting the container var. `learn-hook.md` rewritten to match. The *correct* mechanism already exists in the launcher's `learning_save` (stages only `$entry_path` via `git -C`, push only in public mode) — the docs just shouldn't have asked users to hand-roll a worse version. New regression test `test_learn_docs_no_host_stage_all_footgun` asserts neither doc contains `git add .` or `$VIBE_LEARNING_PATH` in host instructions (confirmed RED before the fix, GREEN after). Full smoke suite green, shellcheck clean. SHA pending commit.

- [x] **conversation-history.md: teach Claude to search past JSONL transcripts before saying "I have no record"** — user-raised: vibe instances frequently say they have no record of things discussed in earlier conversations, even though Claude Code already writes a full JSONL transcript per session at `~/.claude/projects/<slug>/<uuid>.jsonl` (persists in the `vibe-claude-config` Docker volume across container restarts on the same Mac). The gap was that no rule told Claude to look there. Original instinct was to add a parallel committed conversation log; Martin pushed back ("why duplicate data?") and the better answer was just to point at the existing JSONL files. New `devcontainer/claude-md/conversation-history.md` fragment auto-installed into every container's `~/.claude/CLAUDE.md` via the existing managed-block sync. Documents: the JSONL path glob (`~/.claude/projects/-workspace/*.jsonl` inside vibe — note no `sessions/` subdir despite my initial wrong guess); the record-type schema (user prompts have `type:"user"` with string content; tool results are also `type:"user"` but have list content of `tool_result` blocks — filter those out; assistant final text is `type:"assistant"` with text-typed blocks in `message.content[]`, skip `thinking` and `tool_use` blocks); three ready-to-use `jq` recipes (user prompts by keyword, assistant text by keyword, ordered USER/CLAUDE replay of a single file); the single-machine-only caveat (volume scope, not repo-committed); and an explicit "don't duplicate into a parallel file" instruction so future agents don't reinvent the log Martin already vetoed. Both jq recipes verified against a real session file before shipping. 9 new smoke-test sentinels (`test_conversation_history_fragment`) check the fragment exists with the JSONL path, slug, schema cribs, jq recipe, tool_result filter note, Docker-volume caveat, and no-duplication warning. shellcheck clean; full smoke suite green.

- [x] **SSH discipline: rewrite to offer-and-ask default + per-project autonomous-SSH opt-in** — user-raised mid-session: the original `ssh-discipline.md` defaulted to "show command, ask user to paste-back" which created a death-by-copy-paste loop and read as arsey when SSH is a core capability of vibe. Rewrote the fragment so the default is now "I can ssh to <host> and run <command> — OK?" with permission inline; paste-back becomes the fallback only when the user prefers to run it themselves. Kept the bypassPermissions blast-radius rationale in one short section. Added a Read Pure / Write Cautious sub-rule that scales the ask weight by what the SSH op does (read-only inspection → very light; destructive → explicit risk-named ask). New opt-in: `VIBE_SSH_AUTO=1` in `~/.vibe/config` (or `touch /workspace/.vibe-allow-ssh` per-project) skips the per-action ask entirely for projects where the user has pre-authorised autonomous SSH. Plumbed through `devcontainer.json` `containerEnv` + `remoteEnv` from host env, with launcher re-export so plain `VIBE_SSH_AUTO=1` (no explicit `export`) works in the config. `install-claude-extras.sh` omits the `ssh-discipline.md` fragment when either signal is set. README updated. 7 new smoke-test sentinels (`test_install_extras_ssh_discipline_opt_in`) covering both pass/skip paths plus a sanity check that other fragments still install when opt-in is set. Existing `task007/t14` ssh-discipline.md content checks still pass against the rewrite (>=50 non-blank lines, prohibition sentinel present, ssh/scp terms present). Shellcheck clean; smoke tests green.

## 2026-05-19

- [x] **README JIT-training prune (/vss iter, closes 2026-04-25 TODO)** — README dropped from 128 lines to 87 (32% reduction) by removing material vibe teaches just-in-time when users hit each moment. Specific changes: the `## What you get` slash-commands mega-paragraph (line 15, ~600 words documenting every shipped command) collapsed to one sentence pointing at `/help` for discovery and naming only the flagship commands (`/sp`, `/vs`, `/vss`, `/vsss`); dropped `## Authentication` entirely (Claude Code prints the login URL on first run); dropped `## First-run flows` entirely (it was meta-doc that literally said "vibe walks you through GitHub repo detection..."); compressed `## Usage` from 11 example commands to 5 most-common ones with `vibe --help` for the full list; tightened `## Adversarial coding mode /vs` to one explainer paragraph + a pointer to `devcontainer/commands/vs.md` for the full protocol; kept `## Prerequisites`, `## Install`, `## Host-side state`, `## Security model`, `## Coming soon`, `## License` mostly intact (they're install gates or trust signals or roadmap, not duplicated teaching). Smoke sentinels preserved: `test_task009_readme_updated` (literal "PreToolUse hook gates writes") and `test_sp_md_referenced_from_readme` (`/sp` mention). vibe launcher untouched — its `setup_token` and other first-run prompts already do the just-in-time teaching adequately; no echo-statement audit needed for this iter. Closes the 2026-04-25 `## Open` entry. Deferred: a `--quiet` / `--first-run-done` flag to suppress just-in-time prompts after first use (mentioned in the original TODO as a possible side-effect; not blocking since the launcher's existing prompts are already sub-second).

## 2026-05-18

- [x] **TODO.md prune pass 3**: dropped the now-empty `### Small bounded items I can ship without further input` subsection from `## For Martin (review and decide)` — all four items closed today (vs.md+README mapping, agent definitions, checkout@v5 bump, Mistral codestral audit), leaving the subsection as just its intro paragraph with no items. Subsection deleted. Also updated the For-Martin Design-decisions entry "TODO Open's bigger items" to drop the `/BDD` reference (dropped in pass 2) and add the XP-as-umbrella framing.

- [x] **TODO.md prune pass 2**: dropped `vibe --BDD session mode` per Martin's "we're looking at extreme programming with each as a subset" — TDD and spec-first stay as XP subsets, BDD is out-of-scope niche. Added XP-as-umbrella context note to the `vibe --TDD` entry so the methodology framing is durable. Tagged the XAP engagement-plan entry with a note that it should likely move to a separate XAP repo eventually (XAP is a distinct standard from vibe and the engagement-plan lifecycle is separate).

- [x] **TODO.md prune pass 1**: removed `## Open` entry "Authenticated Mistral codestral probe" (added earlier today as a follow-up to the Mistral codestral comparison audit but superseded immediately by Martin's pushback that the whole Mistral evaluation track was scope creep). Rephrased the For-Martin Push-and-CI item to drop the stale "17 commits since 2026-05-09" count in favour of a `git rev-list --count` recipe that stays accurate as commits accumulate. Five other entries surface as ambiguous in the follow-up Q3 to Martin; not removed autonomously.

- [x] **`/expaste` retired** — added `expaste.md` to `install-claude-extras.sh`'s `RETIRED_COMMANDS` array so existing user-state copies get cleaned up on next container start. Source `devcontainer/commands/expaste.md` was already absent (removed in a prior cleanup); the user-volume copy was the residual. `/expaste`'s use case (load clipboard + nudge `/exit`) inherited `/c`'s Opus-turn slowness without adding distinct value beyond a single-line reminder. Better alternatives now in everyday use: mid-turn Bash-write to `/workspace/.vibe/copy-latest.txt` (no slash command needed, no Opus turn), `!vibe-copy <file>` via the TUI shell (OSC 52, no host-watcher dependency), or simply opening a parallel Ghostty tab when the user wants to keep working AND paste somewhere else. Removed the "Migrating from `/expaste`" subsection from `devcontainer/hooks/README.md` and closed two open `/expaste` TODO entries. Third TODO ("vibepaste Mac shell function") kept but its reference to `/expaste` removed. No new smoke-test sentinel needed — existing `test_install_extras_retires_old_commands` (AC19c) already verifies retirement cleanup runs without touching `agents/`.

- [x] **Mistral codestral public-docs probe + Green-AI vendor comparison (/vsss iter 4 2026-05-18)** — new audit at `.vs/audits/mistral-codestral-probe.md` mirroring the shape of the 2026-05-07 GreenPT audit. Findings: (1) `api.mistral.ai` reachable through the vibe firewall without changes (HTTP 401 on unauth, not connection-refused); (2) Mistral's API does NOT expose per-request environmental impact in response bodies or headers — sharp contrast with GreenPT's `impact:` field; (3) Mistral instead publishes industry-first peer-reviewed annual LCA (Mistral Large 2: 20.4 ktCO₂e, 281,000 m³ water; marginal ~1.14 gCO₂e per 400-token response) with ADEME + Carbone 4; (4) codestral pricing $0.30/$0.90 per 1M input/output tokens with FIM endpoint at `/v1/fim`, 32K context. Verdict: Mistral and GreenPT are **complementary, not competitive** on disclosure — GreenPT optimises for developer-facing per-request, Mistral for regulator-facing annual. For vibe's Green-AI integration, Mistral plays an additive codestral-specialist role (Shape 2-bis) but cannot substitute for GreenPT's per-request transparency. Closes the last remaining For-Martin "Small bounded items I can ship without further input" entry. Two follow-up TODO entries added: authenticated Mistral probe (gap analysis from §"What an authenticated probe would add") and the green-aux integration spec sketch (already gated on Vibe001 credit top-up). 8 sources cited in the audit; WebSearch+WebFetch only, no authenticated API calls.

- [x] **CI: bump `actions/checkout@v4` → `@v5` (/vsss iter 3 2026-05-18)** — one-line change in `.github/workflows/ci.yml` to clear the Node 20 deprecation warning. `@v5` is the current GA major (released 2024-08, mature as of this commit). No behavioural change for vibe's CI workflow — `checkout@v5` is a drop-in replacement for `@v4` with the same default behaviour. Closes one of the two remaining For-Martin "Small bounded items I can ship without further input" entries. Will be verified by the next push (autonomous push disabled per `/vsss` policy — Martin reviews and pushes manually).

- [x] **vibe agents: ship canonical Software Architect / Code Writer / Code Reviewer definitions in `devcontainer/agents/` (2026-05-07 audit follow-up, /vsss iter 2 2026-05-18)** — three new agent files (`software-architect.md`, `code-writer.md`, `code-reviewer.md`) following the shape of the existing `security-review.md` / `shellcheck-fixer.md` agents (YAML frontmatter with name/description/model/tools, then a Process / Output / Rules body). Models tier-matched to the role: Architect = Opus (design judgment), Code Writer = Sonnet (cost-efficient routine implementation), Code Reviewer = Opus (review verdict judgment). Each agent's body footer cross-references `devcontainer/commands/vs.md § Modes` so users invoking the canonical agents from `/agents` understand how the `/vs` slash command's adversarial flow extends the three-role pattern. Closes the second of the two follow-up TODO entries from the 2026-05-07 audit (the first half — vs.md + README mapping — landed earlier today as iter 1). `install-claude-extras.sh` auto-syncs `devcontainer/agents/*.md` to `~/.claude/agents/` on container start; no Dockerfile or hook changes. Shellcheck clean across 14 files; smoke-test green (agent files are free-form Markdown, no sentinels needed beyond the existing AC19c retirement-cleanup test that verifies the agents/ directory isn't accidentally tampered with).

- [x] **vs.md + README: canonical Architect/Writer/Reviewer mapping (2026-05-07 audit follow-up, /vsss iter 1 2026-05-18)** — added § "Relation to canonical Architect/Writer/Reviewer" subsection under `vs.md ## Modes` with a 1-paragraph explanation plus a 3-row mapping table (Architect ↔ Planner+Spec Critic, Writer ↔ Generator, Reviewer ↔ Tester-or-Reviewer + Evaluator). Mirrors the recommendation in `.vs/audits/architect-writer-reviewer.md` Findings §"Where the three canonical names could add value". Added a 1-sentence bullet under README's `## Adversarial coding mode /vs` section pointing users coming from Claude Code's welcome-banner tip at the `/vs` mapping. Closes one of the two follow-up TODO entries from the audit (the optional pre-configured agent definitions in `devcontainer/agents/` remains open — separate /vs task).

- [x] **vibe launcher: pbcopy the suggested GitHub token name in `setup_token`** — when the launcher walks the user through creating a fine-grained PAT for a new repo, step 1 of the on-screen instructions previously said "Give it a name, e.g.: vibe-<repo>" and the user had to read and re-type the value into GitHub's web UI. Now `setup_token` does a best-effort `pbcopy` of `vibe-<repo>` before printing the message; if pbcopy succeeded the message degrades to "Give it a name (clipboard has 'vibe-<repo>' — ⌘V into 'Token name')". Silent fallback to the original "e.g.:" wording if pbcopy is unavailable (Linux host with no `xclip`, or unusual macOS state). One-line wording change in `vibe`'s `setup_token` body plus a four-line guard; no devcontainer or hook changes. Shellcheck clean across all 14 files, smoke tests still green.

- [x] **vibe: invert `copy-last-block.sh` default to opt-in via `<!-- vibe: copy -->` sentinel** — user feedback: the prior auto-copy-every-turn default (with `<!-- vibe: no-copy -->` opt-out) clobbered the Mac clipboard ~99% of the time when there was nothing the user actually wanted to paste, and the alternative of running `/c` manually adds an LLM round-trip that takes "ages". Inverted the default in `devcontainer/hooks/copy-last-block.sh` so the hook is silent unless the assistant message contains the literal sentinel `<!-- vibe: copy -->` anywhere in the text. The opt-out marker (`<!-- vibe: no-copy -->`) is removed — it has no meaning under the new default. `~/.claude/settings.json` wired to call the hook alongside `check-numbering.sh` in the Stop array (per-user; vibe still does not auto-edit other users' settings, this commit only wires Martin's host-volume settings file). `devcontainer/hooks/README.md` and root `README.md` rewritten to document the inversion. Smoke-test updates: prior happy-path tests had the marker added so they still assert "marker + block → write"; the old `test_copy_last_block_opt_out_marker` replaced with `test_copy_last_block_no_marker_silent` asserting the inverted contract (block present, no marker → no write). 16 copy-block sentinels still pass; 14 shellcheck files still clean. Tradeoff documented in hooks/README §Trade-offs: assistant must remember to include the sentinel; `/c` remains the manual override when the assistant forgets.

## 2026-05-08

- [x] **vibe: TODO/CHANGELOG split + project-hygiene rules from AEP-Plugin PR review** — three structural fixes from upstream-reviewer feedback on amy-bo/electroPioreactor PR #16. (1) `install-claude-extras.sh` adds a managed `vibe-managed runtime exclusions` block to `/workspace/.gitignore` on first container start, excluding `.claude/settings.local.json` and `.vibe/` so vibe's runtime cruft can never silently ship to a downstream repo. Idempotent on re-run; respects user-removed-block (no re-add); opt-out via `VIBE_AUTO_GITIGNORE=0`. (2) Split TODO.md and CHANGELOG.md responsibilities: TODO is open backlog plus `[!]` abandoned items with failure context (maintainer-facing); CHANGELOG is reverse-chronological done-work audit (reader-facing, scannable by upstream reviewers). vibe's existing `TODO.md ## Done` (37 entries) migrated verbatim to this file. CLAUDE.md TODO section rewritten to spec the split. (3) Two new cross-project CLAUDE.md fragments propagating the rules to every vibe container: `devcontainer/claude-md/todo-changelog.md` (TODO/CHANGELOG split) and `devcontainer/claude-md/project-hygiene.md` (don't commit per-machine runtime files / setup-specific notes / unconsented system-patching scripts in upstream-bound repos; pre-commit checklist for hardcoded IPs, hostnames, and personal paths). 27 new smoke-test sentinels (`test_todo_changelog_split`, `test_project_hygiene_fragment`, `test_install_extras_ensures_project_gitignore`).

## Migrated from TODO.md ## Done on 2026-05-08


- [x] **vibe: auto-promote cross-repo feedback memories to /learnings (2026-05-07 /vsss-resume iter 3)** — closes priority 4 of the original deferred queue. New `devcontainer/claude-md/feedback-auto-promote.md` fragment (always active inside every vibe container, picked up by `install-claude-extras.sh`'s claude-md sync). Behavioral rule for Claude: after saving a `feedback` memory, if it meets ALL FOUR cross-repo-applicable criteria (behavioral/preference rule not project-specific fact; applies regardless of project; not duplicate of existing /learnings entry; user hasn't opted out), propose `Cross-repo applicable - save to /learnings? Y / n / never-ask` as a single-line prompt. On Y → run `/learn <distillation>`; the existing PreToolUse hook on the /learnings write is still the security boundary. On `never-ask` → session-level suppression + surface the host-side `VIBE_AUTO_PROMOTE=0` opt-out. Composes with the /learn slash command (mechanism), task_010 smart-capture (quality gate), and the PreToolUse hook (security boundary) — three-layer trust model. 10 new smoke-test sentinels (`test_feedback_auto_promote_fragment`). Direct-edit; no /vs cycle.

- [x] **/vs: plain-English vs techy modes + 0-9 verbosity knob (2026-05-07 /vsss-resume iter 1)** — vs.md § Flags now declares `--plain` (default ON; clear concise English, minimal harness terminology), `--techy` (inverse opt-in; terse technical shorthand for power users), `--verbosity N` (0-9 global, default 5), and per-output overrides `--vN-spec` / `--vN-test` / `--vN-eval`. Verbosity scale anchors documented at levels 0/3/5/7/9; intermediate levels linearly interpolated. Flags propagate from Planner into subagent briefs at dispatch time; `--plain`/`--techy` and verbosity are independent dimensions (cross-product always meaningful). Per-output overrides win over global. Open design questions from the original TODO entry resolved per Q3=c bias-to-ship inside priority items: `--plain` chosen as default since most humans (including Martin per memory `feedback_plain_english_approval`) prefer it; verbosity applied to ALL THREE outputs (Spec Critic critique, Tester summary, Evaluator verdict) independently. Sharing plain/techy preference via /learnings library deferred — would need the auto-promote-feedback-memories work that's a separate priority. 13 new smoke-test sentinels (`test_vs_md_plain_techy_verbosity_flags`). Direct-edit shape; no /vs cycle (priority 2 was knowable defaults + flag spec; full /vs cycle would have been overkill).

- [x] **/vs role-decomposition audit vs canonical Architect/Writer/Reviewer pattern (2026-05-07 /vsss iter 7)** — research/recommendation report at `.vs/audits/architect-writer-reviewer.md`. Verdict: do NOT re-cast /vs. /vs's 5-role decomposition (Planner / Spec Critic / Generator / Tester / Evaluator, plus Reviewer in fuzzy mode) strictly extends the canonical 3-role pattern with adversarial-separation Spec Critic before coding and mechanical-Haiku Tester separate from judgment-tier Evaluator. Collapsing to 3 roles would lose the adversarial gains (Spec Critic alone caught 4 BLOCKING + 9 MEDIUM concerns in task_010 cycle 1's iter 1). Two follow-up TODOs filed: (a) bounded direct edit to add a "Relation to canonical Architect/Writer/Reviewer" mapping paragraph in vs.md + 1-sentence bullet in README; (b) optional bounded /vs task to ship pre-configured `software-architect.md` / `code-writer.md` / `code-reviewer.md` agent definitions in `devcontainer/agents/` for users who want the canonical names one-keystroke (independent of /vs). No code change in this iter; report-only.

- [x] **task_010 — /learn smart-capture (always intelligent) — passed cycle 1 (2026-05-07 /vsss iter 3)** — extended `devcontainer/commands/learn.md` with a "## Semantic check" phase between format-body and preview steps in the "How it works" list. New phase reads existing `/learnings` entries for contradictions, flags low-quality input (vague references, unclear input), zero-friction passthrough for already-good input (no options surfaced), Z1..Zn option scheme when improvement possible (Z1 is always user-verbatim, Z2-Z3 smarter alternatives capped at 3, Z_n+1 edit-existing-contradiction option, N drops the new capture). Marginal cost ~2-5k tokens per `/learn`; runs on every invocation (no conditional skipping). Spec Critic took 3 iterations (4 BLOCKING + 9 MEDIUM/LOW iter 1 → 1 BLOCKING + 1 MEDIUM + 1 LOW iter 2 → pass iter 3 with 0 new concerns). Generator passed; Tester wrote `test_task010_smart_capture` with 30 checks (above the 19 floor). Cycle closed in 1 cycle vs TODO's predicted 2. Files changed (subset of AC18 allowlist): `learn.md`, `smoke-test.py`, `.vs/spec.md`, `.vs/tasks.json`, `.vs/cycle-1/*`. Mid-cycle interruptions handled cleanly: 270s wait-skip clarification (commit f520904) and `/vsss --hours` flag + Resumption protocol + auto-resume design (commit 82003e5) shipped as separate iterations. Pre-existing `test_task013_diff_scope` retired same commit (was an over-scoped freeze guard tripping on legitimate task_010 files; same pattern as `test_task008_ac15` retirement 2026-04-26).

- [x] **vibe: per-session audit trail + no-autonomous-push policy for /vss + /vsss (2026-05-07)** — user-raised mid-flight: `.vss/loop.md` was a one-line-per-iter rolling file overwritten each /vsss start, not a real reviewable audit. Replaced with `.vss/sessions/<ISO-start>.md` rich per-session format (Mode / Args / Initial plan / per-iter blocks with Plan / Files / Commit / Outcome / Notes / Final state). Migrated the existing committed loop.md to `.vss/sessions/2026-05-07T13-59-32Z.md` and rewrote its content into the new format. Also: explicit no-autonomous-push policy — `/vss` and `/vsss` no longer push at exit; user reviews the session file and pushes manually. `--push-on-pass` flag opts in for a single run. Spec changes in `vss.md` (`## Session audit format` + `## Push policy` sections under State directory) and `vsss.md` (replaced loop.md references; added `git push` to the safety floor; reporting-back now names the session-file path and reminds about the manual-push step). 7 new smoke-test sentinels (test_vss_md_audit_trail_and_push_policy) plus stale-loop.md negative check in the existing /vsss test. README enumeration updated. CLAUDE.md unchanged (shipped-extras line already accurate). One-time autonomous push performed for THIS commit at user's explicit authorisation; from the next /vsss invocation onward, autonomous push is disabled by default.

- [x] **/sp: resolve drift against upstream obra/superpowers (2026-05-07 /vsss iter 4)** — `check-sp-current.sh` flagged 7 skills missing from sp.md vs upstream (`dispatching-parallel-agents`, `executing-plans`, `receiving-code-review`, `systematic-debugging`, `using-superpowers`, `verification-before-completion`, `writing-skills`). Added all 7 to sp.md's bullet list with one-line descriptions; reframed the lead-in from "the seven core skills" to neutral wording (Superpowers now ships 14 skills, not 7). Updated `SP_CORE_SKILLS` in smoke-test.py and `expected_skills` in `test_sp_md_present_and_complete` to match, so a future drop in either direction is caught (was: silent on additions; would only catch a drop of one of the original 7). Probe now exits clean (zero stderr).

- [x] **MANUAL-TESTS: re-order Test 20 to its numerical slot + extend Test 24 for /vss + /vsss (2026-05-07 /vsss iter 3)** — Test 20 (`git config --global` works) had been sitting at the bottom of the file between Test 23 and Test 24, breaking the monotonic numbering readers rely on; moved to its correct slot between Test 19 and Test 21. Also extended Test 24's "shipped commands" check-list to include `vss.md`, `vsss.md`, and the corresponding picker-acknowledgement bullets, matching the contents of `devcontainer/commands/`. Pure documentation drift fix; no code change.

- [x] **vibe: smoke-test sentinels for /vss + /vsss safety boundaries (2026-05-07 /vsss iter 2)** — added three smoke-test functions covering the canonical hard-escalate sentinels in `vss.md` (physical hardware actuation, SSH-out, /vs --fuzzy verdicts, force-push, --no-verify, /learnings, firewall, scope creep) and `vsss.md` (inherited escalate list, BUDGET_HOURS=5 default with explicit no-stale-4h check, safety floor section, .vss/loop.md audit-trail declaration, three-no-op exit condition). 30 new checks. Catches regressions where a careless edit silently drops a safety sentinel; without these, the autonomous-mode safety boundaries are untestable.

- [x] **vibe: ship `/vss` and `/vsss` autonomous-runner commands (2026-05-07)** — `/vss` (versus solo) is a one-shot autonomous wrapper: no args → pick first bounded `[ ]` from TODO.md or fall back to a repo-scan with a 270s redirect window (under the 5-min cache TTL) and a terminal-bell notification (`printf '\a' >&2`, vibe's canonical idle-bell idiom — Mac terminals translate to system sound + dock bounce) at the announcement moment; args → Opus planner picks `/vs` / `/vs --fuzzy` / `/sp` / a named superpower / direct-edit, then announces and proceeds without approval, executor acting as user within a documented hard-escalate list (physical actuation, SSH-out, `/vs --fuzzy` subjective verdicts, destructive git, `/learnings` writes, firewall/hook/perm edits, scope creep). `/vsss` (versus super solo) wraps `/vss` in an autonomous loop with an Opus optimiser between iterations, exit conditions for perfection-gate / 5h budget (full Pro/Max session, no graceful-shutdown cushion — optimiser biases toward "stop" near the cap) / three-no-op A-mode iterations / hard-escalate / destructive git, and `.vss/loop.md` rolling log. Both files at `devcontainer/commands/vss.md` and `vsss.md`; CLAUDE.md shipped-extras line updated. Self-bootstrapped: this entry was created by `/vss` running in B-mode against the spec request.

- [x] **vibe: ship `copy-last-block.sh` Stop hook (2026-05-02 autonomous session)** — Stop hook that auto-extracts the LAST fenced code block from each assistant turn and writes it to `/workspace/.vibe/copy-latest.txt` for the host-side `vibe-copy-watcher.sh` to `pbcopy`. Zero LLM round-trip required. Per-turn opt-out via the literal sentinel `<!-- vibe: no-copy -->` anywhere in the message. Per-user opt-in: add a Stop-hook reference to `~/.claude/settings.json` (vibe does not auto-edit user settings). Awk state-machine handles language-tag stripping, multi-line preservation, multiple-block "last wins". Falls back gracefully on missing transcript / empty stdin / no fenced blocks. New `devcontainer/hooks/copy-last-block.sh` (60 lines, shellcheck clean). `devcontainer/hooks/README.md` rewritten as a multi-hook landing page that documents both `check-numbering.sh` and `copy-last-block.sh` (plus the trade-offs and the way `/c` still composes for override use). 17 new smoke checks (file shape, single block, language-tag stripped, multi-block last-wins, no-fence no-write, opt-out marker, multi-line preservation, empty stdin). 14/14 shellcheck files clean (was 13). The companion TODO entries `vibe: vibepaste Mac shell function` and `vibe: /expaste should actually exit` remain open — both are simpler now that the watcher's clipboard is auto-fresh.

- [x] **vibe: ship `check-sp-current.sh` /sp upstream drift probe (2026-05-02 autonomous session)** — landed the actionable half of the original "keep /sp skill list current" TODO. New `devcontainer/check-sp-current.sh` script extracts hardcoded `superpowers:foo-bar` skills from sp.md (regex `superpowers:[a-z][a-z0-9-]*`, sort/uniq), fetches upstream `obra/superpowers/contents/skills` via the GitHub API (curl + jq, 10s timeout), `comm`-diffs the two sets, prints a stderr summary distinguishing "Missing from sp.md (in upstream skills/)" vs "Extra in sp.md (not in upstream skills/)". Three modes: default online (fetch + compare); `--offline` (skip fetch, exit silent); `--fixture FILE` (read upstream from a local file, used by smoke tests + offline-CI). Always exits 0 in informational paths; exit 1 only on usage errors (`--fixture` without readable file, unknown flag). Fails soft when curl/jq missing or network unreachable: stderr note, no failure. 18 new smoke checks (file shape, --offline silent, no-drift fixture, missing-skill drift, extra-skill drift, fixture-nonexistent error, unknown-arg error). Code-check.py picks up the script automatically (in `devcontainer/*.sh` glob); 13/13 shellcheck files clean (was 12). Auto-invocation from `vibe --rebuild` deferred to a follow-up TODO.

- [x] **task_013 — /vs intelligent Spec Critic iteration cap — accepted 2026-05-02 autonomous session, residual: AC10 process defect** — picked option (b) of the parked entry: accept residual and mark Done. Feature work is complete and tested: vs.md Step 3 now documents convergence (loop until pass), plateau detection (same BLOCKING set twice → user picks accept/restart/drop), divergence detection (concern count grows three iterations → restart), and `--max-iter N` flag for explicit caps. 4 test functions / 13 checks for the new sentinels (test_task013_vs_md_intelligent_stopping, test_task013_vs_md_max_iter_flag, test_task013_no_hardcoded_cap_string, test_task013_diff_scope), all passing in HEAD's test suite. Residual AC10 process-defect (`test_task013_diff_scope` was over-anchored — it reads `.vs/cycle-1/diff.patch` if present, but `git diff` snapshots include Planner bookkeeping in `.vs/spec.md` + `.vs/tasks.json` which the spec didn't whitelist) is benign in practice: diff.patch isn't materialised for committed cycle-1 work, so the test gracefully skips. Lesson: cycle-N harness-bookkeeping artifacts should be explicitly whitelisted in scope-check ACs from cycle 1 going forward. Also commits task_014 cycle-1 spec entry (per-project Claude `projects/` bind, parked at planner stage) and the task_013/task_014 spec.md updates.

- [x] **vibe: `~/.vibe/skipped` persistence — fixed canonical-path mismatch (2026-05-02 autonomous session)** — root cause: `is_github_skipped`/`mark_github_skipped` matched paths via literal `grep -qxF "$WORKSPACE"`, but `$WORKSPACE` came from `pwd` and could differ across runs by trailing slash or symlink resolution. Reproducer (`/tmp/skip-test/run.sh`) confirmed: mark with `/foo/bar`, lookup with `/foo/bar/` → no match; lookup with symlinked `/foo/symlink` (→ bar) → no match. Real-world trigger: macOS Dropbox/iCloud mirror dirs symlinked to `~/Projects` paths; `pwd` returns the form the user `cd`'d through. Fix: new `_canonical_workspace` helper (`(cd && pwd -P) || slash-stripped fallback`), `mark_github_skipped` writes canonical form, `is_github_skipped` matches both literal `$WORKSPACE` (back-compat) and canonical. 7 new smoke tests cover mark+check round-trip, trailing-slash tolerance, symlink-equivalent path, mark-writes-canonical, unrelated-path-rejected, back-compat literal. shellcheck clean across all 12 shell files.

- [x] **vibe: ship `check-numbering.sh` Stop hook (2026-05-02 autonomous session)** — picked option (c) of the original TODO: shipped script via `install-claude-extras.sh` into `~/.claude/hooks/check-numbering.sh`, NOT a CLAUDE.md fragment. New dir `devcontainer/hooks/` with the bash script (jq + awk; reads Stop-hook payload, finds last assistant message in transcript, strips fenced code blocks, regex-matches `^[[:space:]]*[0-9]+\.[[:space:]]` and `^[[:space:]]*[a-z]\.[[:space:]]`, exits 0 + stderr warning if both shapes co-occur). Behaviour is opt-in: script ships to every vibe user but only fires if `~/.claude/settings.json` references it (vibe does not auto-edit user settings). `devcontainer/hooks/README.md` documents the wiring; not injected into CLAUDE.md to avoid preaching the rule to users who don't share the preference. Dockerfile gains `COPY hooks/`; `install-claude-extras.sh` gains `install_hooks()` (chmod +x for *.sh; non-.sh files in src like README.md silently ignored). `code-check.py` extended to scan `devcontainer/hooks/*.sh` (smoke-test.py's literal-string patcher updated to match). 21 new smoke checks (file shape, silent-on-numbered-only, silent-on-lettered-only, warns-on-mixed, ignores-fenced-code, edge cases for empty stdin / no path / unreadable transcript, README presence, install-extras sync). Mid-task: retired `test_task009_install_extras_unchanged` as a stale freeze-guard exactly per `feedback_vs_tester_no_file_freeze_guards.md` — it had been blocking legitimate edits to install-claude-extras.sh forever from task_009 forward. 12/12 shellcheck files clean, all smoke tests pass.

- [x] **vibe: `vibe vibe --continue` / `vibe vibe --resume` were silently dropping the trailing flag (2026-04-29)** - bug: leading-only flag parser (`while [[ "${1:-}" == --* ]]`) exited as soon as `$1` was a non-flag positional, so any `--*` argument after the project name was never read; `vibe vibe --continue` always launched a fresh session. Fix: extracted `parse_vibe_args` as a function defined above the `VIBE_SOURCE_ONLY` guard (so smoke-test.py can source-test it); single while-loop accepts `--rebuild`/`--continue`/`--resume [<uuid>]`/`--list`/`--help` and one optional positional in any order; rejects unknown flags and a second positional. Workspace resolution updated to read `$PROJECT_ARG` instead of `$1`. Help text gains a four-line note showing `vibe vibe --continue` and `vibe --continue vibe` are equivalent. 11 new smoke-test functions / 32 new checks (no-args, leading `--continue`, project-only, project-then-`--continue` regression, `--continue` then project, project-then-`--resume <uuid>`, project-then-`--resume` picker, project-then-`--rebuild`, two-positionals rejected, unknown-flag rejected) plus one assertion added to the existing `--help` test. 573/574 smoke checks green; remaining failure is the parked task_013/AC10 diff-scope check, unrelated. Shellcheck clean across all 11 shell files.
- [x] **task_009 — vibe: in-container `/learn` slash command + `/learnings` write-confirm hook (2026-04-26)** — landed in 2 `/vs` cycles. Shape: stop fighting Docker's `fakeowner` overlay on macOS (which silently ignores `readonly: True` on bind-mounts) and make trust come from a hook, not from the mount. Two new PreToolUse hooks: `devcontainer/guard-fs.sh` (NEW, gates Write/Edit/MultiEdit tool calls; uses `realpath -m` to normalize `tool_input.file_path` so `/learnings/../etc/passwd` traversal can't slip through; emits canonical `permissionDecision: ask` JSON envelope per Claude Code hooks docs); existing `devcontainer/guard-bash.sh` extended with `/learnings` write-idiom detection covering shell redirects (`>`, `>>`, `&>`, `&>>`), `tee`, and the file-modifying binaries `cp`, `mv`, `rm`, `ln`, `mkdir`, `chmod`, `chown`, `truncate`, `dd` plus a special three-clause regex for `sed -i` that distinguishes write (`-i`/`-ri`/`-Ei`/`-iE`) from read (`sed -n '/learnings/p'`). Block-beats-ask: existing exit-2 git-push protections preserved byte-identically; if a single command triggers BOTH a /learnings write AND a force-push, exit-2 wins. New `devcontainer/commands/learn.md` slash command captures cross-org learnings into `/learnings/${ts}-${rand}.md` with body byte-identical to host-side `vibe learn` output; refuses cleanly if `/learnings` isn't mounted; push-to-git stays host-only (mention of future `vibe learn --push`). New `devcontainer/claude-md/learn-hook.md` fragment teaches in-container Claude about the hook trust model (45 non-blank lines; sentinel phrases `permissionDecision`, `do not bypass`, `host-only`). README + MANUAL-TESTS.md updated. Bash hook is best-effort defense-in-depth; documented out-of-scope bypass classes: variable-indirection redirects, env-var path commands, interpreter-embedded writes (`python3 -c`, `perl -e`, `node -e`), `eval`, `bash -c <constructed>`, `tee --` end-of-options, GNU long-form `--in-place`. Spec Critic: 3 iterations (4 BLOCKING + 12 MEDIUM/LOW iter-1 → 2 BLOCKING + 5 MEDIUM/LOW iter-2 → pass iter-3 with 3 LOW residuals folded inline). Cycle-1 FAILED on a spec-clarity bug — Generator edited `.claude/settings.local.json` directly, but that file is gitignored and runtime-generated by `vibe`'s heredoc at lines 974–1007, so the edit was wallpaper. Cycle-2 surgical fix: edit moved into vibe's heredoc (durable across container restarts); runtime artifact regenerated to match; AC4 smoke test rewritten to read vibe source instead of runtime file. Final: 522/522 smoke checks passing, 11 shell files clean. Subsumes the prior TODO `vibe learn: /learnings mount is RW, not RO (security)` — the mount being RW is now intended state; the hook is the security boundary. Lays groundwork for the `auto-promote feedback memories to learning library` TODO (same hook-based trust path). Mid-cycle maintenance: retired `test_task008_ac15_no_scope_drift` (it was over-scoped — anchored to live working tree instead of the cycle's own commit, turning into a permanent file-freeze on `guard-bash.sh`/`Dockerfile`/etc); lesson saved as memory `feedback_vs_tester_no_file_freeze_guards.md` so future Tester instructions avoid the pattern.
- [x] **vibe learn: relax confirm to accept y/yes (any case) + tell Claude in container about /learnings (2026-04-26)** — bundled fix shipped in commit `3713a81`. (1) `_learning_capture` confirm now lowercases the user's reply and matches `y`/`yes` (was strict literal `y` only — typing "Y", "yes", "Yes" silently cancelled). (2) Added `devcontainer/claude-md/learnings.md` fragment so every vibe container's CLAUDE.md tells the inside-Claude that `/learnings` exists, what it is, when to consult it, and the boundary that `vibe learn` is host-only. Both originally listed as separate Open entries; collapsed to one Done line since they shipped in one commit.
- [x] **vibe: add LICENSE file + link from README (2026-04-25)** — dropped a standard MIT LICENSE at repo root (copyright Martin Currie 2026); README's "## License" section now reads `[MIT](LICENSE)` instead of bare "MIT". GitHub will show the license pill under the repo header on next push.
- [x] **task_008 — vibe: drain clipboard scratch on exit (race fix) (2026-04-25)** — landed in 1 `/vs` cycle. User-reported bug: `/expaste` then `/exit` left the Mac clipboard unchanged. Diagnosis: the EXIT trap in `/workspace/vibe` killed the host-side `vibe-copy-watcher.sh` before the watcher's 0.5s poll loop saw the just-written `/workspace/.vibe/copy-latest.txt` (or before the in-flight `cp + pbcopy` chain completed). Commit d38fdcc fixed the symmetric startup-clobber bug; this is the corresponding shutdown-drop bug it exposed. Fix: extended the existing trap with a synchronous drain step. New variables `CLIP="$WORKSPACE/.vibe/copy-latest.txt"` and `WATCHER_SEED_MTIME=$(stat -f %m "$CLIP" 2>/dev/null || stat -c %Y "$CLIP" 2>/dev/null || echo 0)`, both block-scoped inside the existing `if Darwin && pbcopy` block, captured AFTER `mkdir -p` and BEFORE the watcher backgrounds. Trap body (single-quoted so `$()` and `$VAR` expand at fire time): `if [ -s "$CLIP" ] && [ "$(stat ... )" != "$WATCHER_SEED_MTIME" ]; then pbcopy < "$CLIP" 2>/dev/null || true; fi; kill "$WATCHER_PID" 2>/dev/null || true`. Drain runs synchronously before the kill; gated on file-non-empty + mtime-changed so a stale file from a prior session is not re-copied; failure-safe so any drain error cannot skip the kill. String `!=` (not arithmetic `-gt`) — `stat -f %m` can return a float on some BSDs; `[ -gt ]` would abort the trap under `set -euo pipefail`. Existing trap line REPLACED (not appended). Spec Critic took 2 iterations (3 BLOCKING + 4 MEDIUM concerns iter-1 → pass iter-2 with 2 LOW residuals folded inline). 1 new test function from Generator (`test_clipboard_drain_on_exit` — 8 source-level greps per AC18 a–h), 3 hardening tests added by Tester (`test_task008_ac3_block_scoping` — variable each declared exactly once, only inside the Darwin block; `test_task008_ac11_direct_read` — negative grep for `cat | pbcopy` and `cp $CLIP $TMP`; `test_task008_ac15_no_scope_drift` — git-diff scope gate). Final: 360/360 smoke checks, code-check clean. Concurrent vibe instances against the same workspace remain out-of-scope (separate design conversation).
- [x] **task_007 — vibe: ship two operational rules (WebSearch-before-refusing + no-SSH-out-without-explicit-OK) to all vibe users (2026-04-25)** — landed in 1 `/vs` cycle. Two new fragment files: `devcontainer/claude-md/web-research.md` (WebSearch-before-refusing rule, 124 lines) and `devcontainer/claude-md/ssh-discipline.md` (no-SSH-out-without-explicit-OK rule, 137 lines). Mechanism: `install-claude-extras.sh` enumerates `*.md` under `devcontainer/claude-md/` (`LC_ALL=C` POSIX byte-order sort), inlines each fragment's contents (preceded by `<!-- vibe-md: <basename> -->` header) into a delimited managed block at the END of `~/.claude/CLAUDE.md`; coexists with write-env-hint.sh's TOP-of-file block (different delimiters: `<!-- >>> vibe-managed (auto, do not edit) >>>` / `<!-- <<< vibe-managed <<< -->`). Block is regenerated (not appended) on each container start; user content outside both managed blocks is byte-preserved; empty/missing source removes any pre-existing block; idempotent (sha256-stable on re-run). Generator hit one correctness bug: `close` is an awk reserved keyword so the strip-block awk variable was renamed to `closetag`. 14 new test functions / 69 new smoke checks, all 333/333 pass. Spec Critic took 2 iterations (3 high-blocking concerns inline-resolved: dropped unverified `@vibe-md/<file>` import shape in favor of inline prose, pinned block position, pinned `LC_ALL=C` collation; 12 medium/low concerns fixed; 3 residual builder-awareness notes folded inline). Planner amendment 2026-04-25 extended scope from 1 fragment to 2 by adding AC1b + t14; mechanism was already multi-fragment-capable so the extension was pure content. Extension point established for future operational rules — drop a new `*.md` in `devcontainer/claude-md/` and it ships to every container on next start.
- [x] **vibe: `--rebuild` cache fix — resolve Claude Code `latest` host-side and pass as build-arg (2026-04-25)** — bug: `vibe --rebuild` was building a stale Claude Code version (user hit 2.1.117 when npm `latest` had moved on to 2.1.119) because Dockerfile's `RUN npm install -g @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}` with `CLAUDE_CODE_VERSION=latest` is byte-identical between rebuilds — Docker reuses the cached layer regardless of what `latest` actually points to. Fix in `/workspace/vibe` build block: probe `npm view @anthropic-ai/claude-code version` host-side (falls back to `latest` if npm isn't installed), pass result as `--build-arg CLAUDE_CODE_VERSION=<resolved>`, and print the resolved version in the build banner. Build-arg goes into the layer cache key, so a moved `latest` invalidates the npm-install layer. Tests green: shellcheck clean, smoke tests pass.
- [!] **vibe: `/expaste` slash command — reverted (2026-04-25)** — shipped `devcontainer/commands/expaste.md` and persistent-volume copy under a misread instruction; user wanted the Stop-hook auto-write, not a /c-with-exit-nudge wrapper. Both files deleted. Lesson saved as `feedback_no_empty_corrections.md`: I had broken the continuous-numbering rule the turn before, the user's selection ("Second 2 then First 3") was unambiguous only against my malformed multi-list response, and instead of collapsing the lists and re-asking I just acted on the parse — shipping the wrong feature.
- [x] **task_006 — /c slash command + host-side clipboard watcher (2026-04-23)** — 2 `/vs` cycles. Cycle 1 passed on features (watcher, c.md rewrite, vibe integration, retirement loop, 10 new AC19 tests) but failed regression gate when Generator added `REPO.glob("*.sh")` to `code-check.py:scripts()`, which broke the literal-string `.replace()` in `smoke-test.py:_patched_code_check()` and sent 3 pre-existing AC9 code-check tests into "scanning whole repo" mode. Cycle 2: surgical revert of `code-check.py` only; 264/264 checks green. Shape: `/copy` → `/c` rename; skill body drops the Bash-tool `vibe-copy` invocation, writes directly to `/workspace/.vibe/copy-latest.txt` via the Write tool (AC2); `vibe-copy-watcher.sh` at repo root polls the scratch file (`stat -f %m` mtime, 0.5s interval, fswatch fast-path when available) and pipes changes to `pbcopy` via `VIBE_COPY_CMD` hook (pbcopy default; test hook for Linux CI). `VIBE_COPY_WATCHER_FORCE=1` bypasses the Darwin guard for CI. `vibe` launcher derives `VIBE_REPO_DIR="$(dirname "$DEVCONTAINER_DIR")"`, `mkdir -p "$WORKSPACE/.vibe"`, orphan-reaps with canonical pattern `pkill -f "vibe-copy-watcher\.sh ${WORKSPACE}\$"` (end-of-line anchor defeats path-prefix collisions — AC19i verifies), spawns watcher backgrounded, captures PID, `trap 'kill "$WATCHER_PID" 2>/dev/null || true' EXIT`. `install-claude-extras.sh` gains commands-only `RETIRED_COMMANDS=("copy.md")` allow-listed cleanup so existing persistent volumes lose the old slash command on next container start. Linux hosts: watcher no-op, `/c` still writes the scratch file (dead drop). Spec Critic took 2 iterations (13 concerns iter-1 → pass iter-2 with 2 residual non-blocking). Spec defect noted: AC11/AC19h assumed `vibe`'s final line had an `exec devcontainer exec` prefix; it never did — AC19h passes trivially on baseline.
- [x] **task_005 — vibe: OSC 52 clipboard bridge `/copy` + `vibe-copy` (2026-04-23)** — landed in 1 `/vs` cycle. Inside-container exact-byte copy to the Mac clipboard via the OSC 52 escape sequence. Two surfaces: `devcontainer/vibe-copy.sh` shell helper installed at `/usr/local/bin/vibe-copy` (stdin or file arg; `base64 | tr -d '\n'` for GNU/BSD portability; `printf` not `echo -e` to avoid trailing newline; subshell-wrapped TTY write to suppress bash-level redirect noise; 8 KiB warn / 1 MiB refuse / TTY-absent note; normative exit-code + stderr-message table with every path tested); `devcontainer/commands/copy.md` slash command synced via `install-claude-extras.sh` (picks most recent fenced block in prior assistant turn, or `$ARGUMENTS`-matched; UTF-8-only; refuses cleanly on no-prior-turn and no-block-in-turn). Dockerfile gains one `COPY vibe-copy.sh /usr/local/bin/vibe-copy` (canonical form, NOT trailing-slash — Spec Critic iter-2 caught this as a block-level trap) plus chmod include. `install-claude-extras.sh` gains `SRC_ROOT="${VIBE_EXTRAS_SRC_ROOT:-…}"` env override so the extras-sync is now host-testable. 13 new smoke tests / 52 new checks; 194/194 pre-existing still green. Target terminal Ghostty; Terminal.app + stock terminals fall back to bind-mounted scratch file at `/workspace/.vibe/copy-latest.txt`. Spec Critic took 2 iterations (resolved 15 iter-1 concerns + 4 loopholes + 7 missing criteria, caught 1 iter-2 Dockerfile ambiguity, patched inline at max). **Amendment (2026-04-23):** OSC-52-from-tool-call design proven non-viable — inside a Claude Code Bash tool call stdout redirects to file and `/dev/tty` isn't connected to the terminal; superseded by task_006's host-side watcher approach.
- [x] **vibe learn: banner subshell fix + host-only docs + `excluded` state (2026-04-23)** — real-world test surfaced two bugs + one UX gap. (1) `OVERRIDE_CONFIG=$(_learning_build_override_config …)` loaded the learning config inside the `$(...)` subshell; exports died with the subshell, so the parent shell's `learning_should_mount` banner check at vibe:1002 always returned 1 → `learn : <vis> @ <path>` line never printed even though `/learnings` was correctly bind-mounted. Fix: call `learning_load` in the parent shell before the subshell. Verified: reverting the fix fails exactly one new check. (2) User typed `vibe learn --exclude` inside the container where `vibe` doesn't exist. Tightened header comment, bare-`learn` usage, and added a "run on the HOST shell" note so the constraint is visible without guessing. (3) Banner was silent both when never inited *and* when project explicitly opted out — indistinguishable from the user's side. Added `learning_banner_state` helper (returns `silent` / `enabled` / `excluded`); banner now prints `learn : excluded` to confirm a `.no-learn` took effect while keeping silence for default-off. 7 new smoke tests total (banner source-ordering regression + 2 host-only help assertions + 4 banner-state cases); all prior checks still green.
- [x] **/vs: token-spend logging redesign — opt-in via `--cost`, honest about untrackable Opus tokens (2026-04-23)** — superseded the original always-on token-spend logging. Now: default OFF; `/vs --cost <prompt>` enables logging for one run; on enable Planner prints a one-screen disclosure (subagent Sonnet/Haiku auto-captured per dispatch; Opus director+evaluator NOT auto-trackable from inside a session per claude-code-guide research — no hook field, no env var, no CLI flag, no documented JSONL schema; user can run /cost in their terminal and tell me the number to fold in). Cost report keeps Sonnet and Haiku on separate lines (different real per-token costs); Opus reported as user-provided value OR "unknown — run /cost to include". Honest about what can't be measured rather than reporting "0 opus" silently. Lesson saved as feedback memory `feedback_disclose_tracking_constraints.md`.
- [x] **vibe: marker rename + opt-out helper subcommands (2026-04-23)** — renamed marker filename `.vibe-no-learn` → `.no-learn` (legacy still recognized for backward compat). Added `vibe learn --exclude` (creates `.no-learn` in cwd; refuses in `$HOME`) and `vibe learn --include` (removes `.no-learn` and legacy `.vibe-no-learn`). Both idempotent. Avoids users having to remember/type the marker filename. 14 new smoke checks; all 144 pre-existing pass.
- [x] **task_004 — vibe: cross-org learning library (opt-in, host-only capture, public/private) (2026-04-23)** — landed in 1 `/vs` cycle. `vibe learn --init` writes `~/.vibe/learning.config` (chmod 600); `vibe learn "<pattern>"` host-side captures with confirm-before-write (EOF defaults to cancel); public mode prompts per-capture for `git commit --file=<tempfile> && push`; library RO-mounted at `/learnings` via per-session generated `--override-config` JSON; `.vibe-no-learn` marker opts a project out (walk halts at `$HOME`; unset `$HOME` = fail-safe opted-out). 10 AC14 helpers exposed under `VIBE_SOURCE_ONLY=1`. Strict regex parser (no source/eval) refuses code injection — verified independently. 57 new smoke checks across 27 test functions. cost: 4 subagent calls, 218,672 tokens (119,821 sonnet, 98,851 haiku), 822s wall.
- [x] **/vs: token-spend logging (2026-04-23)** — added instructions to `devcontainer/commands/vs.md` (synced to runtime) so Planner/Evaluator parse the `<usage>` block from each Agent dispatch and append a record to `.vs/cycle-N/cost.json` (role, model, iteration, total_tokens, tool_uses, duration_ms, timestamp); on Step-7 pass, Evaluator rolls up to `.vs/cost-summary.json` (committed) and reports one cost line in the pass verdict. No dollar estimates — Pro/Max is flat-rate, tokens are a rate-limit-pressure proxy. Decision recorded in vs.md: revisit if subscriber per-token cost becomes meaningful.
- [x] **task_003 — prize-worthy haiku about vibe → `HAIKU.md` (2026-04-23)** — first end-to-end exercise of `/vs --fuzzy`. Generator (Sonnet) wrote a 5/7/5 haiku evoking 3+ vibe-essence elements; independent Reviewer (Sonnet) returned `pass` with three competitive-margin concerns; Evaluator concurred. Mode validated: Reviewer produced a thoughtful per-AC assessment + concerns, not rubber-stamp or syllable-pedantry. Cycle 1.
- [x] **/vs v3: `--fuzzy` mode + simplicity-gate choice offer (2026-04-23)** — added `--fuzzy` flag that swaps Haiku Tester for a Sonnet Reviewer producing a written verdict (per-AC assessment + concerns + pass/revise/fail), so tasks without mechanical acceptance criteria get adversarial review. Step-1 simplicity gate no longer hard-refuses on "no verifiable criteria"; instead it offers two options to the user: tighten the brief with sketched deterministic criteria, or reply `--fuzzy` to proceed in review mode. State directory adds `cycle-N/reviewer-verdict.md` (gitignored). Immutable-tests rule scoped to rigorous mode only; regression gate becomes manual in fuzzy mode (Reviewer flags suspected regressions, Evaluator runs pre-existing tests by hand). All in `devcontainer/commands/vs.md`; synced to `~/.claude/commands/vs.md`.
- [x] **vibe: `--continue` / `--resume` flags for Claude conversation pickup (2026-04-23)** — bare `vibe --continue` → `claude --continue` (most recent in cwd); `vibe --resume` → `claude --resume` (picker); `vibe --resume <uuid>` → `claude --resume <uuid>` (specific session); flags mutually exclusive; `--rebuild` unchanged. Banner reflects mode (`↻ vibe session continuing` / `resuming (picker)` / `resuming (Claude session <prefix>…)`). Helpers (`build_claude_resume_args`, `is_uuid`) sit before the `VIBE_SOURCE_ONLY=1` guard for unit testability. 17 new smoke tests covering all four modes + 8 UUID-validation cases + help-text checks; all pre-existing tests still green.
- [!] **task_002 — vibe: persistent per-repo container reattach — abandoned at spec-approval (2026-04-23)** — specced as container-layer reattach + `--fresh` + `VIBE_IDLE_TIMEOUT`. Two `/vs` Spec Critic iterations polished the technical detail. When user reviewed the polished spec they identified the framing was wrong: actual goal is Claude *conversation* persistence, not container layer. Container reattach already works via devcontainer CLI labels; idle-timeout solved a problem that didn't matter. Replaced by the `--continue`/`--resume` Open item above. Lesson saved as feedback memory `feedback_confirm_persistence_meaning.md`. Process gap: Spec Critic audits the spec text, not whether the spec solves the user's actual problem.
- [!] **vibe: cloud/async runner — considered, dismissed (2026-04-23)** — investigated routing long jobs to Anthropic infrastructure for fire-and-forget execution. Verdict: **dismissed for now**. Claude Agent SDK is API-billed only (subscription credentials explicitly cannot be used — hard auth boundary, key-only); Claude Code has no native cloud mode; Managed Agents not confirmed available under Pro/Max. Project goal is to be the best vibe-coding tool for Pro/Max users *without* paying API extras, so any cloud mode would betray that. **Revisit when** Anthropic ships a cloud/async path that runs against Pro/Max subscription quota (no separate API billing). Reference: github.com/anthropics/claude-agent-sdk-python/issues/559.
- [x] **/vs v2: spec critic + Haiku Tester + regression gate** — inserted Sonnet Spec Critic between Planner and Generator (catches fuzzy criteria, scope loopholes, type/schema gaps before any code runs); switched Tester to Haiku 4.5 (mechanical work, cheaper); made regression check on pre-existing test suite mandatory (was discretionary). All in `devcontainer/commands/vs.md`; synced to `~/.claude/commands/vs.md`. Steps renumbered 1–7.
- [x] task_001 — `--json` output for `code-check.py`. Single `argparse` flag; emits one JSON object on stdout (`tool`, `shellcheck_version`, `files_checked`, `findings`, `summary`); exit codes preserved (0 clean, 1 issues, 2 missing-shellcheck → JSON error object); default human output byte-identical. Verified via `/vs` cycle 1: 44/44 new smoke tests + 12/12 pre-existing pass. Spec/log under `.vs/`.
- [x] Implement `/vs` — adversarial agentic harness slash command. Shipped as `devcontainer/commands/vs.md`. Planner → Generator → Tester → Evaluator, independent Sonnet subagents, immutable tests, file-based state under `.vs/`, soft cycle ceiling proposed by Planner (user-overridable with `--max N`). Nomenclature and patterns follow Anthropic's harness-design articles.
