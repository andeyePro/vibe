# /vsss session — start 2026-07-10T11:07:55Z — budget 5h — sessions 5

## Initial plan
**Invocation**: `/vsss fix this --sessions 5`
**START_TIME**: 1783681675 (11:07:55 UTC = 12:07 BST)
**Budget**: 5h per window (default); up to 5 credit windows total (`--sessions 5` → marker remaining=4).
**Args context** ("fix this" = three items from the preceding conversation, in Martin's priority order):

1. **[Headline] `--sessions X` auto-resume never fires on token exhaustion.** Empirical failure 2026-07-09 night: 3 vibe instances running `/vsss --sessions 5` hit the credit limit mid-session; interactive `claude` did NOT exit — it displayed a picklist ("stop and wait" / upgrade options) and blocked forever. The launcher's relaunch loop (vibe:~1651+) only triggers on claude EXIT, so all three sat at the picklist all night. Root design flaw: the launcher-side contract assumes claude exits on credit exhaustion; interactively it does not. Fix so overnight `--sessions` runs actually span windows.
2. **Ctrl-C during auto-resume countdown leaves marker active** (finding 3 of PR #12, Martin chose option c): add a trap so Ctrl-C during the countdown clears the marker (`active=0`) — cancel means cancel. Crash-left markers stay trusted deliberately: Martin confirmed a crash SHOULD resume ("if Claude crashes we really want it to resume").
3. **`vibe` always exits 0** (finding 4 of PR #12): capture claude's exit status past the `|| true` and propagate it as vibe's exit code.

**Priority queue**: item 1 first (research claude CLI behaviour on credit exhaustion → design → implement), items 2+3 are small bounded launcher edits (can ship in same or separate commit).
**Hard-escalate awareness**: edits touch `vibe` launcher only — allowed. No firewall/hook/settings edits anticipated. Push policy: local commits only.

## Iter 1 (in progress) — /vss → /vs task_016
**Plan**: /vss Opus planner chose `/vs --gen sonnet`. Research (claude-code-guide agent): NO supported auto-exit/auto-wait/hook for the usage-limit picker — interactive claude blocks by design; headless/-p exit code on limit undocumented. Design: container-side heartbeat via inline PreToolUse/PostToolUse/Stop hook entries in the settings.local.json heredoc (project-scoped .vss/heartbeat; transcript mtime is cross-instance contaminated because the vibe-claude-config volume + `-workspace` slug are shared) + host-side stall watchdog (backgrounded exec-client PID, in-container pkill primary / client-PID kill fallback, three-part gate: marker active=1 AND marker-refreshed-this-session via portable `-nt` ref file (relaunches arm unconditionally) AND heartbeat stale >VIBE_STALL_SECS default 1800). Plus PR#12 findings: countdown INT trap → auto_resume_deactivate (crash-left markers still resume, per Martin), and CLAUDE_EXIT propagation.
**Spec critic**: iter 1 = revise (3 BLOCKING: PID-kill unproven → in-container kill primary; Ctrl-C semantics under backgrounding → raw-TTY rationale + no EXIT-trap edits; remaining=0 last window unprotected → gate on active, not pending). iter 2 = revise (2 BLOCKING: test_vibe_final_line_no_exec dependency; false-positive kill of live sessions → -nt ref gate + PreToolUse heartbeat + 1800s default). iter 3 dispatched.
**Files touched so far**: .vs/spec.md (task_016), .vs/cycle-1/spec-critique*.md, archived task_015 (commit 65ef8e8).
**Cycle results**: cycle 1 = Generator (sonnet) all 13 ACs, gates green, but Evaluator FAILED the cycle on test quality (haiku Tester's negative watchdog tests were vacuous — passed even with a broken kill gate). Tester escalated haiku→sonnet per ladder. Cycle 2 = surgical rewrite of test_vibe_stall_watchdog_functional only; negatives now stub the kill fn + assert killfile absence + sample live PID at t=3s; T1 also proves host-PID fallback. Evaluator re-ran gates independently: smoke 1048/1048, shellcheck clean 15 files. PASS.
**Files touched**: vibe (+247), smoke-test.py (+~420, Tester-owned), .gitignore, MANUAL-TESTS.md (Test 32, 6 sub-cases), devcontainer/commands/vsss.md (watchdog contract), CHANGELOG.md, TODO.md (task_016 entry removed on close per done-items-live-in-CHANGELOG), .vs/{spec.md,progress.md,tasks.json}.
**Commit**: (next line after this write)
**Outcome**: success
**Notes**: mid-run Martin asked (a) status-line model letter F/O/S/H — queued as next iteration candidate; (b) whether --fable forces Fable in /vs etc — answered no, chair only. Empirical side-finding worth keeping: hooks DO fire for subagent tool calls in current claude CLI (probe-tested in-container; refutes two old GitHub issues) — guard-bash's subagent coverage is real.
